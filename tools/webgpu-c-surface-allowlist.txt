# WebGPU C-surface allowlist (#505) — see docs/webgpu-c-surface-audit.md
# Every renderer rae_ext_(Gbuffer|Gpu3d)_* C entry point permitted to exist.
# Adding a line = reviewable event (justify in the commit); removing one = always fine.
# Enforced by tools/webgpu-c-surface-gate.sh (make c-surface-gate). Lines starting with # are ignored.
#
# #862 generic asynchronous buffer-map platform ABI (outside renderer pattern):
# rae_wgpu_read_start: retained buffer plus request-owned C callback state.
# rae_wgpu_read_poll: single nonblocking context event poll and request status.
# rae_wgpu_read_copy: bounded completed-map copy; no retained destination pointer.
# rae_wgpu_read_release: cancel/unmap and release owner, preserving late callback state.
# Known debt tracked separately: #513 removed the dead gbuffer_{debugView,ssao};
# #514 migrates/retires the forward renderer (rae_ext_Gpu3d_* pass encoders).
# #842 platform ABI accessors (outside this gate's rae_ext_* pattern):
# rae_gb_lit_texture: borrowed source texture for a Rae-encoded HDR copy.
# rae_gb_lit_copy_texture: borrowed destination, owned by deferred target lifecycle.
# rae_gb_lit_copy_view: borrowed sampled snapshot view for transparent materials.
# #843 platform ABI:
# rae_gb_lit_format: the HDR target's runtime-chosen format (rg11b10 vs rgba16f), so the
#   Rae-built transparent blend pipeline targets exactly what lighting wrote.
# (#843's rae_gb_transparent_{pipeline,bind} / rae_gb_set_* slot stores, and the AO / composite /
#   light / taa / pyramid / inspector parked slots, were REMOVED by #904: those objects are typed
#   IDs in the DeferredRenderer-owned gpu/GpuResources manager; the C-owned inputs are adopted.)
# (#907: the 2D box pass moved to Rae (lib/Gpu2dBox.rae over a manager); the C rae_g2d_* platform
#   accessors it reads (prim_count/floats/data/clip_at/reset, viewport_uniform, clip_frame_uniform,
#   scissor, prepare_flush) are outside this gate's rae_ext_* pattern, like rae_g2d_pass_get.)
# (#908: the 2D image pass moved to Rae on the same canvas; C keeps CPU decode only
#   (rae_g2d_decode_image/decoded_width/decoded_height/decode_free, rae_g2d_current_clip) and lost
#   every rae_ext_Gpu2d_*Image* entry point — outside this gate's pattern, listed for the record.)
# (#909: the 2D text pass moved to Rae on the canvas too; C keeps the per-atlas glyph batch +
#   CPU atlas pixels (rae_g2d_text_*, rae_sdf_atlas_*) and rae_ext_Gpu2d_flush is gone —
#   outside this gate's pattern, listed for the record.)
# (#910: the 2D frame encoder/pass are canvas-owned; rae_g2d_set_frame/pass_get/encoder_get/
#   frame_active, keep_frame_buf/bind and clip_frame_uniform are gone; rae_g2d_scissor takes the pass
#   and rae_g2d_clip_uniform_at fills a Rae buffer — outside this gate's pattern, for the record.)
# (#913: the 2D box batch is Rae; rae_g2d_push/push_gradient, the rae_g2d_prim_* accessors and the
#   rae_ext_Gpu2d_draw{Rect,RoundedRect,Box,GradientRect,Line} entry points are gone — for the record.)
# (#915: the 2D frame is a manager Recording, the clip stack + scissor + viewport uniform and the
#   text glyph batch are Rae; rae_g2d_scissor/clip_uniform_at/prepare_flush/current_clip/
#   viewport_uniform, rae_g2d_text_{floats,count,data,clip_at,reset} and the
#   rae_ext_Gpu2d_{pushClipRect,pushClipRoundedRect,popClipRect,drawGlyph,drawGlyphEx} entry points
#   are gone; rae_g2d_xform(out) is the one addition (frame-derived uniform data) — for the record.)
# (#920: the offscreen presentable target is the canvas's texture; rae_g2d_off_view/off_view_ready
#   and rae_g2d_present_and_cleanup are gone; rae_g2d_present(texture, w, h) / rae_g3d_present_frame(...)
#   / rae_ext_Gbuffer_present(...) BORROW the texture for the drawable copy + headless readback, and
#   rae_g2d_surface_ready/width/height report the configured surface — surface glue, for the record.)
# (#906 slice a: the geometry FRAME is the Rae GbufferCache's — the four G-buffer targets + views,
#   the frame uniform, the draws buffer + cursor and a manager Recording per frame (the multi-
#   attachment beginRenderPassMulti); rae_gb_{set_target,targets_match,targets_ready,
#   release_targets_ext,set_frame_ubuf,set_draws_buffer,frame_ubuf,draws_buffer,frame_bytes,
#   draws_size,max_draws,draw_count,advance_draws,frame_uniform,set_frame,pass,encoder,
#   clear_frame,frame_active} and rae_ext_Gbuffer_drawCount are gone; rae_gb_frame_data(out) fills
#   the Rae buffer (frame-derived uniform math, the rae_g2d_xform pattern), rae_gb_commit_targets
#   hands C the BORROWED views + size for its still-C post passes (forget_targets at shutdown),
#   rae_gb_set_frame_open flags the pass for the C metaball prep — for the record.)
# (#923 slice 3c: the deferred POST-PASS targets (lit, litCopy, AO, TAA x2, the depth pyramid mip chain),
#   the light + TAA uniforms, the lighting / TAA / pyramid pipelines and the frame-derived math (jitter,
#   previous view-projection, the SSAO / lighting uniform packing, fog, the TAA switch) are Rae manager
#   objects (lib/GbufferTargets.rae, GbufferCache); rae_gb_{frame_data,commit_targets,forget_targets,
#   view_a/b/c/depth,targets_gen,motion_zero,ssao_upload,light_upload,light_ubuf,light_bytes,light_pipeline,
#   lit_view,lit_texture,lit_copy_texture,lit_copy_view,lit_format,ao_view,taa_*,set_taa_enabled,
#   set_fog,pyramid_ready,pyr_*,composite_source_view,composite_source_index} and the gated
#   rae_ext_Gbuffer_{deferredShutdown,pyramidMips,skyHosekPush} are gone (allowlist -3, gate 7);
#   rae_gb_deferred_prepare keeps only the device/surface check + the shadow defaults, the WGSL sources
#   are read through rae_gb_{light,taa,pyr_from_depth,pyr_reduce}_wgsl, rae_wgpu_have_rg11b10 is the lit
#   format capability, and rae_gb_shadow_{frame_ubuf,array_view,sampler} stay until #925 — for the record.)
# (#925 slice 3b-ii: the cascaded SHADOW MAPS are the renderer's Rae ShadowCache (lib/ShadowMaps.rae +
#   lib/ShadowMapsSdf.rae on RenderPasses.shadow / Renderer3d.shadow): the depth array texture + layer /
#   array views + comparison sampler (new manager support in gpu/GpuTextures), the static / skinned /
#   metaball depth-only pipelines (gpu/GpuRenderDepth over the C-kept rae_sm_wgsl_{static,skinned,sdf}
#   sources), the per-cascade uniforms, binds, caster queue, the 320-byte shadow frame uniform (incl.
#   shadowCfg + the ambient knob) and the per-cascade depth passes (beginRenderPassMulti, depth-only);
#   the lighting bind names its IDs. rae_sm_{begin,queue_mesh,queue_skinned,record_metaballs,shutdown,
#   ready,upload_models,set_shadow_ambient,cascade_count,draw_count,layer_view,caster_*,draw_metaballs},
#   rae_gb_shadow_{frame_ubuf,frame_bytes,array_view,sampler} and C's g3d_sm_* state are gone;
#   rae_g3d_set_shadow_inputs(ubuf, view, sampler) hands the FORWARD scene / skin binds the three
#   inputs BORROWED (the Renderer3d pushes them before its scene pass; C rebuilds the binds on change)
#   and rae_gb_deferred_prepare is the device/surface check only — for the record.)
# (#922 slice b-i: the deferred METABALL clusters are Rae manager objects on the GbufferCache
#   (lib/GbufferSdf.rae: pipeline over the C-kept rae_gb_sdf_wgsl, frame uniform from the cache's frame
#   data — inverse of the JITTERED viewProj computed in Rae — per-cluster ball/colour/param buffers +
#   binds); rae_gb_sdf_prepare/pipeline/bind, rae_ext_Gbuffer_sdfShutdown and rae_gb_set_frame_open
#   (the last C coupling to the open geometry pass) are gone — for the record.)
# (#921: the SKINNED meshes + the joint palette are the Rae SkinStore (lib/SkinStore.rae) on the
#   renderer (GbufferCache.skins / Renderer3d.skins); rae_ext_Gpu3d_skinnedMeshCreate/setPalette, the
#   rae_gb_skin_{ready,vbuf,ibuf,icount,palette,palette_size,palette_ready} and rae_g3d_skin_{vbuf,ibuf,
#   icount} accessors and C's g3d_skin_* tables + palette buffer are gone; rae_sm_queue_skinned takes the
#   caster's buffer handles + the palette handle and rae_g3d_push_skinned_draw the palette handle, both
#   BORROWED for the frame (the C skin binds are rebuilt when the handle changes) — for the record.)
# (#905: the static mesh store is the Rae MeshStore (lib/MeshStore.rae) owned by the renderer;
#   rae_ext_Gpu3d_meshCreate/meshUpdate and the rae_gb_mesh_*/rae_g3d_mesh_* accessors are gone;
#   rae_sm_queue_mesh takes the caster's buffer handles (borrowed for the pass) — for the record.)
# (#912 removed the geometry-pass slots the same way: rae_gb_{set_,}{pipeline,skin_pipeline,static_bind,
#   skin_bind,terrain_pipeline,terrain_bind,terrain_tex_view,terrain_sampler,terrain_blend} and
#   rae_gb_entry_{vs,fs}; the C material array's view is read via rae_gb_terrain_array_view.)
# (#830's rae_gb_water_set / _get / _release slot store was REMOVED by #873: water's GPU objects are
#   typed IDs in the App-owned gpu/GpuResources manager; the gbuffer frame resources it draws with are
#   adopted externals, not C slots.)
# present: no-UI final blit to the swapchain; reached from Rae as
# gbuffer.present(canvas:) -> presentFrame(canvas:) -> renderDeferredPass. Platform copy-to-drawable
# of the CANVAS-owned offscreen texture it is handed (#920); C owns nothing of the target.
rae_ext_Gbuffer_present
rae_ext_Gbuffer_shutdown
rae_ext_Gpu3d_shutdown
rae_ext_Gpu3d_skinDrawCount
rae_ext_Gpu3d_skinFrameBegin
rae_ext_Gpu3d_skinShutdown
rae_ext_Gpu3d_skyHosekPush
