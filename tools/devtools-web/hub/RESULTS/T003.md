# T003 - Document compiler test runner .cmd features

## Summary
- added `rae/compiler/tests/README.md` describing test case structure, optional `.cmd` overrides, and the `{{TMP_OUTPUT}}` placeholder
- documented how the runner swaps the placeholder for temp files, enforces no-stdout rules, and cleans up artifacts
- provided quick instructions for adding new tests and running `make test`

## Testing
- `cd rae/compiler && make test`
