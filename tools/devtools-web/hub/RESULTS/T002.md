# T002 - Document Rae format output workflows

## Summary
- expanded `rae/README.md` CLI section with `rae format` usage examples for stdout, `--write`, and `--output`
- updated `rae/compiler/README.md` to describe formatter options in detail, including mutual exclusivity
- added a "Tooling: Formatter CLI" appendix to `rae/spec/rae.md` so the language spec references the available formatting workflows

## Testing
- `cd rae/compiler && make test`
