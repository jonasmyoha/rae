# T015 - Main loop example (indefinite, stop button)

## Summary
- Moved the main loop example into `examples/main_loop/` with devtools metadata and an infinite tick loop using `nextTick`.
- Enabled the devtools Stop button for any active example run and disabled example actions while a run is active.

## Testing
- Not run (not requested).
