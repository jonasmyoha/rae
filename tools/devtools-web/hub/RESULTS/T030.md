# T030 - Enhance Advanced Pong

## Result
Completed.

## Changes
- **advanced_pong/main.rae**:
  - Added `p1Score` and `p2Score` tracking.
  - Implemented `drawUISystem` to display scores at center top using string interpolation: `"{p1Score} - {p2Score}"`.
  - Updated `paddleCollisionSystem` to increase horizontal ball speed by `0.5` on each hit.
  - Added logic to increment scores when the ball goes out of bounds.
- **pong/main.rae**:
  - Added `p1Score` and `p2Score` tracking.
  - Implemented score display using `drawText` and string interpolation.
  - Updated collision logic to increase ball speed by `1` on each paddle hit.
  - Reset ball speed after each goal.

## Notes
- The game now has a clear progression as the ball speeds up, making it harder to play over time.
- String interpolation proved very useful for the UI display.
