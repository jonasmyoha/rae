# T006 - Devtools: list all compiler tests

## Summary
- test list now merges filesystem discovery with live run results, so every `.rae` test appears with pass/fail/pending status even before running the suite
- added periodic + post-run refresh of the test file tree, and wired it into the existing source viewer so changes are reflected without restarting the server
- added placeholder badges/states for tests that have not run

## Testing
- `cd rae-devtools-web && bun run lint` *(tsc exited immediately without diagnostics; unable to capture additional output in this environment)*
