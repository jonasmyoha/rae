# T008 - Compiler: plan multifile build support

## Summary
- captured the current single-file limitations, desired project layout, and CLI experience for a future `rae build` command in `docs/multifile-plan.md`
- outlined a phased architecture (filesystem loader → `ModuleGraph` → CLI emitters) plus a Pong-style demo to validate multi-file wiring
- enumerated follow-up tickets (T010–T015) with rough effort so we can queue implementation work incrementally

## Testing
- `cd rae/compiler && make test`
