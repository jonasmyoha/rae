# T007 - Devtools: unify test run + code viewer UI

## Summary
- Restructured the test panel so the test list and inspector live side-by-side: selecting a test now shows its run status/output and the formatted source via Result/Source tabs
- Removed the old standalone source browser and wired the inspector to auto-load the selected test's `.rae` file using the filesystem discovery map introduced in T006
- Added visual states for pending tests, tab controls, and inline copy-to-clipboard for the currently selected source

## Testing
- `cd rae-devtools-web && bun run lint` *(tsc exited immediately without diagnostics; unable to capture additional output in this environment)*
