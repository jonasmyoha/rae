# T004 - Add regression test for `rae format --write`

## Summary
- added `{{TMP_INPUT}}` placeholder support to `compiler/tools/run_tests.sh` so tests can run the formatter against a temporary copy of the source and compare the rewritten file
- created `tests/cases/204_format_write.*` covering `rae format --write` on messy input and ensuring the rewritten file matches canonical output with no stdout
- wrote `compiler/tests/README.md` describing the `.cmd` overrides plus the new placeholder semantics

## Testing
- `cd rae/compiler && make test`
