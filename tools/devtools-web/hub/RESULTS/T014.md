# T014 - Main loop test (short-run)

## Summary
- Registered a `nextTick` VM native for `rae run`/`--watch` and used it to drive a deterministic main loop test.
- Added `312_vm_main_loop` runtime test cases to verify 5 tick outputs.

## Testing
- Not run (not requested).
