<!-- SUMU_QUEUE_START -->
- [x] #641 Inline representation for `opt T` (drop the RaeAny box). Motivation: `opt T` for a non-reference value type is realized as a heap-boxed `RaeAny` (malloc(sizeof(payload)) + copy + rae_any_owned_ptr, emitted around compiler/src/c_stmt.c:308-338; C type RaeAny at c_backend.c:603). Only the `if let` peepholes on `at`/`viewAt`/`modAt` avoid the malloc, and they are keyed on library function-name strings (emit_list_if_let, c_stmt.c:32-120), so every new accessor is another shape to recognize and every unrecognized shape is a heap cliff (the §8 bug); the representation is the root cause, the peepholes treat a symptom. Scope: lower `opt T` for non-reference `T` to a per-type monomorphized `struct rae_opt_T { bool has; T value; }` (the mechanism `List(T)` already uses); `opt T` needs a drop only if `T` does (type_needs_cascade_drop); reserve `RaeAny` for `Any` only. NOTE (verified, corrects the report): `opt view T`/`opt mod T` are ALREADY plain 8-byte nullable reference wrappers (null .ptr = none, c_backend.c:596-603) — they are NOT boxed and need NO change; this task is only about `opt T` for non-reference T. Then delete the name-keyed `at`/`viewAt`/`modAt` peepholes, or demote them to a pure optimization behind a test proving the generated C is equivalent without them, so the accessors become small ordinary functions the C compiler inlines. Acceptance: no `malloc` in generated C for any call shape of `at`/`viewAt`/`modAt`, including `let maybe: opt Particle = particles.at(index: index)` stored and passed to another function; a user function returning `opt Vec3` produces no heap traffic; the report's layout table still holds; peephole code removed or demoted with the equivalence test in place. Deps: none required; if done after #645 the peephole removal is cleaner; blocks the peephole-removal half of #644. DONE 2026-08-26: struct/aggregate `opt T` now lowers to a monomorphized `struct rae_opt_<T>{ rae_Bool has; T value; }` (0 malloc in emitted C for `at`/`viewAt`/`modAt` and for a user `ret opt Vec3`; `opt Vec3`→`struct rae_opt_rae_Vec3{has,value}` verified; case suite 317/0). Scoped to aggregate payloads: scalar/String `opt` stay on the already-malloc-free `RaeAny` union, and the `at`/`viewAt`/`modAt` peephole is kept as a pure optimization. Follow-ups filed: #651 (finish "RaeAny for Any only" — move scalar/String opt off the union), #652 (pre-existing generic-`get`/`at` double-free on heap-owning struct payloads, surfaced here).
- [x] #642 `opt T` to `T` direct assignment is a sema error for every `T`. Motivation: `let particle: Particle = particles.at(index: index)` emits non-compiling C (a RaeAny box assigned into a struct-typed C variable; emit_opt_unbox_suffix, c_call.c:21-59, emits no suffix for struct payloads), while `let count: Int = list.at(index: index)` does NOT trap on none — the scalar path unconditionally reads the union (.as.i/.as.f/...) so a `none` silently yields zero with no diagnostic (this corrects the report's "auto-unwraps trivially": it is a silent-zero bug, not benign). Two different wrong behaviors for one situation. Scope: one rule — assigning `opt T` to a non-optional binding requires `if let`, a default-value operator, or an explicit unwrap that panics; Rae has no default/unwrap operator yet, so PROPOSE the syntax in this task rather than inventing it during implementation (candidates to decide in review: `list.at(index: index) else 0` for a default, `list.at(index: index)!` for a panicking unwrap); the error message must name the fix. Acceptance: scalar and struct forms produce the same sema error with the same message; the silent-zero scalar path is gone; tests for both forms; the report's §8 bug entry closed. Deps: independent of #641. DECISION 2026-08-26 (approved): NO new language features (no `!`, no `else`, no `[]`, no trap/panic, no `.first()`/`.last()`, no enumerate loop). Rule: every optional ends in `if let`; the else branch returns a value the PROGRAMMER wrote, never a compiler-invented one. Investigation (2 classifiers + Array/Buffer probe): the ~299 grep lines are ~48–265 real sites collapsing to ~15–30 distinct changes; 34 are false positives (`Task.get()` joins + already-`opt` map gets — SKIP), dominated by the `ResolvedTheme` struct-of-arrays. Migration (batched by file, in order): (1) parallel-lists-by-key (ResolvedTheme, frames.rae, gltf channel SoA, legacy textures) → `StringMap` or a `List` of `{name,value}` structs iterated with `loop`+`view` where order matters — `themeSpaceValue` → one `if let` on `StringMap.get(key:)` + `ret fallback`; (2) fixed-size lists unpacked by constant index (`quadTile`, window_geometry parts, color parts, nowPlayingTimes) → a struct returned by value; (3) flat-buffer samplers (`mesh3d`, `gltf_skin`, `compress/*`, tetris grids, json cursor, gltf palette) → keep the data a `List`, one helper per buffer doing `copyAt` in `if let` with an explicit programmer fallback in else (e.g. `ret 0.0`), callers keep clamping; (4) loop-by-counter read-only → range `loop` with `view` (`mod` where the body mutates); (5) genuinely optional + the misc guarded-first/last/single-char reads → `if let` with an explicit else value. Fix `examples/106_mobile_ui` and `legacy/raylib/98_mobile_ui` twins identically. THEN the compiler flip: remove the accessor `opt T` return-stripping in method-call resolution AND the silent `UNBOX` in `ensure_type_match` (sema.c:2009-2021); make `opt T`→non-optional a uniform hard sema error for every T, message "optional not unwrapped — use `if let`"; update negative test 616's expected output; add tests for scalar accessor, struct accessor, and a user function returning `opt`; close report §8. Filed SEPARATELY (not #642): #654 Array `mod`/`view` element-binding copies instead of aliasing (writes silently no-op), #655 unchecked dynamic Array index (docs claim debug-checked).
- [x] #643 Rename `List.at` → `copyAt` and remove `get`. The accessor triad becomes `copyAt` / `viewAt` / `modAt` — the leading verb states copy-vs-alias intent at every call site. Scope: rename `at` → `copyAt` (lib/core.rae:102) and delete the `get` compatibility alias (lib/core.rae:95); drop the parallel `viewGet`/`modGet` aliases (lib/core.rae:113/121) in the same pass. Migrate ALL callers in lib/ and tests (`.at(index:)` → `.copyAt(index:)`, `.get(index:)` → `.copyAt(index:)`); update docs. NOTE: the #641 `emit_list_if_let` peephole and #644 both match the method-NAME string `"at"`/`"get"` (c_stmt.c:39, emit_optional_boxed_expr method list) — update those name checks to `"copyAt"` in the SAME change or the fast path silently stops firing. Acceptance: `at` and `get` no longer resolve; `copyAt`/`viewAt`/`modAt` are the triad; full suite green. Deps: do after #641/#642/#644 so accessor churn settles first. DONE 2026-08-27: `List.at`→`copyAt`; `get`/`viewGet`/`modGet` removed. Compiler/VM name-checks retargeted to `copyAt` (c_stmt.c peephole + boxing passthrough, sema.c `sema_is_list_value_accessor`, vm_emit_stmt.c list-get mapping, and the List-`[]` diagnostic message). ~130 caller files migrated (`.at(index:)`/`.get(index:)` → `.copyAt(index:)` for List; also the plain-call UFCS form `get(list, index:)` in `string.join` + tests 375/376/377/380/381/389). String.at (returns Char32) is a SEPARATE method and stays `at` — over-rename reverts in string/sdf_text/gpu2d_text/json/msdf + tests 371/597; `List2`/`List2Int`/`List2Generic` keep their own `get`. Docs updated (list-indexed-access.md, value-types, ownership-model, native-list-plan). Suite green: cases 321/0, examples 62/3 (3 pre-existing GPU). NOTE: a stray `list.at(...)` now misresolves via generic UFCS to String.at/List2.get and surfaces as a C-level type error rather than a clean "no method `at` on List" sema diagnostic — pre-existing UFCS looseness; filed as #657 for a cleaner message.
- [x] #644 `view`/`mod` binding on `copyAt()` is a sema error. Motivation: `copyAt` (formerly `at`, see #643) is declared to return `opt T` by value, but the `=>` peephole (emit_list_if_let, c_stmt.c, matches the copy accessor and `get`) makes `if let particle: view Particle => particles.copyAt(index: index)` alias live list storage instead of copying — the declared return type and the actual semantics disagree, and the accessor's name and return type must agree. Scope: binding `=> view T` or `=> mod T` to an expression of type `opt T` (i.e. to `copyAt`/`get`) is an error whose message is exactly: "`copyAt` returns a copy; use `viewAt` or `modAt` to alias storage"; remove the cross-form peephole (subsumed by #641 if that runs first). Acceptance: test that the form errors with that message; `viewAt`/`modAt` forms unchanged. Deps: cleaner after #641; coordinate the accessor rename with #643. DONE 2026-08-27: binding `=> view/mod T` to a `copyAt` List value-accessor (method form `list.copyAt(index:)` AND plain-call `copyAt(list, index:)`, in both `let` and `if let`) now errors with exactly "`copyAt` returns a copy; use `viewAt` or `modAt` to alias storage". Two sema sites: `ensure_type_match` emits it (instead of the #642 "optional not unwrapped") when the target is a reference (TYPE_REF) fed by a copyAt accessor — the plain-`let` form; and the `if let` binding validator (AST_STMT_IF) checks `(view||mod)` binding to a copyAt source, with a callee-name fallback since the plain-call decl_link isn't resolved there yet. The cross-form is now rejected at sema BEFORE the peephole, so `emit_list_if_let` only ever sees matching name/binding shapes (value+copyAt, view+viewAt, mod+modAt) — the aliasing cross-form is gone. `viewAt`/`modAt` bindings unchanged (not List value accessors). Tests: new 662_reject_alias_bind_copyat asserts the message; 616 updated (it is now the #644 case). Suite green: cases 322/0, examples 62/3 (3 pre-existing GPU).
- [x] #645 Aliasing rule for `view`/`mod` bindings into containers (INVESTIGATE FIRST — deliverable is a design note plus a follow-up implementation task, not code). Motivation: a `viewAt`/`modAt` binding is a raw pointer into `list->data`; any grow reallocates it, and nothing in the report says what stops a mutating call on the same list (`add`/`set`/`remove`) inside the body of a live `modAt`/`viewAt` binding — if nothing does, this is a dangling-pointer bug class that value copies do not have, and it must be settled before `view`/`mod` become the recommended idiom. Scope: (1) write a test that mutates the list inside a live `modAt` body and inspect generated C and runtime behavior; (2) if currently allowed, write a design note with options — borrow the container for the binding's scope so no `mod` use of the list is permitted inside the body (Rust-style, the safe default); or a runtime generation-counter check on the list; or document as undefined — and recommend one; (3) file the implementation as a follow-up task. Acceptance: note with recommendation exists; follow-up task filed. Deps: blocks the `view`/`mod` half of #650 for any site whose body mutates the container. DONE 2026-08-27: hazard CONFIRMED and currently ALLOWED — `if let p: mod Int => xs.modAt(index: 0) { xs.add(...) ; p = 99 }` compiles with no diagnostic; generated C captures `&data[0]` once, a later `add`→grow→realloc leaves it dangling, and `p = 99` is a silent use-after-free (write lost; `copyAt(0)` stays 5). `copyAt` value bindings are immune (detached copy). Existing machinery: the range-loop borrow check (`sema_expr_mutates_collection`/`sema_stmt_mutates_collection`, sema.c:1283/1688; diagnostic "cannot mutate a List while a collection loop borrows its elements") already rejects add/set/insert/remove/clear/drop/grow/pop in `loop element in list` bodies — it just isn't applied to `viewAt`/`modAt` `if let` binding bodies. Design note: docs/viewat-modat-aliasing.md, comparing (A) borrow-the-container Rust-style / (B) runtime generation counter / (C) document-undefined. RECOMMENDATION: (A) — extend the existing loop-body check to `viewAt`/`modAt` `if let` bindings; compile-time, zero runtime cost, reuses shipped machinery + diagnostic, needs no trap primitive (Rae has none per #642), conservative-lexical per the codebase's stated intent. Follow-up implementation filed as #658. No code changed.
- [x] #646 Make the existing `copy` mode semantically enforced for cascade-drop types (INVESTIGATE FIRST — note plus follow-up task, not code). Motivation: requiring `copy` on every binding would be noise on `Int`/`Float`/`Vec2` and would duplicate the `=` vs `=>` distinction; types where `type_needs_cascade_drop` is true own heap, so copying them is a deep copy with allocation and should be spelled out. NOTE (verified, corrects the report): a `copy` mode ALREADY EXISTS as a keyword (TOK_KW_COPY, lexer.c:54) and a valid parameter/binding mode (parser sets is_copy; sema accepts it alongside view/mod/own) — BUT it is Stage-A annotation-only (ast.h:38: codegen treats `copy T` identically to bare `T`); so this task is "make `copy` semantically enforced with real deep-copy codegen", NOT "add copy". Scope: (1) determine what happens today when a struct that owns a `List` is bound by value (`let copyOfOwner: Owner = owner`): move, deep copy, or shallow copy with a later double free; (2) design: require `copy` in the binding for cascade-drop types, implicit copy for everything else, and give `copy` real deep-copy codegen; (3) file implementation as a follow-up. Acceptance: note plus follow-up task; the follow-up's tests cover — owning struct without `copy` errors; with `copy` deep-copies; no double free; `Particle` still binds without `copy`. Deps: relates to #641 (drop generation). DONE 2026-08-27 — investigation (emitted C): (1) binding a cascade-drop struct by value (`let x: Owner = owner`) is ALREADY an implicit DEEP copy (`rae_deep_copy_<T>`), unconditional (even on a dead/last-use source — no move optimization); it is SAFE — independent heaps, both dropped, no double-free/leak (RAE_MEM_STATS outstanding=0). NOT a shallow copy. (2) `copy T` and `own T` LOCAL bindings emit the SAME deep copy as bare `T` — `is_copy`/`is_own` are annotation-only for locals (ast.h:38), affecting only parameter-passing codegen. (3) Non-cascade-drop types (Point/Vec2/Particle-no-heap) are a plain `rae_T b = a;` struct copy — no deep copy, no allocation. CORRECTION to premise: the deep-copy codegen already exists AND bare `T` already uses it; there is no missing codegen and no shallow-copy double-free — the gap is a SEMANTIC REQUIREMENT (nothing forces `copy`, so the heap allocation is hidden). Design note docs/copy-mode-cascade-drop.md: RECOMMEND requiring `copy` on cascade-drop value bindings (bare → sema error; write `copy T` to deep-copy or `view`/`mod` to alias — mirrors #643 copyAt/viewAt/modAt and #644 `=`/`=>`), non-cascade-drop keep implicit copy. HONEST CAVEAT recorded: with no move-of-locals, `copy` deep-copies exactly as the implicit bind does today, so the payoff is explicitness/auditability, NOT perf or correctness; the higher-value alternative is move-on-last-use for the bare case (separate, larger). Follow-up + decision point filed as #659.
- [x] #647 Add an `alias` type alias (LOW PRIORITY). Context (verified): `alias` is not a keyword today (lexer.c keyword table) and `sema_resolve_type_internal` (sema.c:258) has no type-alias resolution — "alias" in sema currently means only import aliases. Scope: `alias Size = Vec2` resolving to the target's `TypeInfo` in `sema_resolve_type_internal` before the nominal decl lookup, as report §7 describes; document it as a naming convenience, not type safety (`func area(size: Size) ret Float` is callable on any `Vec2` via dot syntax); decide whether error messages print the alias name or the canonical name (prefer the name the user wrote where cheap). Acceptance: `type Rect { position: Vec2  size: Size }` compiles to 16 bytes and `vec2.*` works on `size`; test covering the error-message decision. Deps: none. DONE 2026-08-27: `alias Name = Type` implemented. CONTEXTUAL keyword (NOT reserved — `alias` is still a valid identifier, e.g. `let alias: String` in gltf_retarget.rae; recognized only as `alias <Ident> = <type>` at top-level decl position). New AST_DECL_ALIAS (ast.h) + parse_alias_declaration (parser.c). Resolution in sema_resolve_type_internal: a bare alias name is REWRITTEN in place to its target's `parts` before name-based lookup (a loop resolves alias-of-alias chains, guarded), so not only sema but also the mangler and C-type emission (which read `parts->text`, not `resolved_type`) see the canonical type — no `rae_Size` leaks into the C. find_alias_target scans the module + its imports (all_decls isn't populated until the backend). AST_DECL_ALIAS handled in sema_analyze_decl (validates target) and pretty.c (round-trips; the `else` fallthrough would otherwise misread it as a func decl). ERROR-MESSAGE DECISION: prints the CANONICAL name (cheap — the alias rewrites away at the use site), verified `let v: Vec3 = someSize` → "cannot convert Vec2 to Vec3". Verified: `struct rae_Rect { rae_Vec2 position; rae_Vec2 size; }` (16 bytes inline, 0 rae_Size), `r.size.x` / `area(size: view Size)` / `vec2.*` all work. Tests: 663_type_alias (positive), 664_type_alias_error_canonical (error decision). Doc: docs/type-aliases.md (naming convenience, NOT type safety). Suite green (432_mobile_ui_leak_regression is a pre-existing flaky leak test — passes in isolation; #647 is compile-time only).
- [x] #660 Do not make stupid decicions like allowing alias to be a variable name. Of course it has to be a real keyword. I saw you wrote: "**Important snag:** `alias` IS used as an identifier today — `lib/gltf_retarget.rae` has `let alias: String = ...` (4 sites). So a hard keyword would break shipped code and reserve a common word forever. A **contextual keyword** (recognize `alias Ident = Type` only at top-level decl position) avoids that." That is absolutely stupid. No such contextual keywords are good. Don't allow alias to be a keyword. DONE 2026-08-27 — `alias` is now a REAL reserved keyword (reversing #647's contextual-keyword hack). Added TOK_KW_ALIAS to lexer.h (inside the TOK_KW_TYPE..TOK_KW_AS keyword range that `is_keyword` checks) + the lexer.c KEYWORDS table + TOKEN_KIND_NAMES (designated initializers, so the enum insertion doesn't misalign the name array). parse_declaration now dispatches on `parser_match(TOK_KW_ALIAS)` (removed the IDENT-"alias" + lookahead detection); parse_alias_declaration reads the keyword via parser_previous. Renamed the 7 identifier uses of `alias`: lib/gltf_retarget.rae (4 → `aliasKey`) and test 551_as_conversion (3 → `aliasF32`; the string label `alias=` and expected.txt are unchanged). Verified: `let alias: Int = 5` now errors "'alias' is a reserved keyword"; the alias-decl tests 663/664 still pass; gltf_retarget compiles. Doc docs/type-aliases.md corrected ("reserved keyword", not contextual). Suite green: cases 324/0, examples 62/3 (3 pre-existing GPU).
- [x] #648 Decision record: no `using`/field promotion for now (NOT a code task). Scope: add an entry to the design-decisions doc (docs/, wherever such notes live) recording that field promotion is deferred; if ever added it must ship with per-field rename in the same change (`using size: Vec2 as { width: x, height: y }`), because `alias` guarantees two promoted `Vec2` fields collide on `x`/`y`; dot-syntax functions already give read-only promotion (`func width(rect: Rect) ret Float { ret rect.size.x }`). Acceptance: the decision record exists. Deps: relates to #647. DONE 2026-08-27 — decision record written at docs/field-promotion-deferred.md: field promotion deferred; if ever added, per-field rename is mandatory in the SAME change (`using size: Vec2 as { width: x, height: y }`) because aliases (#647/#660) make same-type field collisions unavoidable (`Rect { position: Vec2  size: Size }` → bare promotion makes `rect.x` ambiguous); read-only promotion is already available via dot-syntax accessor functions (`func width(rect: view Rect) ret Float { ret rect.size.x }` → `rect.width()`). Doc-only, no code.
- [ ] #649 `Array[i]` vs `List` indexing consistency (INVESTIGATE FIRST — note plus follow-up if `List` gets `[]`). Context (verified): `arr[i]` on `Array(T, cap: N)` is accepted and typed by sema (AST_EXPR_INDEX, sema.c:2624-2625, with compile-time bounds checking for constant indices) while `list[i]` on `List(T)` is explicitly rejected with a diagnostic (sema.c:2604-2621, "a List is not indexed with []"); the asymmetry is the issue, not the missing sugar. Scope: pin down what `arr[i]` means today (value read, in-place lvalue, or both by context) and what it does out of bounds; then decide — either `List` gets `[]` with identical semantics (bounds-checked, panics), or document why the two containers differ. Acceptance: note with decision exists; follow-up task filed if `List` gets `[]`. Deps: none.
- [x] #650 CLOSED 2026-08-26 — now tracked in the downstream game project's own repo; this carry-over placeholder is no longer needed here. (Was: carry over the QUEUE #112 abbreviated-binding + access-mode cleanup.) Scope: keep the naming half — spell out one-letter bindings like `m`, `ar`, `b` into full names (`mesh`, `array`, ...); narrow the access-mode half — convert sites that mutate an element to `modAt`, convert read-only access to large elements or elements with owned heap fields to `viewAt`, leave small plain-data read-only copies (`Particle`, 48 bytes) as value bindings (expect NO gain from converting those: at -O2 the C compiler scalarises the copy and loads only the fields used; verify on one site before converting more); any site whose body mutates the list while a `mod`/`view` binding is live waits on #645; migrate any `get` callers to `at` (#643). This entry only carries the amended text over — do not implement here. Deps: #645 (mutate-while-live sites), #643 (remove get).
- [ ] #651 Complete the `opt T` uniform representation: move scalar/String optionals off the `RaeAny` union too (finish "reserve RaeAny for Any only" from #641). Motivation: #641 migrated struct/aggregate `opt T` to `struct rae_opt_<T>{has,value}` but left `opt Int`/`opt Float`/`opt Bool`/`opt Char`/`opt String` on the inline `RaeAny` union (already malloc-free, so out of #641's acceptance); to fully reserve `RaeAny` for `Any` only, migrate scalars/String to `rae_opt_<T>` too. Scope: extend the struct-rep predicate (`rae_opt_is_struct_rep`, c_backend.c) to all non-ref non-Any payloads; update the paths that currently stay on RaeAny — the `.as.i/.as.f/.as.b/.as.s` unbox (c_call.c `emit_opt_unbox_suffix`, c_expr.c UNBOX), `rae_any_is_none` none-checks (c_expr.c binary, c_stmt.c if/match), and especially log/interpolation/JSON/toString, which today rely on RaeAny being self-describing (a `rae_opt_<T>` is not, so these need per-type formatting). Acceptance: no value-`opt` spells `RaeAny` in emitted C (only `Any` does); scalar/String opt round-trip incl. log/interp/json covered by tests. Deps: builds on #641.
- [ ] #652 Fix pre-existing double-free: generic `get`/`at` (non-peephole) on `opt <struct-with-an-owned-heap-field>`. Motivation: surfaced during #641 — calling the generic `get`/`at` as a REAL function (not the `emit_list_if_let` peephole) to extract an `opt <struct that owns heap, e.g. a String field>` and then dropping it double-frees, because those accessors build the optional by MOVING a `buf_get` alias (shallow) instead of deep-copying, while the drop then frees a payload the list still owns; the peephole path deep-copies and is safe. Predates #641 (same shallow-move logic as the old malloc branch). Scope: make `get`/`at` deep-copy heap-owning payloads (or treat `buf_get` as non-movable for owning payloads). Acceptance: a program that stores `let x: opt <StructWithString> = list.at(index:)` via the general (non-peephole) path, drops it and the list, runs clean under `RAE_MEM_STATS` (no double-free, no leak); test added. Deps: relates to #641/#643.
- [ ] #653 Verify `view`/`mod` on primitives (`Int`, `Float`, `Bool`) are legal types that lower to a plain pointer with NO boxing — prerequisite for the downstream access-mode sweep (formerly #112, now in the game repo). Motivation: that sweep converts read-only element access to `viewAt` and in-place mutation to `modAt`; for a `List(Int)`/`List(Float)`/`List(Bool)` this means bindings like `if let health: view Int => healths.viewAt(index: index)` and `if let health: mod Int => healths.modAt(index: index)`. This must be a real reference into `list->data` (a plain `T*` — the existing `rae_View_Int64`/`rae_Mod_Int64`/`rae_View_Float`/`rae_Mod_Float`/`rae_View_Bool`/`rae_Mod_Bool` wrappers, c_backend.c emit_type_ref_as_c_type), NOT a boxed/copied value. Scope: (1) test that `view`/`mod` on each of Int/Float/Bool is accepted by sema and that `viewAt`/`modAt` on a `List(<primitive>)` binds a pointer to the element — a `modAt` write must persist and be visible to a later read; (2) if any primitive view/mod is rejected today or copies instead of aliasing, make it legal and pointer-lowered. Acceptance: a test per primitive (Int, Float, Bool) for BOTH `view` and `mod`, with the generated C inspected to confirm a `rae_View_*`/`rae_Mod_*` pointer wrapper and no boxing/copy; a `modAt` mutation round-trips. Deps: none; blocks the downstream access-mode sweep.
- [ ] #654 Fix `Array(T, cap: N)` `mod`/`view` element-binding: it copies instead of aliasing. Surfaced during #642 Array/Buffer investigation. `let refv: mod Cell => grid[i]` currently emits `__auto_type __rae_bind0 = grid.v[i];` then takes `&__rae_bind0` (a COPY), so writes through the bound `mod` reference silently no-op and never reach the array storage (unlike `List.modAt`, which aliases real storage). Direct `grid[i].field = value` DOES work (it's only the alias-binding path). Scope: make `view`/`mod` binding to an Array element alias `&grid.v[i]` directly (like List modAt), in c_expr.c/c_stmt.c index-binding lowering. Acceptance: a test where a `mod` binding to `arr[i]` writes a field and a subsequent read sees it; generated C takes the element address, not a copy. Deps: none.
- [ ] #655 Implement (or correct the docs for) dynamic `Array(T, cap: N)` bounds checking. Surfaced during #642 investigation. `docs/value-aggregates-and-ownership.md:214-228` specifies dynamic Array indexing is "dynamic-checked in debug", but codegen (c_expr.c:627-657, AST_EXPR_INDEX) emits raw `grid.v[i]` with NO bounds check in ANY profile — constant indices are compile-time checked (sema.c:2624-2645) but a runtime index is fully unchecked. Scope: either implement the documented debug-gated dynamic bounds check (emit `if (i<0||i>=N) <trap>` around dynamic Array reads/writes — note: needs a trap mechanism, currently absent) OR correct the doc to state dynamic Array indexing is unchecked. Decide which. Acceptance: docs and codegen agree; if implemented, a test with an out-of-range dynamic index traps in a debug build. Deps: none.
- [ ] #656 Investigate: `if let x: view String => <opt String field>` presence-check disagrees with `is not none` after the field is REASSIGNED to none. Surfaced during #642 (test 531_opt_string_ownership `describe`): a plain `if let noteText: view String => card.note` ENTERS its branch even after `card.note = none` (yielding e.g. `alpha/` instead of `alpha`), while `card.note is not none` detects the none correctly; init-to-none works fine (test 607). Worked around in 531 by nesting the `view =>` under an `is not none` guard. Looks like a real none-detection inconsistency for a REASSIGNED `opt String` (RaeAny) field via `view =>` narrowing. Scope: reproduce minimally, find why `view =>` narrowing's presence test misses a reassigned-to-none RaeAny opt-String field, fix so it agrees with `is none`. Deps: none.
- [ ] #657 Clean "no method on receiver" diagnostic for generic UFCS misresolution. Surfaced during #643: calling a method that doesn't exist on the receiver (e.g. `someList.at(index: 0)` after `at` was removed from `List`) does NOT produce a clean sema error — the generic UFCS matcher instead binds it to a same-named function on a DIFFERENT type (e.g. `String.at`, or the generic `List.copyAt`/`List2.get`), emitting confusing C-level type errors (`initializing 'RaeAny' with int64`, `rae_core_copyAt_rae_List_rae_T_...` undeclared) at the generated-C line instead of the .rae call site. Scope: when UFCS/method resolution finds a candidate whose receiver type doesn't actually match, reject it and emit a sema error naming the receiver type and method ("no method `at` on `List(Int)`; did you mean `copyAt`?"). Acceptance: a `list.at(index:)` (or any wrong-receiver method) errors at the .rae site with a clear message, not a downstream C error. Deps: none.
- [ ] #658 Implement the `viewAt`/`modAt` aliasing borrow check (from #645; design note: docs/viewat-modat-aliasing.md). Extend the existing collection-loop borrow check (`sema_expr_mutates_collection`/`sema_stmt_mutates_collection`, sema.c:1283/1688, which rejects add/set/insert/remove/clear/drop/grow/pop with "cannot mutate a List while a collection loop borrows its elements") to the then-branch of an `if let x: view/mod T => list.viewAt/modAt(index:)` binding: while the binding is live, reject the eight structural mutators on the ALIASED List (identify it from the `viewAt`/`modAt` receiver), reusing the diagnostic reworded for the binding form. Rationale: today the binding captures `&list->data[i]` once and a later `add`→grow→realloc leaves it dangling — a silent use-after-free (repro + generated C in the note). Compile-time, zero runtime cost, no new primitive. Acceptance: mutating the aliased List inside a live `modAt`/`viewAt` `if let` body is a sema error; mutating a DIFFERENT List and `copyAt`+`if let` still compile; test added; suite green. Deps: none; blocks the `view`/`mod` half of #650 for any site whose body mutates the container.
- [ ] #659 Require `copy` for cascade-drop value bindings (from #646; design note: docs/copy-mode-cascade-drop.md). DECISION POINT: adopt the requirement, OR keep status quo and pursue move-on-last-use instead — see the note's "honest caveat" (the benefit is explicitness/auditability, NOT perf or correctness; today's implicit bind already deep-copies safely). If adopting: make a bare value binding `let x: T = y` a sema error when `type_needs_cascade_drop(T)` is true, with message naming the fix (write `copy T` to deep-copy, or `view`/`mod` to alias); `copy T` binding deep-copies (the existing `rae_deep_copy_<T>` codegen — no new codegen needed); non-cascade-drop types (Int/Vec2/Particle) keep the implicit plain copy. Migrate existing `let x: HeapOwningStruct = y` callers to `copy` (or a `view`/`mod` alias). Acceptance: owning struct bound by value WITHOUT `copy` errors; WITH `copy` deep-copies with no double free (RAE_MEM_STATS outstanding=0); `Particle`/`Point`/`Vec2` still bind without `copy`; suite green. Note: `is_copy` is currently annotation-only for LOCAL bindings (only parameter passing honors it), so the change is primarily the sema gate + routing the local `copy` binding through the existing deep-copy path. Deps: relates to #641 (drop/deep-copy helper generation).
<!-- SUMU_QUEUE_END -->
