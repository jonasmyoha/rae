<!-- SUMU_QUEUE_START -->
- [x] #769 [ecs] Flip hierarchy authority: `Children` (ordered) is AUTHORITATIVE and serialized; `Parent` is the DERIVED reverse index, rebuilt by HierarchySystem. Reverses #711. Why: sibling ORDER is real information (layout + paint order) that lives only in Children — component tables are swap-remove, so rebuildFromParent scrambles sibling order after any destroy; a Parent-serialized snapshot would reload with shuffled siblings. Children arrays are also how humans and .raescene author trees. API: setParent stays the single mutation entry (now edits the parent's Children list + rebuilds the child's Parent); add insertChild(parent, child, index) for explicit ordering; a `rebuildParentsFromChildren` self-heal; enforce the single-parent invariant (a child may appear in exactly one Children list — reparent removes it from the old list first). depthOrder dirty-tracks on the Children table generation instead of Parent's. Update registry flags (#730): Children `serialize`, Parent 0. Files: lib/ecs/hierarchy.rae, lib/ecs/HierarchySystem/HierarchySystem.rae, lib/ui/registry_flags.rae, callers (scene_loader/scene_instance/frames unchanged API). Deps: #726. Verify: REGRESSION TEST — build a 5-sibling tree, destroy sibling 2, rebuild Parent from Children, sibling order must survive; 687/688/692/704 pass; 106 screenshot byte-identical. DoD: Children is the source of truth for the hierarchy including order; Parent is a cache.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.] DONE: Children authoritative + serialized, Parent derived (flags flipped); added insertChild + rebuildParentsFromChildren (heals duplicate listings, keeps first parent); depthOrder tracks the Children generation; rebuildFromParent removed. New case 713 (5 siblings, destroy #2, rebuild -> order 1 3 4 5; insert/reparent/heal/depthOrder); 687/706 expectations updated. Suite 373/373 + examples 66/66 green; 106 headless screenshot byte-identical vs. pre-change build. LANG-CHECK: nothing new.
- [x] #772 [compiler] Compile-time field reflection v1, per docs/compile-time-reflection.md: (1) LEXER: reserve lowercase `any` as a keyword (compile-time type wildcard, legal only inside a type pattern; the existing `Any` runtime box is UNTOUCHED — `ComponentTable(Any)` stays the concrete dynamic-box table type, which is exactly why the wildcard needs its own spelling); (2) SEMA: recognise `fields(value)` in the iterable position of the EXISTING `loop let x: <mode> T in EXPR` form (parser unchanged) as a field loop over a struct value; the binding's explicit type is the filter (`ComponentTable(any)` matches every ComponentTable field regardless of element type, a concrete type matches only that type); type-check the body ONCE PER MATCHING FIELD with the binding aliased to `value.<field>` at that field's concrete type; `let`+`view`/`mod` only (`var` = copy of a heap struct, rejected by the existing alias rule); a non-`fields(...)` struct iterable and `any` outside a type pattern are errors with clear diagnostics; (3) `fieldName(binding)` = compiler-recognised function folded to the slot-name String literal, also reachable via UFCS `binding.fieldName()`; (4) EXPANSION: unroll the loop into N copies of the body BEFORE codegen so BOTH backends (C + VM) get it for free — no per-backend special case; zero matching fields = empty. Scope OUT (v2, #774): `fields(Type)` / construction, bare `view any` + `match`, nested recursion. Tests: compiler/tests/cases: filter by concrete type, filter by `ComponentTable(any)`, `mod` write-through into the real field, `fieldName` literal, zero-match loop, `any` misuse diagnostic, `var` rejected. Verify: full suite via watch-tests.sh + examples green. DoD: a non-generic `func clearAll(world: mod UiWorld, entity: view EntityId) { loop let table: mod ComponentTable(any) in fields(world) { componentRemove(this: table, entity: entity) } }` compiles on both backends to the same calls the hand-written destroyEntity makes today.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]  DONE: lexer reserves `any`; parser accepts it in type position; sema unrolls `loop let x: view/mod T in fields(value)` into `if true { let x => value.f; body }` per matching field BEFORE the switch, so both backends see plain AST; binding type is the filter (`ComponentTable(any)` vs concrete); `fieldName(x)` / `x.fieldName()` fold to the slot-name literal; clear diagnostics for `any` as a real type and for `var`. Tests 714_field_loop_clear/715_field_loop_fieldname/716_field_loop_any_misuse/717_field_loop_var_rejected. DoD verified: `clearAll(world: mod UiWorld, ...)` compiles + unrolls. Suite 377/377 + examples 66/66 green. v2 (`fields(Type)`, bare `view any`+match, recursion) is #774. LANG-CHECK: nothing new.
- [x] #773 [compiler] Field reflection through a GENERIC world parameter: `func clearEntityComponents(W: type, world: mod W, entity: view EntityId) { loop let table: mod ComponentTable(any) in fields(world) { ... } }` must instantiate per concrete W (UiWorld, World3d, GameWorld) with the field loop expanded AFTER generic substitution (the loop's field set depends on the concrete W, so expansion must happen per instantiation, not once on the generic body). Also infer W from the argument so callers write `clearEntityComponents(world: uiWorld, entity: e)` with no explicit W. This is split from #772 on purpose: it leans on the generic-substitution machinery that needed #758 and still cannot do two-type-param generics (memory: decompose into single-type-param helpers) — expect it to surface generic bugs; fix them here, not by special-casing the loop. Tests: one generic func used against two different structs in one program; `fieldName` inside a generic body; `view` and `mod` variants. Deps: #772. Verify: full suite + examples. DoD: the SAME generic function clears entities in UiWorld, World3d and the 116 GameWorld.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]  DONE: generic field loops expand per instantiation. Shared unroll core (reflect_fill_unrolled) drives both the non-generic #772 path and a new reflect_instantiate_body that clones+expands a specialization's body using the concrete world resolved from params + generic substitution; invoked from BOTH the C-backend emit path (emit_specialized_function) AND the discovery pass (so the concrete inner componentRemove/etc. specializations get registered + prototyped). Also fixed a real generic bug: bare generic-param inference (`world: W`) — infer_generic_args/_multi never bound a param that IS the whole pattern, only nested ones — so W is now inferred from the argument (no explicit W:). Test 718_field_loop_generic (mod+view+fieldName, two worlds, one func). DoD verified: same clearEntityComponents compiles + unrolls against real UiWorld (76 tables), World3d (5), 116 GameWorld (4), skipping non-ComponentTable fields. Suite 378/378 + examples 66/66 green. LANG-CHECK: nothing new (fixes were compiler bugs, not missing language features).
- [x] #760 [ecs] FIRST CONSUMER of field reflection (#772/#773): make whole-world ops zero-edit. (1) lib/ecs: generic `clearEntityComponents(W: type, world: mod W, entity: view EntityId)` as the `loop let table: mod ComponentTable(any) in fields(world)` from docs/compile-time-reflection.md; (2) lib/ui/ecs.rae `destroyEntity` calls it (drop the hand-listed per-table `componentRemove`s; hierarchy detach + `freeEntity` stay explicit); (3) examples/116_ecs_headless_sim `worldToJson`/`worldFromJson` become the `loop let table: view ComponentTable(any) in fields(world) { addComponentTable(..., name: fieldName(table), registry: reg) }` form — registry flags STILL gate which tables serialize, the loop only removes the hand-listing; (4) 114 gameworld.rae / lib/scene3d.rae: use the generic clear wherever an entity is despawned (net/physics seams from #746). NOT a compiler builtin, NOT an @-attribute (history: the original #760 plan was a one-use sema/c_backend builtin — rejected, see AGENTS.md). Deps: #772, #773. Verify: full suite + examples + RAE_MEM_STATS outstanding=0 on the ECS leak-gated cases; 116 snapshot round-trip identical before/after. DoD: add a new `ComponentTable(T)` field to UiWorld or GameWorld and NO edit is needed in destroyEntity / worldToJson / worldFromJson (prove it with a test that adds a table and round-trips it).  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]  DONE: (1) lib/ecs/world.rae generic clearEntityComponents(W, world: mod W, entity) = `loop let table: mod ComponentTable(any) in fields(world) { componentRemove }`; (2) UiWorld destroyEntity now calls it (dropped ~75 hand-listed removes; freeEntity stays explicit); (3) 116 worldToJson/worldFromJson are the registry-gated `loop ... in fields(world) { addComponentTable/loadComponentTable(name: fieldName(table)) }` form — registry re-keyed by FIELD name so fieldName() matches, Enemy still gated out; Clock resource stays explicit; (4) 114 netLeave + despawnPhysicsBody use the generic clear. Not a builtin, not an @-attribute. DoD test 719_field_loop_serialize_roundtrip: snapshot/restore never name a table, a new table auto-serializes, registry gates a runtime-only table, round-trip identical. Suite 379/379 (incl mem-stats leak-gated ECS cases -> outstanding=0) + examples 66/66 green; 116 round-trips. NOTE: 116 snapshot keys are now field names (positions vs Position) — an inherent consequence of fieldName() keying; round-trip is stable. LANG-CHECK: added `typeName(T)` element-type-name reflection to docs/ecs-language-wishlist.md (would let the loop key by component type name and avoid re-keying the registry).
- [x] #768 [ui] UI MACHINE snapshot: worldToJson/worldFromJson for UiWorld, registry-driven (one addComponentTable/loadComponentTable per table gated by createUiComponentRegistry() from #730 — derived caches skipped by flag); write it as the `loop let table: view ComponentTable(any) in fields(world)` form from #760/#772, NOT a hand-list, so a new table needs no snapshot edit). This is SEPARATE from the human-authored .raescene loader (applyComponentByName stays; it parses a hand-friendly grammar with enum strings + theme tokens, not struct JSON). Files: lib/ui/serialize.rae (new). Deps: #767 (enum round-trip), #730. Verify: load a .raescene, snapshot, reload into a fresh world, snapshot again -> byte-identical JSON; layout+render parity. DoD: a UiWorld round-trips through the registry with no per-component switch.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]  DONE: new lib/ui/serialize.rae worldToJson/worldFromJson for UiWorld = the reflection `loop let table: view/mod ComponentTable(any) in fields(world)` form (no per-component switch); createUiComponentRegistry re-keyed by FIELD name so fieldName(table) matches the gate (bijection with the 76 tables; 706 updated). Separate from the .raescene loader (applyComponentByName untouched). Required extending the compiler's auto toJson/fromJson, which emitted a literal `...` for any non-scalar field: now recurses into NESTED structs (Vec2/Color/Insets) and serializes LIST fields (Children.ids: List(EntityId)) as JSON arrays; forward-declares all toJson/fromJson; struct lookup scans merged all_decls; new general runtime primitives rae_json_extract_object/_array + rae_json_array_count/_item. DoD test 720_ui_machine_snapshot: load .raescene -> snapshot -> reload fresh world -> snapshot again = BYTE-IDENTICAL, nested Vec2 Rect (600x1300) survives. Suite 380/380 (incl 695/696/710/712 json + leak-gated ECS) + examples 66/66 green (render parity covered by byte-identical data + green UI example screenshots). LANG-CHECK: nothing new — the field-name-vs-type-name friction is the already-filed typeName(T) wishlist bullet (#760).
- [x] #749 [cleanup] Documentation: flip docs/ecs-general-architecture.md status to implemented; add a lib/ecs API reference (entities/handles, ComponentTable, query2/3, HierarchySystem, TransformSystem, registry+serialize); cross-link from ui-render-loop-performance.md and value-aggregates docs. Files: docs/*. Deps: #760, #768, #769 (after all ECS implementation tasks). Include the new API surface: Schedule, resources, tags, EventQueue, and the whole-world helpers as shipped by #760 (generic `clearEntityComponents` + registry-gated serialize written as `loop let table: ... ComponentTable(any) in fields(world)`), linking docs/compile-time-reflection.md for the language feature; note there is NO compiler-synthesised world-helper builtin. Verify: docs consistent with code. DoD: docs describe the shipped ECS.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]  DONE: new docs/ecs-api-reference.md (entities/handles, ComponentTable, query2/3 + tags, HierarchySystem, TransformSystem, registry+serialize, resources, EventQueue/Queue, Schedule, and the #760 whole-world helpers via `loop ... in fields(world)` — every signature verified against lib/ecs, no phantom funcs). ecs-general-architecture.md status flipped to IMPLEMENTED with a 'what shipped' block linking compile-time-reflection.md and noting NO compiler-synthesised world-helper builtin; section 1 relabeled as the pre-migration baseline (its citations are historical). Cross-links added from ui-render-loop-performance.md and value-aggregates-and-ownership.md. ecs-world-helper-synthesis.md status -> RESOLVED (#760/#772/#773). Verified docs vs code (func/const diff empty; flag values match; #771 correctly marked in-progress). LANG-CHECK: nothing new (docs task; the fieldName-vs-typeName friction is the already-filed typeName(T) bullet).
- [x] #775 [test] De-flake `432_mobile_ui_leak_regression`: it passes 3/3 standalone but fails intermittently under the full suite (seen during #747/#758). Find the cause (shared build cache, timing on RAE_SDL_HEADLESS_MS, mem-stats site accounting, or a real leak that only shows under load) rather than retrying or loosening the assertion. Files: compiler/tests/cases/432_*, tools/run_tests.sh. Deps: none. Verify: 5 consecutive full-suite runs via watch-tests.sh with 432 green. DoD: cause identified and fixed; test deterministic.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]  DONE: ROOT CAUSE = the mem-stats gate, not a real leak. 431/432/433 are RSS-based leak tests (measure processRssKb, never call the mem_stats native) but were in the `43[1-9]_*` RAE_MEM_STATS=1 group; the side-hash is per-allocation bookkeeping that itself grows RSS, so 432's reading went from ~0 KB (clean) to up to ~2.9 MB against its 3 MB bound — intermittently over. Fix: tools/run_tests.sh gate `43[1-9]_*` -> `43[4-9]_*` so the RSS tests run WITHOUT RAE_MEM_STATS (deterministic 0-96 KB growth); native-based leak tests (434-439/449/54x/666/712) still gated. Added a harness note to 432 main.rae. Verify: 5 consecutive full-suite runs via watch-tests.sh — 432 PASS every run, 380 cases + 66 examples green, 0 fails. LANG-CHECK: nothing new (test-harness issue, not a language gap).
- [x] #750 [verify] Final integration: full `make test` green (cases + examples), screenshot parity for 105/106/109/110/112/114 (#759 showed 109/110/114 render fine headless; they are real gates now), and RAE_MEM_STATS outstanding=0 for the leak-gated ECS tests. Files: whole tree. Deps: ALL of #700-#749 and #751-#761 + #767/#768/#769 + #772/#773/#760 + #775 (every ECS-surfaced task now sequenced before it). Verify: green suite + parity + no leaks. DoD: the ECS migration is complete, verified, and green.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]  DONE: final integration verified. Full suite via watch-tests.sh = 380 cases + 66 examples, 0 failures. Screenshot parity for all six: 109/110/112/114 pass their run_examples content/screenshot gates; 105 (986 colors) and 106 (308 colors) verified non-blank headless (105's headless render is cwd/demo-mode sensitive, so no fragile permanent gate added — verified, not gated). RAE_MEM_STATS outstanding=0 confirmed for the leak-gated ECS tests (434-439/449/542/543/545/666/712 all PASS; 712 atexit dump shows every site alloc==free outstanding=0). Sign-off recorded in docs/ecs-general-architecture.md. LANG-CHECK: nothing new.
- [ ] #764 [lib] Migrate the lib-level mutable globals into App-owned resources per docs/globals-and-app-ownership.md: lib/ui/theme.rae `activeTheme`+`g_themeGeneration` -> a `Theme` resource on App (also closes the two-globals bug at app3d/ui_shell.rae:74); lib/gpu2d.rae `gG2dColorAtts` -> `Gpu2dContext` handle; lib/gbuffer_passes.rae `gTaa*`/`gComposite*` -> renderer resource; lib/profile.rae `gProf*` -> `Profiler` resource. C runtime state stays global behind `extern` (no `unsafe` keyword). Deps: #750. Note: lib/gbuffer_passes.rae is also touched by the later renderer epic #771, but its globals are per-pass pipeline/bind caches independent of the scene source, so migrating them here does not conflict. Verify: suite green; 106/111/112/114 screenshot parity. DoD: zero module-level `var` in lib/.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #766 [compiler] No-globals diagnostic, ERROR phase: flip #763's warning to a hard compiler error. Deps: #764, #765. Verify: full suite + examples green with the error enabled. DoD: a module-level `var` or heap `let` fails to compile; the rule in docs/globals-and-app-ownership.md is enforced.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #771 [renderer/ecs epic] Migrate the deferred renderer off the hand-rolled Scene3d SoA onto lib/ecs World3d, for a SINGLE 3D ECS world end-to-end (the REAL version of the old #748, which was mis-scoped as a 2-file cleanup). Scope: (1) give renderScene/renderSceneShadow/renderDeferredPass a World3d path (query MeshRenderer/Transform3d/Material3d); (2) migrate the direct Scene3d builders — 96_easing_3d, 97_tetris3d, 108/109/110/113/115 — to World3d; (3) parse .raescene straight into World3d (drop the Scene3d intermediate from loadWorld3dFile; move 111 off Scene3dAsset); (4) then delete Scene3d + Scene3dAsset + buildWorld3dFromScene3d. Deps: #716/#732 (World3d exists). Verify: suite green + screenshot parity for every migrated example. DoD: no Scene3d/Scene3dAsset in the tree; the renderer takes World3d.
- [ ] #770 [naming] Symmetric ECS naming: World2d (rename UiWorld) / World3d (exists) for ECS CONTAINERS; Scene2d (rename lib/ui/scene.rae Scene) / Scene3d for authored .raescene CONTENT UNITS. CORRECTION (was based on a false #748 assumption): the Scene3d name is NOT freed — Scene3d stays as the live shared RENDERER type until the renderer->ECS epic #771 retires it, so the Scene3dAsset->Scene3d half is BLOCKED on #771. Weigh value first: UiWorld->World2d is 100+ refs of pure churn that DROPS the UI-domain hint, and the type-collision problem it targets is better solved by per-module type namespacing (docs/ecs-language-wishlist.md "Namespacing & modules") than by a mass rename. Recommend: do the low-churn lib/ui Scene->Scene2d if wanted, gate UiWorld->World2d on a human call, defer the Scene3d half to #771. Deps: #771 (frees the Scene3d name). Verify: build + case suite green. DoD: naming decision recorded and whatever renames are chosen land cleanly.
- [ ] #774 [compiler, deferred] Field reflection v2, per docs/compile-time-reflection.md open questions: (1) `fields(Type)` — iterate a TYPE's fields with no value, plus the small design for how the loop body assembles a struct literal, so `createUiWorld`/`createWorld3d`/`createGameWorld` become one generic `createWorld(W: type)`; (2) bare `loop let field: view any in fields(x)` + `match field { case ComponentTable(any) {...} case EntityAllocator {...} }` for multi-kind dispatch; (3) decide one-level vs recursive `fields()` (design says one level, recurse explicitly). Only start if #760/#768/#771 show a real need — construction is one line per table and the cheapest of the three whole-world ops. Deps: #773. Verify: suite green. DoD: the create half of the world boilerplate is gone, or the task is closed as not-needed with the reason noted in the doc.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
<!-- SUMU_QUEUE_END -->
