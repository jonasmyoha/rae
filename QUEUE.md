<!-- SUMU_QUEUE_START -->
- [x] #767 [compiler] `fromJson` must round-trip enum fields. VERIFIED gap: for `type Cell{d: Dir, n: Int}` the generated fromJson parses only `n` and leaves the enum at 0 (`down` reloads as `up`) — toJson writes the enum name, fromJson ignores it. Nearly every UI component carries an enum, so the UI machine snapshot (#768) is blocked on this. Files: compiler/src/c_backend.c (per-struct fromJson synthesis: match the string against the enum's cases via the existing rae_enum_toString_<E> table, or emit a rae_enum_fromString_<E>). Verify: test case round-tripping a struct with an enum + nested struct with an enum; suite green. DoD: Cell.fromJson(c.toJson()) is c for enum fields.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.] DONE: BOTH sides were broken — toJson emitted the "..." placeholder for an enum field (not the name, contra the brief) and fromJson had no enum branch (left it 0). Fix in c_backend.c: emit rae_enum_fromString_<E> (member NAME -> ordinal) next to the existing rae_enum_toString_<E>; toJson now writes the member name string; fromJson extracts that string and maps it back (freeing the malloc'd json-extract temp via rae_ext_rae_str_free so leak-gated tests stay outstanding=0). Verified: test 710_fromjson_enum_roundtrip (a struct with enum+int round-trips each Dir value; a richer struct with enum-first + enum-last + int + string all round-trip); RAE_MEM_STATS outstanding=0; full suite green (370 cases + 66 examples). BOUNDARY: nested-STRUCT-field serialization is a SEPARATE pre-existing gap — a struct-typed field still emits invalid "field": ... (no rae_json_extract_object exists); enum fields round-trip inside any directly-serialized struct, but an enum inside a nested STRUCT field will not round-trip through the outer until recursive struct serialization lands. Flag for #768 if its components nest structs. LANG-CHECK: nothing new — serialize-codegen completeness, not an ECS language-feature gap.
- [x] #758 [compiler] Fix generic value-type `view T` specialization so `is`/`contains`/`indexOf` work for String and struct element types. SURFACED during #703. Two symptoms (verified): (1) a call `xs.contains(value: "b")` on a `List(String)` emits an UNSPECIALIZED `&(rae_T){...}` cast at the call site (`rae_T` undeclared); (2) a generic `view T` param (T=struct) lowers to a by-VALUE C param yet emit_expr derefs it as `(*a)` — so `func same(T: type, a: view T, b: view T) ret Bool { ret a is b }` and `List.contains`/`indexOf` (which do `cur is value`) fail to compile for String/struct element types. Root cause is generic-view param lowering + call-site arg specialization, NOT `is` itself (concrete `is` and concrete String/struct `is` all work — #703). Fix: make generic `view T` (T=struct) lower to a pointer consistently (matching concrete view-struct + emit_expr), and specialize the call-site arg cast. Acceptance: `List(String).contains/indexOf` compile and work; a generic `a is b` with T=String and T=struct compiles and gives value equality; add tests. Deps: none. NOTE: this is the last blocker to `is` being the single comparison in ALL contexts (concrete already is, after #703 deleted every eq/equals).  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.] DONE: root cause was the generic param `T` not being resolved to its concrete type at FOUR codegen points. Fixes: (1) is_primitive_ref (c_backend.c) now resolves T via ctx generics, so `view T`(=String) is recognised as a wrapper ref (rae_View_String, read `(*x.ptr)`), not a raw struct pointer; (2) emit_type_ref_as_c_type's parts-path generic substitution now carries view/mod onto the concrete type (with the Stage-6 numeric-view -> by-value collapse preserved), so `view T` lowers to rae_View_String / const Struct* / plain int64_t correctly; (3) the `is` String branch (c_expr.c) uses eff_base_name so `cur is value` with T=String takes rae_str_eq instead of C `==`; (4) the call site (c_call.c) uses the CONCRETE base to route generic String/prim args through the ref-wrapper path and substitutes T in the wrapper/compound-literal cast (fixing the `&(rae_T){...}` / `rae_T __rae_pw` undeclared-identifier). Verified: List(String) and List(struct) contains/indexOf compile and work (found + not-found, struct value equality); a generic `same(T, a: view T, b: view T) ret a is b` gives value equality for T=String and T=struct; numeric view-T still passes by value (389_list_helpers fixed after a first-cut regression). Full suite green (371 cases incl. new 711_generic_view_is_specialization + 66 examples); RAE_MEM_STATS outstanding=0. BOUNDARY: a TOP-LEVEL generic function called WITHOUT an explicit T (relying on inference from its value args, e.g. `same(a: "x", b: "x")`) is not specialised at all — a SEPARATE pre-existing gap in generic-function instantiation-by-inference (affects bare-T too, e.g. `idb(a: 3)`), independent of is/view-lowering; explicit `T:` works. LANG-CHECK: nothing new — compiler codegen fix; the inference gap is adjacent to the existing Generics & queries wishlist item.
- [x] #761 [compiler] Fix owned-String double-free when binding a method result to a local and passing it by view: `let s: String = obj.toJson(); f(valueJson: s)` (f takes `view String`) frees `s` twice (RAE_MEM_STATS string outstanding=-1, ~13B over-free), while inlining `f(valueJson: obj.toJson())` is clean. Found in #757 (ecs resource serialize). Files: compiler/src/{sema.c,c_backend.c} ownership/drop path. Verify: a `let s = x.toJson(); useView(s)` test shows outstanding=0. DoD: the let-then-view idiom no longer double-frees.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.] DONE: root cause was NOT the drop path (sema.c/c_backend.c) but the runtime producer rae_json_build (the toJson result builder): unlike every other owned-String producer (rae_ext_rae_str_interp/concat etc.), it neither mem-tagged NOR pool-registered its malloc. The C backend emits owned method results as pool temps — pool_take hands ownership to a local, a surrounding pool_flush frees an inlined temp — so an unregistered toJson result bound to a local (`let s = obj.toJson()`) was freed as an UNTRACKED heap (one 'unknown free' per call -> RAE_MEM_STATS TOTAL outstanding=-1), while the INLINED form silently LEAKED it (untagged, invisible). Fix: moved rae_json_build from a header static-inline to runtime_buffers_math.c (next to rae_json_extract_string, where rae_mem_str_tag is visible) and made it mem-tag + pool-register its result. Now both the let-then-view idiom AND the inlined form are clean. Note: _outstanding() deliberately skips the UNKNOWN site, so this class of over-free was invisible to it — added rae_ext_rae_mem_stats_unknown_frees() and test 712_tojson_let_view_no_double_free (5000 iters: 5000 untracked frees WITHOUT the fix -> verdict OVERFREE, 0 WITH -> OK; toggled the fix off/on to prove the guard bites). Full suite green (372 cases + 66 examples); existing json/serialize tests (691/696/698/700/703/710) unaffected. LANG-CHECK: nothing new — a runtime producer-protocol consistency fix, not an ECS language-feature gap.
- [x] #759 [examples] Re-home the forward-path draw-limit guard test. The three stale render gates (109/110/114) were NOT "needs real hardware" failures (contra #750) — all three rendered correctly headless and failed only on stale assertions; fixed in run_examples.sh (114 `casters 13`->415; 110 `depth pyramid: N mips` log -> `3: depthPyramid` pass-order line; 109 dropped the forward draw-limit phase after its #514 migration to the deferred path). REMAINING GAP: the runtime forward draw-limit guard (rae_g3d_push_draw_record -> "[gpu3d] ERROR: draw limit exceeded") now has NO example test host — 109 was its only one. Re-home the RAE_GPU3D_DRAW_LIMIT=4 assertion onto a still-forward example that issues >4 mesh draws (identify one empirically; 107/108/111/112 use non-mesh draw paths). Files: compiler/tools/run_examples.sh. Verify: the chosen example emits the guard error under DRAW_LIMIT=4. DoD: the forward draw-limit guard is exercised by some example again.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.] DONE: the task's premise (a still-forward example issuing >4 MESH draws) does not hold — EVERY >4-mesh example now uses the DEFERRED path, and the forward examples 107/108/111 are SDF/metaball. The guard's counter + check live ONLY in the mesh path (rae_g3d_push_draw_record, runtime_gpu3d.c:987/1013); there is no push_metaball that touches them, so SDF draws cannot trip it. The ONLY forward MESH example is 113_gltf_character (character + ground = 2 draws via gpu3d.renderScene). Re-homed the guard there with RAE_GPU3D_DRAW_LIMIT=1 (not =4 — 113 draws 2, so limit 1 is what trips it): confirmed empirically it emits '[gpu3d] ERROR: draw limit exceeded: configured=1', while the default limit renders clean. run_examples.sh 113 block now runs a second time under DRAW_LIMIT=1 and asserts the error appears exactly once (folded into 113's pass condition, so a guard regression fails 113). Full example suite 66/0. LANG-CHECK: nothing new — test-infrastructure/runtime plumbing, no Rae language-feature gap.
- [ ] #769 [ecs] Flip hierarchy authority: `Children` (ordered) is AUTHORITATIVE and serialized; `Parent` is the DERIVED reverse index, rebuilt by HierarchySystem. Reverses #711. Why: sibling ORDER is real information (layout + paint order) that lives only in Children — component tables are swap-remove, so rebuildFromParent scrambles sibling order after any destroy; a Parent-serialized snapshot would reload with shuffled siblings. Children arrays are also how humans and .raescene author trees. API: setParent stays the single mutation entry (now edits the parent's Children list + rebuilds the child's Parent); add insertChild(parent, child, index) for explicit ordering; a `rebuildParentsFromChildren` self-heal; enforce the single-parent invariant (a child may appear in exactly one Children list — reparent removes it from the old list first). depthOrder dirty-tracks on the Children table generation instead of Parent's. Update registry flags (#730): Children `serialize`, Parent 0. Files: lib/ecs/hierarchy.rae, lib/ecs/HierarchySystem/HierarchySystem.rae, lib/ui/registry_flags.rae, callers (scene_loader/scene_instance/frames unchanged API). Deps: #726. Verify: REGRESSION TEST — build a 5-sibling tree, destroy sibling 2, rebuild Parent from Children, sibling order must survive; 687/688/692/704 pass; 106 screenshot byte-identical. DoD: Children is the source of truth for the hierarchy including order; Parent is a cache.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #772 [compiler] Compile-time field reflection v1, per docs/compile-time-reflection.md: (1) LEXER: reserve lowercase `any` as a keyword (compile-time type wildcard, legal only inside a type pattern; the existing `Any` runtime box is UNTOUCHED — `ComponentTable(Any)` stays the concrete dynamic-box table type, which is exactly why the wildcard needs its own spelling); (2) SEMA: recognise `fields(value)` in the iterable position of the EXISTING `loop let x: <mode> T in EXPR` form (parser unchanged) as a field loop over a struct value; the binding's explicit type is the filter (`ComponentTable(any)` matches every ComponentTable field regardless of element type, a concrete type matches only that type); type-check the body ONCE PER MATCHING FIELD with the binding aliased to `value.<field>` at that field's concrete type; `let`+`view`/`mod` only (`var` = copy of a heap struct, rejected by the existing alias rule); a non-`fields(...)` struct iterable and `any` outside a type pattern are errors with clear diagnostics; (3) `fieldName(binding)` = compiler-recognised function folded to the slot-name String literal, also reachable via UFCS `binding.fieldName()`; (4) EXPANSION: unroll the loop into N copies of the body BEFORE codegen so BOTH backends (C + VM) get it for free — no per-backend special case; zero matching fields = empty. Scope OUT (v2, #773): `fields(Type)` / construction, bare `view any` + `match`, nested recursion. Tests: compiler/tests/cases: filter by concrete type, filter by `ComponentTable(any)`, `mod` write-through into the real field, `fieldName` literal, zero-match loop, `any` misuse diagnostic, `var` rejected. Verify: full suite via watch-tests.sh + examples green. DoD: a non-generic `func clearAll(world: mod UiWorld, entity: view EntityId) { loop let table: mod ComponentTable(any) in fields(world) { componentRemove(this: table, entity: entity) } }` compiles on both backends to the same calls the hand-written destroyEntity makes today.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #773 [compiler] Field reflection through a GENERIC world parameter: `func clearEntityComponents(W: type, world: mod W, entity: view EntityId) { loop let table: mod ComponentTable(any) in fields(world) { ... } }` must instantiate per concrete W (UiWorld, World3d, GameWorld) with the field loop expanded AFTER generic substitution (the loop's field set depends on the concrete W, so expansion must happen per instantiation, not once on the generic body). Also infer W from the argument so callers write `clearEntityComponents(world: uiWorld, entity: e)` with no explicit W. This is split from #772 on purpose: it leans on the generic-substitution machinery that needed #758 and still cannot do two-type-param generics (memory: decompose into single-type-param helpers) — expect it to surface generic bugs; fix them here, not by special-casing the loop. Tests: one generic func used against two different structs in one program; `fieldName` inside a generic body; `view` and `mod` variants. Deps: #772. Verify: full suite + examples. DoD: the SAME generic function clears entities in UiWorld, World3d and the 116 GameWorld.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #760 [ecs] FIRST CONSUMER of field reflection (#772/#773): make whole-world ops zero-edit. (1) lib/ecs: generic `clearEntityComponents(W: type, world: mod W, entity: view EntityId)` as the `loop let table: mod ComponentTable(any) in fields(world)` from docs/compile-time-reflection.md; (2) lib/ui/ecs.rae `destroyEntity` calls it (drop the hand-listed per-table `componentRemove`s; hierarchy detach + `freeEntity` stay explicit); (3) examples/116_ecs_headless_sim `worldToJson`/`worldFromJson` become the `loop let table: view ComponentTable(any) in fields(world) { addComponentTable(..., name: fieldName(table), registry: reg) }` form — registry flags STILL gate which tables serialize, the loop only removes the hand-listing; (4) 114 gameworld.rae / lib/scene3d.rae: use the generic clear wherever an entity is despawned (net/physics seams from #746). NOT a compiler builtin, NOT an @-attribute (history: the original #760 plan was a one-use sema/c_backend builtin — rejected, see AGENTS.md). Deps: #772, #773. Verify: full suite + examples + RAE_MEM_STATS outstanding=0 on the ECS leak-gated cases; 116 snapshot round-trip identical before/after. DoD: add a new `ComponentTable(T)` field to UiWorld or GameWorld and NO edit is needed in destroyEntity / worldToJson / worldFromJson (prove it with a test that adds a table and round-trips it).  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #768 [ui] UI MACHINE snapshot: worldToJson/worldFromJson for UiWorld, registry-driven (one addComponentTable/loadComponentTable per table gated by createUiComponentRegistry() from #730 — derived caches skipped by flag); write it as the `loop let table: view ComponentTable(any) in fields(world)` form from #760/#772, NOT a hand-list, so a new table needs no snapshot edit). This is SEPARATE from the human-authored .raescene loader (applyComponentByName stays; it parses a hand-friendly grammar with enum strings + theme tokens, not struct JSON). Files: lib/ui/serialize.rae (new). Deps: #767 (enum round-trip), #730. Verify: load a .raescene, snapshot, reload into a fresh world, snapshot again -> byte-identical JSON; layout+render parity. DoD: a UiWorld round-trips through the registry with no per-component switch.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #749 [cleanup] Documentation: flip docs/ecs-general-architecture.md status to implemented; add a lib/ecs API reference (entities/handles, ComponentTable, query2/3, HierarchySystem, TransformSystem, registry+serialize); cross-link from ui-render-loop-performance.md and value-aggregates docs. Files: docs/*. Deps: #747, #748 (after all implementation tasks). Include the new API surface: Schedule, resources, tags, EventQueue, and the PURE-RAE world helpers (per-table destroyEntity + registry-driven serialize) — note there is NO compiler-synthesised world-helper builtin (see #760); the zero-boilerplate version is a future field-reflection/derive language feature, not shipped. Verify: docs consistent with code. DoD: docs describe the shipped ECS.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #750 [verify] Final integration: full `make test` green (cases + examples), screenshot parity for 105/106/112/114 (noting the pre-existing GPU-only failures 109/110/114 that need real hardware), and RAE_MEM_STATS outstanding=0 for the leak-gated ECS tests. Files: whole tree. Deps: ALL of #700-#749 and #751-#761 + #767/#768/#769 (every ECS-surfaced task now sequenced before it). Verify: green suite + parity + no leaks. DoD: the ECS migration is complete, verified, and green.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #764 [lib] Migrate the lib-level mutable globals into App-owned resources per docs/globals-and-app-ownership.md: lib/ui/theme.rae `activeTheme`+`g_themeGeneration` -> a `Theme` resource on App (also closes the two-globals bug at app3d/ui_shell.rae:74); lib/gpu2d.rae `gG2dColorAtts` -> `Gpu2dContext` handle; lib/gbuffer_passes.rae `gTaa*`/`gComposite*` -> renderer resource; lib/profile.rae `gProf*` -> `Profiler` resource. C runtime state stays global behind `extern` (no `unsafe` keyword). Deps: #750 (these libs are reworked by #737/#748 first). Verify: suite green; 106/111/112/114 screenshot parity. DoD: zero module-level `var` in lib/.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #766 [compiler] No-globals diagnostic, ERROR phase: flip #763's warning to a hard compiler error. Deps: #764, #765. Verify: full suite + examples green with the error enabled. DoD: a module-level `var` or heap `let` fails to compile; the rule in docs/globals-and-app-ownership.md is enforced.  [LANG-CHECK: after finishing, note in docs/ecs-language-wishlist.md any missing Rae language feature that would have made this task easier or cleaner — add a bullet only if it is new there; skip if nothing new.]
- [ ] #771 [renderer/ecs epic] Migrate the deferred renderer off the hand-rolled Scene3d SoA onto lib/ecs World3d, for a SINGLE 3D ECS world end-to-end (the REAL version of the old #748, which was mis-scoped as a 2-file cleanup). Scope: (1) give renderScene/renderSceneShadow/renderDeferredPass a World3d path (query MeshRenderer/Transform3d/Material3d); (2) migrate the direct Scene3d builders — 96_easing_3d, 97_tetris3d, 108/109/110/113/115 — to World3d; (3) parse .raescene straight into World3d (drop the Scene3d intermediate from loadWorld3dFile; move 111 off Scene3dAsset); (4) then delete Scene3d + Scene3dAsset + buildWorld3dFromScene3d. Deps: #716/#732 (World3d exists). Verify: suite green + screenshot parity for every migrated example. DoD: no Scene3d/Scene3dAsset in the tree; the renderer takes World3d.
- [ ] #770 [naming] Symmetric ECS naming: World2d (rename UiWorld) / World3d (exists) for ECS CONTAINERS; Scene2d (rename lib/ui/scene.rae Scene) / Scene3d for authored .raescene CONTENT UNITS. CORRECTION (was based on a false #748 assumption): the Scene3d name is NOT freed — Scene3d stays as the live shared RENDERER type until the renderer->ECS epic #771 retires it, so the Scene3dAsset->Scene3d half is BLOCKED on #771. Weigh value first: UiWorld->World2d is 100+ refs of pure churn that DROPS the UI-domain hint, and the type-collision problem it targets is better solved by per-module type namespacing (docs/ecs-language-wishlist.md "Namespacing & modules") than by a mass rename. Recommend: do the low-churn lib/ui Scene->Scene2d if wanted, gate UiWorld->World2d on a human call, defer the Scene3d half to #771. Deps: #771 (frees the Scene3d name). Verify: build + case suite green. DoD: naming decision recorded and whatever renames are chosen land cleanly.
<!-- SUMU_QUEUE_END -->
