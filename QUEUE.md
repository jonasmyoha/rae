<!-- SUMU_QUEUE_START -->
- [ ] #115 [Tier 0→1] ECS draw-list extract (clip-in-render-walk DONE 2026-07-01, commit d465286; flat DrawList IR + raylib-removal deferred to parked #122): pure O(n) system walking UiWorld alive entities → flat DrawList {boxes,glyphs,images,clips}, sorted by (layer,clip,pipeline); no nested per-entity scans. Introduce lib/ui/draw.rae draw-sink interface (WebGPU impl) so lib/ui stops importing raylib. See doc §9.
- [ ] #116 [Tier 0→1] Example: ui_basic — simple data-driven UI (panel + buttons + list + toggle), render-only/minimal interaction. Proves ECS extract → DrawList, clipping, layering, text+box compositing.
- [ ] #117 [Tier 1] Offscreen render targets + Image pipeline (textured quads for icons/photos AND 3D render-target output) + premultiplied-alpha compositor ordering 2D passes and 3D targets onto the swapchain. See doc §5/§7.
- [ ] #118 [Tier 1] SDF clipping (box pipeline DONE 2026-07-01, commit 2f352c1; text/image rounded-clip follow-up): per-instance clipId indexing a uniform array of rounded-rect clip regions (rounded/nested clips, e.g. rounded card containing a scroll list); scissor stays the axis-aligned fast path. See doc §6.3.
- [ ] #120 [Tier 1, stretch] Example: ui_3d_embed — raytracer/spinning mesh inside a rounded UI card + fullscreen toggle. Proves render targets + compositor + 3D-in-UI/fullscreen-3D.
- [ ] #121 [Tier 2] Polish/effects: stencil arbitrary-path clip, separable blur/glow/shadow passes, compute-driven particles (via lib/gpu.rae) + particle pipeline, per-widget custom fragment shaders, static-subtree offscreen caching.
- [ ] #123 [raepack v2, design] Finalize v2 semantics AND syntax — resolve the provisional names + grammar in docs/raepack-v2-and-packages.md (executable/execution/module/embeds/requests/grants/delivery/overrides), the meta-vs-devtools split, and the interface-IDL direction (§15 Q1/Q4). Gate before any implementation. Design/spec only.
- [ ] #124 [raepack v2] Implement v2 parsing + normalized JSON output: parse the v2 schema (package meta, devtools block, dependencies, modules, targets, capabilities) in raepack.c; extend `rae pack --json` to emit the full normalized manifest incl. the devtools presentation block. Keep a v1 parse path temporarily.
- [ ] #125 [raepack v2] Make `rae run` / `rae watch` / `rae build` discover + honor a `.raepack` in the folder: read default target + entry + module composition from the pack (building on zero-config inference); `.raepack` stays optional (no manifest → existing zero-config defaults).
- [ ] #126 [raepack v2] Migrate devtools metadata to the pack + remove devtools.json: devtools consumes `rae pack --json` (incl. the devtools block) instead of readExampleMetadata; one-time script generates minimal `.raepack` (meta+devtools) from each devtools.json; delete devtools.json; folder-derived metadata for packless examples. Examples still list without a pack.
- [ ] #127 [raepack v2] Remove/migrate per-source emit + hybrid-as-backend: drop `emit: live|compiled|hybrid` per source and the `hybrid` target enum; migrate v1 packs (92_pong_import, 22_raepack_demo, 302_auto_app) to module boundaries; hybrid becomes native-host-embeds-wasm composition.
- [ ] #128 [raepack v2] Implement package-level module composition: modules defined once at package level (native|wasm + interface + capability request); targets select native modules and `embeds` wasm modules with per-target overrides (delivery, grants, jit).
- [ ] #129 [raepack v2] Design + implement typed native↔WASM interfaces with generated marshalling (§9): the interface IDL (open question #123), value/handle crossing rules, versioning, host-import/guest-export glue generation. Largest unknown — likely its own sub-design first.
- [ ] #130 [raepack v2] Implement capability validation + runtime enforcement (deny-by-default, §8): the three-way contract (host exposes / module requests / target grants); validate granted ⊆ requested ⊆ exposed at build+load; runtime denies ungranted host imports. A module request must never grant.
- [ ] #131 [raepack v2] Add dependency management: path + Git dependency sources, a `rae.lock` lockfile (pinned rev + content hash), resolver integration with the stdlib/module search (resolve_stdlib_root / try_resolve_lib_module). Registry deferred. Workspaces deferred.
- [ ] #132 [raepack v2] Investigate bundled vs OTA WASM delivery — separately for desktop and iOS (§11): non-JIT interpreted execution, integrity-check, versioning vs interface, rollback, capability-gating. Default bundled; OTA explicit + policy-controlled. No iOS approval claims. Investigation/spec before implementation.
- [ ] #133 [Live soft-deprecation] Formalize the Live test split (docs/live-vm-status.md): designate a tiny Live SMOKE subset (VM starts, basic call, basic alloc/ownership, one hot-reload) runnable as opt-in `TEST_TARGET=live` / a separate non-blocking job; mark the rest as historical-retained-but-disabled. Default `make test` is already Compiled-only.
- [ ] #134 [Live soft-deprecation] Strip `live` from ordinary examples' supportedTargets (and the future .raepack equivalent) so the UI never offers Live for them. Don't delete example code. Rides with the devtools.json→.raepack migration (#126).
- [ ] #135 [Live soft-deprecation] Relocate the remaining hot-reload examples (24_code_hybrid_hot_reload, 99_data_hot_reload, 100_code_hot_reload) under examples/legacy/live/ and/or hide them in devtools, so in-process VM hot reload no longer influences the normal workflow. Document the temporary capability gap (replacement = native-hosted WASM module replacement). Keep that example code — these three run on Hybrid or Compiled. 23_code_hot_reload was live-only and has been DELETED instead, superseding this item's original "keep the example code" for that one.
- [ ] #141 [gpu2d, render-loop] lib/gpu2d.rae now exposes pointerX/pointerY (design units), pointerDown, wheelMove, nowSeconds, waitEvents, and RAE_GPU2D_SCREENSHOT (surface readback → BMP). example 105 is a self-contained data-driven mobile UI (ECS + scroll physics + hybrid loop + nav routing + theme text). Remaining for true 98 parity: this is NOT yet driven by the same render path as the raylib lib/ui.
- [ ] #151 [responsive] Safe-area Phase 2 — phone-in-window render: when a device preset is active, render the world into a preset-sized offscreen target and centre it in the actual window with a device frame (rounded bezel, faked status bar). See docs/ui-viewport-and-safe-area-plan.md Phase 2. Deferred until a product reason (depends on #117 offscreen render targets).
- [ ] #152 [responsive] Safe-area Phase 3 — editor camera: wrap the phone-in-window blit in a zoom/pan camera (wheel-zoom-around-cursor, middle-drag-pan) with inverse-transform pointer mapping so input still hits the right widget at non-1x zoom. See plan Phase 3. Deferred.
- [ ] #228 Finish docs/image-decoding-design.md (#217) follow-up: stb JPEG is vendored and raylib fallback removed, but valid Spotify JPEGs fail in stb on macOS (`can't merge dc and ac`), so keep ImageIO as the macOS default, gate stb testing behind `RAE_G2D_STB_JPEG=1`, add corrupt-file/error-policy coverage, and decide the non-macOS JPEG strategy before marking complete.
- [ ] #240 Language: add real `const` declarations (compile-time, immutable). Today "constants" are zero-arg functions (e.g. `func PI()`/`func TAU()` in lib/math.rae) because Rae has no immutable value binding (`let` is reassignable). Proposed: `const NAME: Type = <const-expr>` at module (and local) scope; UPPER_SNAKE naming; const-expr folding (literals + ops on consts); reassignment is a compile error; folded/inlined in both backends (VM constant pool, C `static const`/literal). Then convert PI()/TAU() to `const PI`/`const TAU` and drop the call `()`.
- [ ] #241 Lint: optional compiler/linter WARNINGS for naming-convention violations (Types/enums PascalCase; functions/vars/consts/enum-cases camelCase). Must be warnings, not errors; exempt/suppressible for extern (C-binding) names. Needs a general warning framework first.
- [ ] #242 Examples: raytracer step 6 — GPU raytracer (deferred; needs a compute/GPU path).
- [ ] #244 Prototype: WASM generation via the C backend (clang/wasi-libc or Emscripten). Compile the raytracer to .wasm, run from a tiny JS harness writing into a SharedArrayBuffer drawn to a <canvas>. Measure WASM-vs-native speed. (Highest value-per-effort; gates the rest.)
- [ ] #245 Prototype: WASM threads (SharedArrayBuffer + workers) + SIMD128 for the tiled raytracer — confirm real parallelism in the sandbox.
- [ ] #246 Prototype: iPhone PoC app with OTA updates — Capacitor-style WKWebView shell running the Rae→WASM app, WASM/JS bundle hosted remotely and updated over-the-air without App Store resubmission (native shell fixed, WASM layer updatable). Confirm the App-Store-legal split.
- [ ] #247 Prototype (if needed): one Rae→C native plugin driven at control-rate (e.g. audio) to validate the partition-by-data-rate bridge; Rae generates both ends.
- [ ] #248 Later: WebGPU path from the WASM app (GPU compute / GPU raytracer step 6).
- [ ] #251 GPU denoiser / temporal reprojection for the interactive path tracer (example 52/53). Motion can't accumulate across frames (camera moved → reset every frame), so it's limited to spp samples = noisy while moving. Reuse the previous frame's result warped to the new camera (reproject by re-projecting world hit points / or screen-space motion vectors) so accumulation survives camera motion (TAA-style), optionally + an à-trous / edge-aware spatial denoise pass. Cleanest motion at high fps; needs a history buffer + per-pixel reprojection in the WGSL. Alternative cheaper lever already scoped: auto-drop render resolution while moving (reuses the P-toggle machinery). Effort: large.
- [ ] #252 Strategic (parallel track): iPhone OTA proof-of-concept. Wrap the wasm app in a WKWebView/Capacitor shell, host the bundle remotely, demonstrate over-the-air updates (native shell fixed, wasm/JS layer updatable). Proves the deployment thesis (WASM-first, OTA-legal on iOS).
- [ ] #254 (gated dogfooding) Own DEFLATE + the PNG container in Rae. Inflate first (~400-600 LOC; decode is what texture loading needs), validated against lodepng; then deflate (fixed-Huffman first, then LZ77 hash-chain match-finder). Shared later with .raepack/gzip. lodepng stays as the correctness+speed reference oracle; drop only once the Rae path passes round-trip + zlib-interop + acceptable-speed gates. ~1500-2500 LOC across bitio/huffman/inflate/deflate/checksums/png modules. See docs/png-and-deflate-strategy.md. Effort: large.
- [ ] #255 Module namespacing for the stdlib (Option A) — see docs/module-namespacing.md. Stdlib accessed via module qualifier (math.sin, filesystem.exists, gpu.reset); project files/subfolders keep flat auto-visibility; core stays global. Dot resolution: LHS that is an in-scope stdlib module name => namespace qualifier, else value => normal UFCS; unqualified UFCS must NOT search stdlib namespaces (collision risk). Phase 1 (additive, front-end only ~100-150 LOC in main.c merge_module_graph + sema.c method_call): stamp each decl with its module_path, resolve `mod.func(args)` when LHS is a module name (rewrite to plain call, no receiver) alongside existing flat calls. Phase 2: migrate stdlib+examples module-by-module (suite green each step). Phase 3: drop flat auto-load of non-core stdlib. Optional: `import ... as` alias; namespace-qualified UFCS (path.filesystem.exists()).
- [ ] #256 lib/filesystem.rae — Rae Filesystem & Paths API (namespaced), wrapping SDL3's SDL_filesystem.h (known folders incl. Desktop/Pictures, list/glob/getPathInfo, makeDir/remove/rename) + pure-Rae path helpers (join/baseName/dirName/ext). See docs/filesystem-and-paths.md. Then fix the raytracer PNG save: target = Desktop (else prefDir/render), filename rae_render_<ISO-date>_<NNNN>.png via glob-scan next-index (no overwrite). Needs a runtime strftime date helper. Depends on module namespacing.
- [ ] #257 Namespacing test-case debt — ~38 tests/cases fail with "X is in package Y, which is not open here" (json/hot_reload/file_watch/time/raylib) after the module-namespacing migration. Add the needed `open`/qualified calls (or per-case prelude) so `make test` is green again. Pre-existing, non-blocking; unrelated to recent compiler changes (verified: 525_bitwise_ops passes both targets, no codegen regressions).
- [ ] #258 Theme M6 follow-up — container styles: apply a container style's `text` style to the node's child Text entity (needs a child-propagation pass at scene-mount; today the `text` field is parsed+carried but not applied), and add `icon` styles (§5 tier 5, never built) so a container style can reference an icon size+tint bundle. lib/ui/container_style.rae currently expands only background/radius/padding.
- [ ] #259 Theme M5 follow-up — migrate remaining 106 code-driven colors to slots so the theme toggle drops its world rebuild (pure repaint). The page background now theme-tracks via `pageBgColor`+the `background` palette slot, but the nav-bar (surface@238), pill, and gradient blends still bake at world-build, so the toggle keeps `switching=true`. Blocker: those carry per-instance alpha/blend a bare slot can't model — needs slot+alpha composition. (Re-adds the follow-up lost when the queue was renumbered.)
- [ ] #273 ECS E3b — port now-playing title/artist/time labels to Observe (the deferred half of #268): add nowPlaying title/artist to PlaybackState (+ time from elapsed/duration), tag TrackTitle/TrackArtist/TimeNow/TimeTotal + MiniTitle/MiniArtist with a text DataDependency, and have UiPlaybackSystem re-point them by identity; then delete pollSpotify's direct updateNodeText writes. Split from #268 (which fixed the cover) because it's entangled with the default-ON Spotify text path, which can't be exercised headlessly. §1/§9.
- [ ] #274 ECS E4-pre — port the DataDependency observe carrier off DataResourceId/DataFieldId onto the SystemRegistry Observe (SystemId + component ids, #262/#263), so #269 can delete those enums. These enums are still the field types of the live DataDependency component (used by the play/pause icon, cover, liked heart, playing-track row highlight, debug toggle) + lib/ui/registry.rae helpers, so they are NOT dead code — #269 only removed the write-only AppDataResources/bumpXResource/dataResourceRevision mechanism. Wire SystemRegistry into 106, then delete DataResourceId/DataFieldId. §8/§10.
- [ ] #277 Testing — generic headless UI scroll-injection mechanism: a general test API / env var (e.g. RAE_UI_SCROLL=<pageId>:<y> or a scripted-input file) that sets scroll positions before a headless screenshot, so automated tests can byte-verify scrolled UI (needed to validate #270c virtualization, but kept generic — not virtualization-specific — since scripted UI state will be useful for other snapshot tests too).
- [ ] #279 Concurrency follow-ups (build on #271's Channel(T)): (a) richer-than-Int payloads — box a struct T behind a heap pointer per slot (or a byte-ring with element size) so Channel(command-struct) works, not just Channel(Int); (b) once (a) lands, port ONE real 106 system's inbox onto a Channel and run it on a spawn'd worker (NOT gpu2d asset upload — GPU calls must stay on the main/GL thread; pick a pure-CPU system such as a metadata/JSON loader or a compiler-worker stand-in), observing results read-only via revisions; (c) consider a blocking receive / wake primitive so a worker needn't busy-poll (only if a real use demands it — keep the model small). §3.2.
- [ ] #281 106 — residual ~1MB/30-60s leak (interp site): the history-art idle re-scan (loadCachedHistoryArtChunk) re-processes never-completing CDN-fetch entries on every idle wake (~20Hz), churning interp/fetch strings (some leak) and re-firing fetchArtworkAsync for already-in-flight entries. Bound it: track already-fired entries (fire each fetch once), and/or stop re-scanning once all reachable entries are loaded/failed/enqueued (pick up late completions on the next history (re)build instead of live). Removes the churn + the residual leak.
- [ ] #282 Leak (real, supersedes #280 premise) — PER-FRAME LEAK FIXED 2026-07-13, residual is flat one-time/global. Root cause of the growth: owned String temps/locals detached from the statement pool via `pool_take` but not auto-dropped at scope exit. THREE fixes landed: (1) commit 0a77060 — cascadeGpu2dAssets latches once covers load (stopped per-frame albumCoverKey rescan). (2)+(3) commit f29e582 (compiler) — (a) extern/FFI params BORROW unless `own` (c_call.c): owned String passed to an extern was move-marked but never freed → now caller auto-drops; (b) member/index String returns are OWNED not aliases (c_stmt.c): ret codegen always deep-copies `ret x.field`, so the classifier now agrees and callers drop the copy (fixed pageOf/jsonObjectKeyAt). Measured (106 headless, history, Spotify off): String outstanding 3497→9065 growing (~680KB/min) → FLAT 756→757 over 6s→18s. Full suite green (167 unit + 66 example, 0 fail, no double-free). REMAINING for literal-0-at-exit (~757, flat, not unbounded): (i) module-global container caches never scope-dropped (theme g_activeContainer*/g_activeSpace*/etc.) — needs a global-teardown mechanism or explicit cleanup; (ii) verify scene-registry/JsonDoc cascade fully drops JsonValue/JsonField Strings (top residual sites: parseValue 210, optFloatOrToken 93, loadSceneFile 74, mountSceneInstanceIntoPage 68, StringMap keys 65). #432 (rebuild leak regression) passes at 0, so the mount/rebuild cycle is leak-free; residual is the live-at-exit state + globals. RA/dladdr outstanding-by-callsite instrumentation (temp patch in rae_mem_str_tag + rae_mem_stats_print) localises sites.
- [ ] #284 Leak-to-zero (2/3) — module-global theme caches never scope-dropped: g_activeContainerNames/BgSlots/RadiusTokens/PadTokens/TextStyles, g_activeSpaceNames/RadiusNames/PadNames (List(String) in theme_resolved.rae) + any other module-level `var` container. These live the whole process so they show as outstanding at exit. Needs a global-teardown mechanism — either a language feature (module-level destructors / an `atexit`-style drop for module `var`s) or an explicit `clearThemeCaches()` cleanup called from main teardown. Prefer the language feature for professional quality; propose design first.
- [ ] #285 Leak-to-zero (3/3) — scene/JsonDoc held-state cascade. Scenes hold their JsonDoc (type Scene { doc: JsonDoc }) for the app lifetime; outstanding at exit includes parseValue (JsonValue Strings), jsonString, loadSceneFile source, mountSceneInstanceIntoPage, StringMap keys. Verify the scene-registry auto-drop cascades fully through JsonDoc (List(JsonValue)/List(JsonField) → each element's Strings) and scene node-id maps; close any cascade gap so parsed scene state frees to 0 at teardown.
- [ ] #307 3D: Mat4/Vec value types to stop per-frame heap churn — every `mat4*` returns a fresh `List(Float)`, so a frame allocates dozens of transient matrices. Rae has no fixed-size arrays, so the practical shape is a `Mat4` of four inline `Vec4` columns (+ `Mat3`, and `Quat` to replace Euler `Vec3` rotation in Transform3d). Measure the allocation count before/after.
- [ ] #308 3D: inverse-transpose normal matrix — normals are currently transformed by the model matrix directly, so non-uniform scale skews lighting. Pass a proper normal matrix per draw (or renormalize in-shader) and lift the "uniform scale only" constraint documented in lib/gpu3d.rae.
- [ ] #310 3D: shrink the C layer to raw WebGPU — move embedded WGSL, PBR policy, material packing and render-graph decisions out of `runtime_gpu3d.c` into Rae; C keeps resource/pass primitives only (graph scheduling itself moved to lib/rendergraph.rae under #331; WGSL/PBR/material packing remain). Centralize buffer-layout constants + add layout validation tests so Rae packing and WGSL structs cannot drift.
- [ ] #311 3D: port the raymarchers off the CPU round-trip — 107/108 do `gpu.run` -> `downloadU32` -> `updatePixels` every frame. Render the compute output into a storage texture and blit into the gpu2d offscreen (or convert to a fullscreen fragment pass over the scene SSBO). Removes the readback, unifies present/screenshot, enables the 2D HUD overlay. Adopt 107's split static/per-frame buffer policy while doing it.
- [ ] #312 3D: raymarch backend consumes lib/scene3d — wire 107/108 to `SdfPrimitive`/`Material3d`/`Camera3d` from scene3d so one authored scene drives both backends; share one WGSL material include (GGX/Smith/Schlick/ACES) between raster and raymarch so materials match across backends.
- [ ] #313 3D: owned renderer/device context — GPU3D currently reaches into GPU2D's private surface/offscreen globals, and resource tables are global fixed-capacity C arrays with only whole-renderer teardown. Introduce an owned device/renderer context (without breaking 106), plus generation-checked handle validation in the registry, resize, device loss and partial resource destruction.
- [ ] #314 3D: extraction/culling upgrade — grow scene3d's parallel arrays into `ComponentTable`-backed storage with transform hierarchy propagation, dirty revisions (upload only what changed), frustum culling, and sort/batch into immutable frame data. Keep app-facing `beginScene`/`renderScene` unchanged.
- [ ] #315 3D: hybrid SDF/mesh depth — Metal-native M1 now renders animated smooth-union `SdfPrimitive` spheres through a fullscreen fragment pass writing `frag_depth`, with example 110 scene 1 as the acceptance fixture; finish general proxy bounds/shapes and validate runtime behavior across Vulkan/D3D12 and browser WebGPU before depending on it broadly.
- [ ] #326 Assembly 2026 demo track — build an impressive procedural realtime scene over the unified renderer with generated meshes, FBM/ridged/curl/domain-warp noise, camera choreography plus interactive debug camera, materials, particles/effects, post grading, and audio-reactive hooks; ship native desktop first, with web delivery gated by #321.
- [ ] #327 Browser frame-callback API — replace the `-sASYNCIFY` compatibility yield used by `--target wasm` with a first-class Rae/SDL3 requestAnimationFrame application loop, preserving renderer APIs while reducing bundle/runtime overhead and making browser lifecycle control explicit.
- [ ] #330 RENDERER ARCH — architecture decided in `rae/docs/render-graph-and-gi-plan.md`: render graph from v1, HDR pipeline, TAA (MSAA dropped), and real-time dynamic GI over a unified scene representation with SDF-based transport leading. Baked GI is excluded as a primary path because destructible content invalidates it. #330-#340 implement that document in dependency order; read it before starting any of them. Architecture is committed, parameters (cascade resolution, accumulation constants, proxy choices) are experimental.
- [ ] #338 Unified dynamic GI scene representation — the core architectural piece, and the reason the GI question is tractable. Do NOT build "an SDF GI pass"; build a representation that light transport queries via distance(p) / trace(ray) / radiance(p, dir), composited by min() over distance from PROVIDERS: analytic SDF (metaballs — exact, zero build cost), voxel clipmap from triangles with dirty-region updates, per-object SDFs baked object-space and instanced for rigid dynamics, proxies (capsules) for skinned, and screen-space fallback for anything missing. Decisive property: RENDER GEOMETRY != GI REPRESENTATION, which turns every hard geometry class into a pluggable policy rather than an architectural veto. Requires #336's change signal.
- [ ] #339 First SDF GI prototype against the #338 query interface. Leading direction on three Rae-specific grounds: metaballs are already an SDF so they trace exactly with no voxelization; destruction dirties a bounded region which suits grids over BVH refit; sphere tracing suits a platform with no fixed-function BVH traversal. Radiance mechanism deliberately unfixed — evaluate voxel radiance injection vs trace-and-shade vs radiance cascades behind the same radiance() query. Known limits to design around, not around: thin geometry is not fully solvable at reasonable voxel resolution (mitigate with the screen-space component), and cascade memory is bounded by the WASM target before parameters are chosen.
- [ ] #340 Validate GI against the compute path tracer — `rae_ext_webgpu_raytrace` becomes a graph pass writing HDR instead of reading back to CPU, and serves as in-engine ground truth: render the same scene at offline quality and diff against the realtime GI. Most projects do not have a reference tracer in-engine; Rae does. Argues for unifying its scene encoding (currently a flat sphere list) with lib/scene3d.rae.
- [ ] #341 Screen-space GI as a FALLBACK COMPONENT inside the GI pass — NOT a standalone phase and NOT a stepping stone. Its role is covering geometry the volumetric representation cannot see: thin detail below voxel resolution, skinned characters absent from the field. Also useful as cheap ground truth during #339 development. Do not build it as a separate technique to be replaced later.
- [ ] #342 Graph optimisation — transient pooling, resource aliasing, barrier inference, per-pass GPU timing. Explicitly LAST and driven by measurement: v1 (#331) deliberately omits these, and they are additions behind an unchanged declaration surface rather than a redesign. Do not start before pass count or memory pressure justifies it.
- [ ] #350 Direct shadows (shadow maps) for the sun — SSAO (#337) attenuates INDIRECT light only, which is correct but means a sunlit floor shows almost no darkening beneath an object sitting on it: the hard contact shadow you expect there is DIRECT light occlusion, not ambient occlusion. Verified in 110 — the AO buffer clearly darkens the plane under the spheres, but the composited image barely changes because the plane's ambient fraction is small next to the sun. Without this the renderer will keep looking like objects hover even when they are touching. DESIGNED in rae/docs/shadow-system-design.md; implementation split into #382-#386. The design is a three-layer hybrid (cascades + screen-space contact + SDF cone trace) rather than plain CSM+PCF, because no single technique meets soft-edges, low-shimmer and mobile-to-desktop scaling at once. Must compose with #337 (AO on indirect) and later #339 (GI) without double-darkening.
- [ ] #353 READINESS TRACK A2 — enforce ownership transfer at container boundaries per #351's rules: compile-time diagnostic for non-owned values into `own` params, defined+tested semantics for List(T) with heap-field structs (the known deep-copy gap), and a test suite that specifically exercises value aggregates through containers (the combination the renderer leans on hardest). This closes the class of bug behind log_overlay's double-free and the #225 own-param alias. PARTIAL: the own-argument rule + one-hop alias tracking land for direct calls and close #222's exact shape; it found five real latent hazards in 106_mobile_ui (fixed). Globals bound to string literals are exempt (their drop is a no-op). NOT covered: generic-container method calls — sema never binds them — filed as #363.
- [ ] #356 READINESS TRACK B — target-renderer vertical slice, the REAL exit criterion for Track A: a thin but genuine slice of the deferred architecture — G-buffer pass -> depth pyramid -> one lighting pass -> composite — with transforms and a skinning palette flowing through the #354 value types, declared in the existing render graph (lib/rendergraph.rae). NOT a port of the forward prototype and NOT a microbenchmark. Everything the slice cannot express naturally becomes a new language requirement filed back into this track. Acceptance: the slice's per-object data path is allocation-free. IN PROGRESS. (1) Frame declared as a render graph in lib/renderer_deferred.rae (gbuffer -> depthPyramid -> lighting -> composite -> present), order DERIVED not written, pinned by test 572; depth pyramid included from the start so Hi-Z culling slots in later rather than reshaping the frame. (2) G-buffer pass built: lib/gbuffer.rae + compiler/runtime/runtime_gpu3d_gbuffer.c write albedo (rgba8unorm) / world normal (rgba16f) / metallic+roughness (rgba8unorm) / depth32float, no lighting; world position reconstructs from depth rather than being stored, and emissive is deliberately absent since it is an additive term rather than a BRDF input. renderDeferredFrame dispatches on pass tag over the DERIVED order — no ordering written in the dispatch. Separate pipelines/uniforms/targets from the forward frame; only meshes (an asset) and the platform present are shared. ALLOCATION-FREE IS NOW MEASURED: added an always-on cumulative alloc counter (rae_ext_rae_mem_alloc_total) because the existing per-site stats are opt-in and would report zeros — test 573 asserts exactly 0 allocations across 512 objects x 20 frames, verified to report 40 and fail when the path is made to allocate. Example 111 is the vertical slice on a real scene, with all four channels screenshot-checked separately. (3) Depth pyramid (MIN reduction, fragment passes), deferred lighting (one fullscreen BRDF evaluation over the G-buffer into linear HDR — cost per pixel, not per object-pixel) and composite (exposure/ACES/gamma) all built; the frame now produces a LIT image and every declared pass executes. Test 573 widened to measure renderDeferredFrame, so the graph walk (which reads a PassDesc per pass, and PassDesc carries a String) is inside the measurement — still zero. Deliberate divergence from the forward pass: hemisphere ambient blends on N.z, not N.y — the world is Z-up and the forward pass predates the convention; fix it there separately. The pyramid is built but UNREAD: its consumers (Hi-Z culling, deferred SSAO) do not exist yet, and the graph's lighting->pyramid edge is a placeholder rather than a true dependency — revisit that edge when the real consumer lands. Remaining for #356: the skinning palette through the #354 value types, which is the last acceptance item.
- [ ] #357 READINESS TRACK D — performance items pulled in when #356 shows they matter: (a) SIMD lowering of Vec4/Quat ops in the C backend to __m128/float32x4_t with scalar fallback — start with quaternion multiply ONLY (the one op the reference engine hand-vectorised; skinning's hot inner loop) and measure before widening; (b) request the WebGPU `shader-f16` device feature in runtime_webgpu.c (runtime-only change, cheap enough to do early) enabling `enable f16` in AO/blur/bloom/lighting WGSL; (c) `Float16` CPU storage type ONLY if profiling shows packing is hot — conversions, not arithmetic.
- [ ] #358 READINESS TRACK E — structural items designed against #356's real shape, NOT in the abstract: (a) cross-thread hand-off for a render worker — compiler-checked ownership transfer so a queued scene packet cannot be mutated after send, an immutable-snapshot notion for interpolation, bounded queues; (b) SoA support ONLY if profiling of the allocation-free AoS slice demands it; (c) function references ONLY if a data-driven pass registry emerges as a real need (tag-switch dispatch is serving fine and keeps passes statically enumerable). Each sub-item needs its own design pass when pulled.
- [ ] #359 RENDERER REBUILD GATE — note: #350 (shadows) and #338-#342 (GI arc) are DEFERRED behind the readiness tracks and will be built against the NEW deferred architecture from #356 rather than the forward prototype. The architecture review (session 2026-08-07) additionally requires before world-space GI: geometry retention (meshCreate currently frees CPU-side vertex data after upload, so nothing can build a BVH or voxelise) and a triangle-capable reference tracer (#340's tracer is sphere-only). Fold both into the rebuilt renderer's design.
- [ ] #364 Renderer: split compiler/runtime/runtime_gpu3d.c (1045 lines, over the 1000-LOC guideline — it was already 1033 before #356's shared-present refactor added 12). Natural seams: mesh upload/asset storage, the forward scene pass + its WGSL, and the tonemap/TAA resolve passes, which are three unrelated concerns sharing one file. Do this before the deferred lighting pass grows the gpu3d family further.
- [ ] #365 Deferred renderer: emissive and motion vectors in the deferred frame. Both were left out of #356's G-buffer deliberately — emissive is an additive term no lighting calculation reads back, and motion vectors have no consumer until the deferred frame has a TAA pass. CORRECTION: this item originally claimed 8-bit unorm cannot hold an HDR emitter and framed the choice as packing-vs-extra-pass. That premise is FALSE. A logarithmic encoding — store e, decode exp(e * 6.91) - 1 — gives roughly [0, 1000] of range in one 8-bit channel, and a 2-bit shading-mode field selects whether that channel means ambient occlusion or emissive. No extra pass, no extra target. Superseded by #366, which repacks the G-buffer around exactly this; keep this item only for the motion-vector half.
- [ ] #366 Renderer: repack the deferred G-buffer to the reference layout — A: RGB10A2 (octahedral normal .xy at 10 bits, reserved .z, 2-bit material_mode .w), B: RGBA8 (albedo.rgb, roughness .a), C: RGBA8 (motion .xy, metallic .z, ao-or-emissive .w), plus depth. Ours is currently 20 bytes/pixel (RGBA8 albedo + RGBA16F normal + RGBA8 material + D32) carrying LESS information than the reference's 16 — four channels are unused and there is no motion, AO, emissive or shading-mode field. Octahedral at 10 bits is the enabler, not a later tweak: it is what frees the room. 8-bit oct bands visibly on smooth surfaces, so the 10-bit format matters. material_mode selects lit/emissive/unlit and lets lighting early-out on unlit. Do this BEFORE more passes depend on the layout. DONE: A=rgb10a2 (oct normal + 2-bit mode), B=rgba8 (albedo+roughness), C=rgba8 (motion+metallic+ao/emissive), 20 -> 16 bytes/pixel. Emissive landed with it, log-encoded in C.w and selected by the mode field, which also closes #365's packing half. Targets are named A/B/C because no target holds one thing any more. Motion channels are written as 0.5 (unorm mid = no motion), not 0.0, which would encode a large negative velocity. Verified by the lit image being unchanged across the encode/decode round-trip.
- [ ] #367 Renderer: reverse-Z depth for the deferred frame. Standard-Z with a float depth buffer wastes most of the available precision; reverse-Z is close to free and strictly better. Needs a mat4PerspectiveReverseZ in lib/math3d.rae (NOT a change to mat4Perspective — the forward frame must keep its current convention or every one of its passes shifts), depthCompare Greater, depth clear 0.0, and every consumer's near/far test inverted. Cheap now, expensive once more passes bake in the convention — pair it with #366. DONE: mat4PerspectiveReverseZ in lib/math3d.rae (forward frame untouched), Greater depth test, 0.0 clear, and the depth pyramid's reduction flipped from min to max since nearest is now the maximum.
- [ ] #368 Renderer: reconstruct world position in the deferred lighting pass from an interpolated view ray times linearised depth, instead of the per-pixel inverse-view-projection multiply plus perspective divide we do today. The ray is three interpolated floats from the fullscreen triangle; the current path is a mat4x4 transform and a divide per pixel for the same answer.
- [ ] #369 Renderer: depth pyramid parity — carry MIN and MAX in one RG target rather than min alone, linearise and normalise by far-Z instead of storing raw nonlinear depth, and reduce with textureGather (one instruction per four texels) instead of four textureLoads. Add the separate median-of-four depth target (outlier rejection for SSAO) when SSAO actually lands, not before.
- [ ] #370 Renderer: lit/HDR target to RG11B10_FLOAT instead of RGBA16F, with an explicit clamp to the format's range in the lighting shader. Halves the bandwidth of the largest full-res float target in the frame. Applies to the deferred litColor; consider the forward frame's hdrColor separately. DONE for the deferred litColor, including the device-feature negotiation it needed — the device was previously created with no features at all, so the format was not renderable. Falls back to rgba16float when the adapter lacks it. Radiance is clamped before the write on BOTH paths (the format is unsigned; a negative would become NaN and survive every later blur). Verified numerically: mean |delta| 0.23/255 vs the rgba16float render. The forward frame's hdrColor is untouched and could take the same treatment.
- [ ] #371 Renderer: deferred lighting quality items, in rough dependency order — f16 arithmetic throughout the lighting/pyramid shaders (needs the shader-f16 device feature, which is #357b); the GGX numerator written in the cross-product form (dot(n x h, n x h)), which is numerically stabler at low roughness than the NoH form we inherited from the forward pass; then cascaded shadow maps with PCF, SH diffuse irradiance plus a prefiltered specular cubemap with an env-BRDF approximation, SSAO with depth-aware upsample, and normal mapping with a real TBN. Each is its own increment; none should start before #366/#367 settle the formats they read.
- [ ] #372 Renderer: dedicated depth-aware SSAO blur pass, and consider r16unorm for the AO target. After #366's sector fix (32 sectors + dithered boundary, longest identical-AO run 56px -> 16px) the visibility bitmask is no longer the banding limiter — the 8-bit AO texture and the ABSENCE of a blur are. The reference implementation runs a separate depth-aware blur over its AO before the composite; ours only does a bilateral upsample inside the composite, which smooths across the half-res -> full-res step but does nothing about in-plane noise. Do the blur first: it is the fix that makes the dither pay off, and it may make the wider AO format unnecessary. Banding is worst close-up and at grazing angles, where the AO gradient spans many pixels and marchUv hits its clamp.
- [ ] #373 SSAO close-up banding: root cause is the SCREEN-SPACE FOOTPRINT estimate (marchUv), not normals and not sample count. Two hypotheses were implemented and MEASURED FALSE — do not repeat them. (a) Interpolated-normal-vs-faceted-depth self-occlusion: deriving the tangent plane from depth derivatives instead of the vertex normal changed the close-up artefact by nothing at all (stdev 9.56 vs 9.55 on an identical frame), so the smooth/faceted disagreement is not the driver despite the cube-vs-sphere contrast suggesting it; that code was reverted rather than left in as unearned complexity. (b) Undersampling: raising taps from 3x6 to 4x8 made it slightly WORSE (10.35 vs 9.56), which is the opposite of what too-few-samples predicts. What DOES track it is march length: radius 1.2 -> 0.4 -> 0.15 drops stdev 9.56 -> 1.94 -> 0.92. Suspect `marchUv = radius / worldPerUv` clamped to [1.5/W, 0.08]: worldPerUv is estimated from a single horizontal offset at the CENTRE pixel's depth, which is wrong on sloped surfaces and varies rapidly across a curved surface seen close up, so the sampling footprint swings across the object and the clamp adds a hard view-dependent cutoff. Next: measure worldPerUv directly across a close-up torus, then estimate the footprint from actual neighbour depths (or in view space) rather than one offset, and revisit the 0.08 clamp. The angle bias and the depth-aware blur (#372) stay useful regardless but neither addresses this.
- [ ] #374 Renderer: SKINNED VERTEX FORMAT and a second GPU pipeline. Prerequisite for everything else in the skinning arc. Add a skinned mesh type carrying pos3/nrm3/uv2 plus JOINTS_0 and WEIGHTS_0 (16 floats/vertex), a gpu3d upload path for it, and a skinned render pipeline whose vertex shader transforms by a joint palette. Deliberately a SEPARATE path rather than widening MeshData: the existing stride-32 layout is hardcoded in mesh3d, gpu3d's vertex layout and the WGSL, and widening it would ripple through every mesh generator and every 3D example while making static geometry pay 32 bytes/vertex it never reads. Joints arrive as floats in the vertex buffer and are cast to u32 in the shader, since Rae has no integer vertex attribute path yet.
- [ ] #375 Renderer: SKIN PARSING and the Mat3x4 joint palette — #356's actual acceptance item, and where Mat3x4 earns the place #354 made for it. Parse skins[] (joints, inverseBindMatrices), the node hierarchy and each node's TRS; compute joint world matrices by walking the hierarchy; palette[j] = jointWorld[j] * inverseBind[j], packed as Mat3x4 (affine transforms need no fourth row — 25% less palette traffic). Verify against BIND POSE first: with no animation applied the palette must be identity-equivalent and the skinned mesh must render pixel-identical to the unskinned one. That check is what separates a palette bug from an animation bug later.
- [ ] #376 Renderer: ANIMATION SAMPLING. Parse animations[] samplers (input=time accessor, output=values) and channels (target node + path), sample at a wall-clock time into node TRS, and drive the #375 palette from it. LINEAR and STEP interpolation only — that is all the walker clips use; CUBICSPLINE can wait for an asset that needs it. Rotations interpolate with Quat slerp (lib/quat.rae), not component-wise lerp, which is the whole reason slerp exists. RETARGET BY JOINT NAME, not index: the walker's clips are separate .glb files whose joint ORDER differs from the character's, so an index-based mapping silently produces a scrambled pose rather than an error.
- [ ] #377 Example 113: animation UI — clip selection (Idle / Walk / Walk fast / Run), play-pause, and a speed control, with a clip playing by DEFAULT so the example demonstrates itself without interaction. Built on lib/ui like example 110's overlay. Convert the remaining source clips with examples/114_walker_character/tools/fbx_to_glb.py; they are ~600 KB each, so commit only the ones the example actually uses.
- [ ] #378 Renderer: per-vertex colour baked from a palette atlas, closing the fidelity gap example 113 documents. Nine of that model's twelve primitives sample a single swatch and are exact today; three are flattened (body 61% skin across 17 swatches, trim 51%, boots 86%). Options: bake per-vertex colour by sampling the atlas at each vertex UV (needs a colour channel in the vertex format — fold into #374 if it lands first, since the atlas is FLAT and per-vertex is then pixel-identical to texturing), or real texture sampling in gpu3d, which is the general answer and a larger job.
- [ ] #379 Reusable 3D app shell, part 1: extract example 110's CameraController into lib/app3d/camera_rig.rae — orbit + WASD/free modes, pointer-drag and key input, wheel zoom, symmetric pitch clamp, apply-to-Camera3d, moved-detection and serialize/restore. Every 3D example currently re-implements a subset of this by hand (112 and 113 each have their own yaw/pitch/dist block). Use it in 110 and 113; 110's example gate must still pass unchanged, which is what proves the extraction was behaviour-preserving.
- [ ] #380 Reusable 3D app shell, part 2: extract the UI bootstrap into lib/app3d/ui_shell.rae — logical window extent (dpr-corrected), design-resolution sync, font + theme + UiWorld creation from an assets directory, scene load, resize re-anchor, input update, and the node helpers 110 keeps locally (nodeEntity / setNodeText / setNodeActive) plus action-event iteration. These are copied verbatim into every example that uses lib/ui today.
- [ ] #381 Reusable 3D app shell, part 3: lib/app3d/control_panel.rae — a side/bottom control panel an example fills with CUSTOM buttons, built on lib/ui's Layout (vertical/horizontal, gap, Align) so it lays itself out rather than needing per-example absolute coordinates and an anchorUiToExtent function. Buttons mount a shared panel-button prefab via SceneRegistry/mountSceneInstance with SceneOverride for the label; ControlPanel tracks id -> actionId -> label/selected and syncs into the world on change. Then give example 113 a REAL ui.raescene built on it, replacing the immediate-mode overlay #377 shipped.
- [ ] #383 Shadows M2 — AFTER #393 (deferred migration). Blocked on the forward path because a screen-space march must SAMPLE the depth buffer, and the forward pass is writing that same buffer in the pass where lighting happens, so there is nothing to read. Example 111 (deferred) could take it today; 110/113 cannot. Screen-space contact shadows (design doc §3 Layer B): short ray march over the existing depth pyramid toward the sun, ~8-16 steps over ~0.5 m, run ONLY where the cascade says the pixel is lit, combined with min() not multiplication. Fixes the bias gap that detaches a contact shadow from its object. Gate: the contact shadow touches at every camera angle, including grazing.
- [ ] #385 Shadows M4 — tier knobs (design doc §6): cascade count/resolution, Depth16Unorm on mobile (halves the bandwidth that dominates on tiled GPUs), sample counts, contact-shadow steps, half-res mask on low. Gate: measured frame-cost ratio between low and high is at least 5x, and the graph still never contains the token "shadow".
- [ ] #386 Shadows M5 — SDF cone-traced shadows (design doc §3 Layer C), after #339 gives an SDF representation: far-field shadows beyond the last cascade, shadows for metaball/analytic SDF geometry that has no triangles to rasterise, and the LOW TIER'S ENTIRE SHADOW IMPLEMENTATION — soft and stable at low cascade resolution beats a crunchy 1024 shadow map, and costs nothing extra because GI pays for the representation anyway. Cone tightness derives from the same 0.53 degrees as #384 so the two layers agree on softness.
- [ ] #394 DEFERRED MIGRATION — move the remaining 3D examples (109, 112, and any others on createRenderer3d/WithUi) onto the deferred frame.
- [ ] #401 SKY M2 — SH9 irradiance from the procedural sky, replacing the authored ambSky/ambGround blend in the deferred lighting pass. Diffuse irradiance is very low frequency, so 9 coefficients reconstruct it to a few percent for 9 vec3 of uniform and no sampler. Watch for ambient DOUBLE-COUNTING: the authored constants must stop contributing, not add.
- [ ] #402 SKY M3 — .hdr (RGBE) decode in pure Rae, HDRI background + SH irradiance. Poly Haven "Pure Sky" variants, CC0 (licence recorded in an ATTRIBUTION.md before commit — this repo has already rewritten history once over an unclear asset licence). .hdr not EXR: a much smaller spec for no benefit at sky dynamic range.
- [ ] #403 SKY M4 — sun extraction from the HDRI (brightest region -> direction + colour) driving the shadow cascades, so the visible sun and the cast shadows cannot disagree. Also a yaw control, so an HDRI is rotatable without re-exporting it.
- [ ] #404 SKY M5 — STYLISED sky for example 113, built as composited LAYERS (gradient + parallaxed painted/threshold clouds + sun disc + horizon silhouettes), NOT an HDRI. A photographic environment behind a cel scene reads as 3D models pasted onto a photograph. Layers buy time-of-day, parallax and a palette that agrees with the toon shadow tint. Stylised irradiance comes from the gradient's anchor colours deliberately rather than being integrated.
- [ ] #405 SKY M6 — prefiltered specular environment cube + split-sum BRDF LUT, after #401. Deliberately last: diffuse is the larger and far cheaper win, and a roughness-mipped cube before diffuse works is optimising the second-order term first.
- [ ] #412 Timestamp/Duration ergonomics: calendar breakdown and formatting for Timestamp (formatTimestamp already exists for epoch Int ms), and any further Duration units beyond seconds/millis. The types can gain fields for this without a language change.
- [ ] #413 Finish the Timestamp/Duration adoption: remaining 106 time fields, then flip uiNowSeconds()/gpu2d.nowSeconds() to Timestamp and rewrite the 34 dependent arithmetic sites into lib/time calls. Do the fields first — they are independent — and the clock flip as one reviewable sweep. #410 would make it catch more.
- [ ] #416 GPU UNIFORM LAYOUT AS A RAE TYPE (under #295, docs/webgpu-resource-management-in-rae.md). Today the CPU packing (u[56]=skyKind, u[57]=turbidity, ...) and the WGSL `struct LightU` are two hand-maintained lists that must agree, checked by nobody — LightU has to declare `viewProj` purely so u[56] lands right. Make the layout ONE Rae type that emits its own WGSL struct declaration, so the two cannot drift. This would have prevented three separate bugs in one session: sky args silently dropped when the callee kept its old arity, an 8-vs-9 vec4 array size, and a channel stride that read past the uniform. Prove it on one small pass before touching the deferred lighting path, which is the most load-bearing shader in the tree. rae_ext_gbuffer_lighting is the motivating case: 71 lines, of which 4 are raw wgpu* calls and 40 are uniform packing — the doc's rule is 'raw API calls stay in C, decisions stay in Rae'.
- [ ] #417 FLAKY TEST 432_mobile_ui_leak_regression: it compares RSS growth over 5000 iterations against a flat 3 MB threshold, and lands close enough to it to fail intermittently — measured 2 passes and 1 failure in three consecutive clean runs, failing values 3872 KB and 4544 KB against passes under 3000 KB. It imports only core/string/sys, so this is allocator variance rather than a real regression, but a leak test that cries wolf one run in three trains everyone to ignore it. Options: measure the PLATEAU (growth between iteration 2500 and 5000, which is flat for allocator slack and linear for a real leak) instead of total growth from baseline, or take the median of a few samples. Prefer the plateau — it distinguishes the two shapes rather than just widening the tolerance.
- [ ] #418 SKY FOR THE FORWARD RENDERER, so 111_metaballs_forward and 112_metaballs_deferred differ only in lighting path. Today the sky is deferred-only: the ~111 lines of sky WGSL (preetham/stylised/hosekChannel/skyHosek/skyColor) live inside the deferred LIGHTING pass and read seven fields off its LightU uniform — sunDir, sunColor, clearColor, skyParams, skyZenith, skyHorizon and hosek[9]. The forward path has no fullscreen pass at all, so this is a NEW pass, not a wiring change: extract that shader into a string both runtimes include, add a forward sky pass (pipeline + its own uniform + a fullscreen draw before the scene), and give Renderer3d a sky field with a tag the app dispatches, mirroring DeferredRenderer. THE RISK IS THE UNIFORM PACKING, which is #416's territory: LightU already carries viewProj purely so the sky fields land at the right offsets, and getting it wrong is silent — a sky coloured by whatever was in the matrix. Verify by byte-comparing a deferred screenshot before and after the extraction, so a shared shader cannot quietly change the path that already works.
- [ ] #434 114's animTime accumulates unbounded in f32 and never wraps; after a long suspend (now integrated in full, since the dt clamp is gone) its ULP can exceed a frame's increment and the animation freezes
- [ ] #436 Offer browser WASM for the 8 gpu2d/gpu3d examples that lack wasmWebApp (101-104, 106, 110, 113, 114): each needs the flag plus display dims in a pack, and each needs confirming that it actually renders in Chrome first
- [ ] #438 SUMU 3D/2D preview strategy: WKWebView (Tauri on macOS) exposes no WebGPU — measured on macOS 15.6.1 / WebKit 18.6, navigator.gpu is undefined and WKPreferences._features has no WebGPU flag to enable — so embedded WASM cannot render inside SUMU's window. Devtools already falls back to a managed Chrome window; decide whether that is the answer, or a Chromium shell (Electron/CEF) for the preview pane, or streaming frames from a natively-run Compiled example
- [ ] #439 Decide whether log() should accept only String, with braced interpolation as the sole way to print a value (owner's stated preference). Blast radius measured: 189 non-string call sites — 7 in lib, 39 in examples, 143 in compiler/tests/cases — so it needs a sweep plus expected.txt updates, not just a sema change
- [ ] #464 Member access directly on a reference-returning call emits `.field` where the call lowers to a C POINTER, so it needs `->`: `tracks.viewAt(index: i).seconds` fails to compile ("member reference type is a pointer; did you mean ->"). Binding first works (`let t: view Track => tracks.viewAt(index: i); t.seconds`), and returning the call works (#620), but the direct chain does not. Fix the AST_EXPR_MEMBER emitter to dereference when the object is a view/mod-returning call. Found writing examples/09_playlist
- [ ] #465 FINDING (bounds-design investigation): `List.set` silently IGNORES an out-of-range index (lib/core.rae) — not an error, not an opt, the write just vanishes. Worst of all options; align it with whatever at/viewAt bounds policy is decided
- [ ] #467 FINDING (bounds-design investigation): List.grow() reallocs (rae_ext_rae_buf_resize -> realloc), so ANY outstanding view/mod element reference (viewAt/modAt/componentView results) dangles after add() forces growth. No spec rule, no check, no doc states this; the session's own README example is safe only by accident of ordering. Needs a stated temporal rule (mutating the container invalidates outstanding element views) + ideally a debug-mode revision check, alongside the existing RAE_DEBUG_BOUNDS pattern
- [ ] #468 Complete the List accessor naming cleanup: migrate remaining `get`/`viewGet`/`modGet` compatibility callers to explicit `at`/`viewAt`/`modAt` handling, then remove the aliases once the repository and downstream examples no longer depend on them
- [ ] #469 Make explicit handling of List optionals context-complete in sema: reject implicit unboxing of `at`/`viewAt`/`modAt` (and compatibility aliases) in every expression context, including generic `TYPE_ANY` lowering, so `if let` or an explicitly optional destination is always required [Investigated 2026-08-21: not a quick ensure_type_match check. For `List(Int)`, `.at()` UFCS-lowers to a free CALL whose return SPECIALIZES `opt T` down to plain `Int` — the optional is fully erased before ensure_type_match sees it, and the lowered call reaches ensure_type_match with decl_link=NULL so sema_is_list_index_optional can't recognize it either. The existing branch-5 check only fires when the accessor's result stays TYPE_OPT (never for a concrete generic element). Fix belongs EARLIER — in how the generic accessor's `opt T` return is specialized (keep it optional / box as opt-any), not in ensure_type_match. Larger change; needs its own pass. if-let and `opt` destinations already work.]
- [ ] #470 RAYLIB RETIREMENT M0 — establish the boundary and archive convention before porting examples: active visual examples render through `gpu2d` or Rae's 3D renderer, `sdl3` is only platform/window/input where needed, and no active example may `open/import raylib`; move retired sources intact under `examples/legacy/raylib/`, exclude that tree from Devtools/default smoke tests, add a gate rejecting new active Raylib imports, and document where archived examples live and that they are historical/non-gating.
- [ ] #473 Example 91_pong_implicit — migrate the canonical Pong game to `gpu2d` + SDL3-backed input, preserving gameplay but replacing immediate Raylib text/shapes with GPU2D and an ECS `.raescene` score/game-over overlay; keep it as the featured Pong, remove its unnecessary `.raepack` unless the example still demonstrates package behavior, and add a deterministic headless gameplay/render gate.
- [ ] #476 Example 94_tetris2d — migrate to `gpu2d`/SDL3 while preserving board rules, controls, scoring, pause and game-over behavior; render board/pieces through GPU2D, move menus/HUD into an ECS `.raescene`, use logical design coordinates and resize-safe input, retain its featured status, and add deterministic state tests plus a non-blank headless screenshot gate.
- [ ] #477 Example 95_easing_2d — migrate to GPU2D/SDL3 because the easing visualization remains a useful focused library example; replace Raylib gradients/circles/text with `gpu2d` + GPU text, preserve the easeInOutQuad timing demonstration, and add a fixed-time screenshot mode so the gate is deterministic.
- [ ] #478 Example 96_easing_3d — migrate to Rae's deferred 3D renderer rather than SDL drawing: keep it as the smallest animation/easing example for a generated sphere and ground, use SDL3-backed app input/window through the renderer, render its label with the GPU2D/ECS UI overlay, and gate a fixed-time bounce frame; do not carry over fake Raylib highlight/shadow geometry when PBR/shadows already provide those effects.
- [ ] #479 Example 97_tetris3d — migrate the complete game to `renderer_deferred` + SDL3-backed input and GPU2D ECS UI: preserve its multi-file gameplay/particles/camera design, replace Raylib `Vector3`/`Color` with Rae math/material types, batch cubes as shared-mesh instances, move HUD/next-piece/pause/game-over into `.raescene`, use the reusable app3d camera/UI shell where appropriate, and add deterministic simulation plus headless renderer gates; stage this after 94 so game-rule/UI migration lessons are reused.
- [ ] #481 Example 100_font_example — ARCHIVE the Raylib bitmap-vs-legacy-MSDF comparison after verifying example 103_gpu2d_text covers every still-relevant size, colour, halo, shadow and outline regression; move source/assets to `examples/legacy/raylib/100_font_example`, keep only missing renderer regression cases in 103, and retire `lib/ui/legacy_raylib/msdf` if no active caller remains.
- [ ] #482 RAYLIB RETIREMENT M1 — after #471-#481, update README featured screenshots, Devtools categories, example counts, docs and smoke-test manifests to reference the migrated examples; ensure archived Raylib examples are visible only through an explicit Legacy/Archived view and are never presented as recommended Rae architecture.
- [ ] #483 RAYLIB RETIREMENT M2 — once the active-import gate is green, make Raylib a legacy-only optional compatibility dependency or remove it entirely if no compiler test requires it; retire/supersede #433 rather than debugging Raylib initialization, stop linking Raylib in normal compiler/example builds, and verify the compiler, SDL3 software examples, GPU2D/UI, GPU3D, WASM and Devtools from a clean machine setup.
- [ ] #484 GRASS EPIC (GPU-driven vegetation) — master task. Investigation findings that scope every phase below: the deferred renderer (shared by 110-114) has compute + storage buffers, BUT (a) the only compute helper (runtime_webgpu.c) makes its own encoder, submits, and BLOCKS on a mapAsync readback — not frame-integrated; (b) NO instanced draw anywhere (every DrawIndexed uses instanceCount=1, per-draw data via a dynamic-offset UNIFORM bind group); (c) NO indirect draw (wgpu-native headers expose WGPUBufferUsage_Indirect, core DrawIndexedIndirect, and MultiDrawIndexedIndirect behind a native feature flag not currently requested at device creation); (d) the GBuffer mode field is 2 bits and all four values (LIT/EMISSIVE/UNLIT/TOON) are used, so SSAO exclusion needs an encoding change touching SSAO+lighting decode; (e) TAA velocity exists. Deliver in the phases #485-#495, each buildable/verifiable against 110-114 before the next. Adding Rae externs/APIs (and, if genuinely needed, small language support) for storage-buffer instance data / compute dispatch is expected and fine. Keep walker_grass.rae transform-sway as the stopgap until #495. RE-SEQUENCED per #506: all grass phases are built in Rae over the WebGPU bindings (lib/webgpu/*.rae, lib/gpu*.rae, lib/gbuffer*.rae) — NO new renderer-specific rae_ext_(gbuffer|gpu3d)_* C helper; instance/compute/indirect resources are created + encoded from Rae. The #505 gate (make c-surface-gate) enforces this. WGSL shader source may still live as C string literals returned via a rae_gb_* accessor, or as Rae string constants.
- [ ] #488 GRASS phase 4 — GPU CULLING + DIAGNOSTICS. GPU cull DONE with #487: the generation compute frustum(cone)+distance-culls candidates and writes the indirect args, so culled instances cost no vertex/fragment work. REMAINING: the diagnostics overlay (chunk/generated/visible/culled/shadow counts + timings through the existing log/debug overlay — needs reading the compute-written visible count back, e.g. a tiny copy of the indirect instanceCount into a readable buffer surfaced in the debug overlay). Frustum + distance cull on the GPU, writing the indirect args from #487 so culled instances cost no vertex/fragment work. Add diagnostics through the existing log/debug overlay: chunk count, generated / visible / culled / shadow instance counts, and GPU/CPU timings where the existing profiling permits — do NOT add a separate profiling framework.
- [ ] #489 GRASS phase 5a — GRASS SHADOW pass. A dedicated reduced-density grass shadow path: near grass only, distance-based participation, substantially fewer instances and/or simpler geometry than the main pass; no far-distance grass shadows. Add a grass-shadow render-graph pass if that is cleanest.
- [ ] #490 GRASS phase 5b — SSAO EXCLUSION via GBuffer classification. Extend the GBuffer encoding so grass is classified as not contributing thousands of tiny blades to SSAO while the ground still receives SSAO. BLAST RADIUS: the 2-bit mode field is full, so this changes the GBuffer encoding read by the SSAO pass AND the lighting decode — update every consumer and verify 110-114. Pick the cleanest encoding (extra bit / repurpose / separate mask) after re-reading the SSAO + lighting shaders.
- [ ] #491 GRASS phase 5c — TAA integration for animated grass. The grass vertex shader must compute its PREVIOUS-frame wind position and write correct motion vectors into the existing velocity buffer, so wind motion, density changes and alpha-tested thin edges do not ghost/flicker. Do not disable TAA. Verify against the documented velocity bias encoding.
- [ ] #492 GRASS phase 6 — BIOME ECS + CAMERA/VIEW PROFILES + full config. Represent vegetation/biome REGIONS with the existing ECS (regions/params only, never a per-blade entity): density, min/max blade height, width, colour variation, wind response, allowed slope (from terrain normal), fade distances, seed, and room for multiple procedural 'species' (no loaded meshes). ADD GrassViewSettings so the SAME GPU system is reparameterised per camera/view profile rather than hard-coding third-person assumptions: fields nearDistance/mediumDistance/farDistance, nearDensity/mediumDensity/farDensity, shadowDistance, windStrength (+ the rest of the config surface: enabled, chunkSize, renderDistance, maxInstances, bladeHeightRange, bladeWidth, windDirection, windScale/gust, shadowDensity, slopeLimits). Ship presets: THIRD-PERSON (high close density, blades close to character, more per-blade detail) and TOP-DOWN/ISOMETRIC (lower blade detail, much larger visible area, aggressive distance density reduction, stronger ground-level clumping/colour). Follow existing Rae config/component conventions.
- [ ] #493 GRASS tests — deterministic CPU-side logic: placement hash/noise, chunk<->world-cell mapping and camera-relative wrap stability, LOD/density selection, and the GrassViewSettings density/fade curves per profile. Add headless renderer gates (screenshot/among the existing 3D gates). Reuse existing test infra; no new framework.
- [ ] #494 GRASS cleanup — once #485-#492 cover the walker meadow, remove the transform-sway stopgap (examples/114_walker_character/walker_grass.rae) and repoint 114 at the GPU grass system; keep the meshUpdate-based terrain hills (that path is unrelated and still correct).
- [ ] #496 WEBGPU BINDINGS EPIC (master) — CORRECTED ARCHITECTURE: the renderer must be Rae over LOW-LEVEL Rae WebGPU bindings (webgpu.rae) → WebGPU C API → wgpu-native → GPU, NOT a growing set of renderer-specific C helpers. Prior "the whole WebGPU API is too much glue" assumption WITHDRAWN. Grounding from investigation: (a) authoritative source present locally — ~/.local/wgpu-native/include/webgpu/webgpu.h (6766 lines, standard) + wgpu.h (1551, wgpu-native extensions); (b) the ENTIRE runtime uses only ~64 distinct wgpu* functions, so full low-level bindings are tractable, not a mega-effort; (c) List(T) is emitted as { T* data; i64 length; i64 cap } and already crosses the FFI ZERO-COPY today (drawRecords receives field.records.data as const float* — no copy), so the pieces exist; (d) globals g_wgpu_device/queue/adapter/instance/surface already hold the device — object construction (buffers/textures/pipelines/bind groups/encoders/passes) can move to Rae; (e) WGSL shader source is C string literals (stays near C or becomes Rae string constants + createShaderModule). Target layering: webgpu.rae (raw generated bindings) → gpu.rae (Rae convenience) → render_graph.rae / gbuffer.rae / grass.rae. Deliver in #497-#506; do this BEFORE further renderer feature work (grass #486+); preserve behavior; run full suite each phase.
- [ ] #500 WEBGPU phase 2b — IMPLEMENT the chosen List↔FFI span/view feature from #499 as general-purpose language/runtime (not graphics-specific): construct a (ptr,len) span/view over a List's storage with no copy, correct lifetime/invalidation semantics, read-only vs mutable forms, tests pinning zero-copy + the realloc-invalidation rule. Enables uploadBuffer(device, data: <span/view>) with a pointer+byte-length and no intermediate C array.
- [x] #513 WEBGPU cleanup — remove dead deferred-renderer C superseded by the Rae migration: rae_ext_gbuffer_debugView (→ gbuffer_inspector.rae debugView), rae_ext_gbuffer_ssao (→ gbuffer_passes.rae ssaoPass), rae_ext_gbuffer_present (→ Rae composite/present). Zero live bindings today. Confirm no bare-extern binding, delete defs+stubs+prototypes, drop the 3 allowlist lines, rebuild. Scoped cleanup, not a drive-by.
- [ ] #514 WEBGPU phase 9 — migrate or retire the FORWARD renderer (Track A): runtime_gpu3d.c (~1221 LOC) + runtime_gpu3d_ssao.c + runtime_gpu3d_sky.c still C-encode a whole second renderer (rae_ext_gpu3d_{begin,draw,drawMetaballs,drawSkinned,end,submit,tonemap,taa,ssao,skyDraw,skyHosekPush}) driving forward examples 109/111. Either port its passes to Rae over the bindings (like the deferred renderer) or retire it in favour of the deferred path and migrate 109/111. Last real block of high-level graphics C; out of #502–#504 scope. Not required before grass.
- [ ] #515 GRASS/TERRAIN — DESIGN TASK (design only, do NOT implement): design a top-down-capable vegetation + terrain rendering system. PROBLEM: the #487 procedural blades (thin vertical quads) look great in 3rd person but break in top-down/oblique views — seen from above a vertical quad projects to a sliver ("magnetic-field lines") and the bare terrain shows through the gaps, because ALL the grass look comes from vertical geometry which has ~no top-facing area, and density is tuned for sideways face-overlap not for hiding the ground from above. GOAL: grass/terrain that reads well across the full camera range (3rd person → top-down), ideally staying all-3D so a future 2.5D top-down mode is just a constrained camera, not a separate 2D path. Produce a design doc (docs/top-down-vegetation.md) that: (a) states the core insight — decouple "grass look" from vertical blades, the ground must carry coverage; (b) evaluates and ranks the candidate techniques for top-down fit, cost, and how they layer with our GPU-compute + DrawIndirect + deferred-G-buffer stack: [1] TEXTURE THE TERRAIN (grass/dirt/sand/snow splat or triplanar albedo+normal, detail normals, distance tiling break-up, optional parallax-occlusion mapping for apparent depth, optional deformation/footprint render-targets) as the robust base that reads as grass from any angle with 3D blades demoted to an additive parallax/motion detail layer; [2] SHELL TEXTURING (N stacked offset semi-transparent terrain layers sampling a grass-density/alpha noise — the elegant ground-hugging volumetric carpet that stacks solid top-down and reads as height side-on; note the overdraw cost and the SHELLS+FINS hybrid, fins = vertical cards only at silhouettes, for grazing angles); [3] GROUND-COVER LAYER under the blades (flat terrain-hugging clump/disc/decal per base, or a mat of near-horizontal micro-quads) guaranteeing coverage from above while tall blades stay for near-camera detail; [4] VIEW-ADAPTIVE BLADES (widen/curve toward the camera, blend upright-card→flat-fan by view tilt, scale near-camera density with view angle — we already pass the camera into the cull compute); [5] CROSSED-QUAD / CLUMP-MESH blades (X/asterisk of 2–3 quads or a small fanned tuft) for projected area from many directions incl. above; [6] IMPOSTORS/octahedral billboards for distant vegetation (orthogonal to top-down but the standard far-LOD). (c) recommend a concrete layered path (starting point: texture the terrain, then a shells+fins ground layer with the current procedural blades kept as near-camera tall detail, then view-adaptive blending) with how each maps onto our existing stack (shells as instanced layers into the same G-buffer, terrain splat as a richer terrain material, view-adaptive as vertex-shader math, all inheriting the existing cull/LOD); (d) sanity-check against sand/rock/snow (same splat + normal/POM + deformation) so the system generalises beyond grass; (e) note open questions: overdraw budget for shells, alpha-test vs coverage for shell/fin edges, how the deferred G-buffer normal encoding handles thin/volumetric surfaces, TAA behaviour for shell layers, and authoring (density/biome maps) tie-in with #492. DELIVERABLE is the design doc + a recommended phased implementation plan filed as follow-up tasks; no renderer code in this task.
- [ ] #516 iOS TESTING HARNESS — implement per docs/ios-testing.md (design landed). Goal: run Rae featured examples (later self-contained Rae app dirs) on a real iPhone to profile on-device perf; no Rae program has run on iOS yet. Grounding: runtime is one amalgamated TU (rae_runtime.c) recompiled per app, the native link is assembled in one place (gcc_link_c_to_binary, main.c ~2666-2738), and the GPU path is SDL3 Metal-view → wgpu surface (identical on iOS). TWO TRACKS, TWO generated Xcode projects: (A) NATIVE RaeExamples.xcodeproj — 1 shared RaeRuntime static-lib target + 1 app target per featured example (scheme dropdown = the launcher); the perf-representative path (Metal via wgpu-native). (B) RaeWebLauncher.xcodeproj — one WKWebView app hosting the existing emcc WASM builds (real WebGPU via Safari) with a featured-example gallery; breadth + fast iteration, NOT perf-representative. NEW infra needed = 2 xcframeworks built once (SDL3 device+sim; wgpu_native via cargo aarch64-apple-ios + -sim → xcodebuild -create-xcframework). GENERATOR = tools/ios/gen.sh + CMakeLists (cmake -G Xcode) that: scans examples/*/*.raepack for featured:"true", runs `rae build --emit-c` per example (reusing existing codegen), reads .deps for RAE_HAS_SDL3/WEBGPU macros, bundles assets + shared lib runtime data preserving relative paths, emits build/ios/*.xcodeproj (gitignored; tools/ios committed). Asset shim: chdir(SDL_GetBasePath()) at launch (iOS base path = bundle resources) so existing repo-root/{dir} paths resolve with zero ObjC. Parameterise gen by project dir so standalone Rae apps (own .raepack) get their own xcodeproj; long-term fold into `rae build --target ios`. MILESTONES: (1) build the 2 xcframeworks (gating unknown), (2) hand "hello iPhone" with one example (102 then 114) on device to prove clang-for-iOS + static SDL3 + wgpu Metal surface + a WGSL shader, (3) the generator, (4) asset bundling generalised, (5) WebLauncher, (6) Instruments profiling pass on 114. OPEN RISKS (see doc §7): busy render loop vs iOS lifecycle (SDL3 main-callback API vs pause-on-background), SDL_main/SDL_MAIN_HANDLED entry reconciliation, code signing/provisioning, wgpu-native on iOS device (compute/DrawIndirect/storage used by grass), touch→pointer + safe-area insets (ties to #207/#209), iOS Safari WebGPU maturity for track B. Frameworks kept on iOS: Metal QuartzCore CoreFoundation Foundation CoreGraphics ImageIO; dropped: Cocoa OpenGL IOKit CoreVideo (raylib legacy); exclude runtime_spotify_apple.c.
- [ ] #517 CLEANUP — remove deprecated LIVE + HYBRID build targets: Rae ships only two real targets, Compiled (emit C → native) and WASM (emit C → emcc → browser). The `live`/`hybrid` values in the compiler's BUILD_TARGET enum (compiler/src/main.c ~82-85) plus their arg parsing (`--target live|hybrid`, `--debug`/profile plumbing tied to them), default out paths (e.g. build/out.hybrid ~main.c:533), and any live-VM/hybrid dispatch in the build switch (~main.c:3760-3823) are legacy that should not be there. Audit and remove: the enum values, CLI parsing, dispatch arms, help/usage text, and docs/marketing copy that lists Live/Hybrid as modes (note CLAUDE.md/AGENTS.md currently describe "Live/Compiled/Hybrid" official labels — reconcile to Compiled + WASM). Keep the bytecode VM only if it is still used by tooling/tests for a non-target purpose; if a `run` fast-path depends on the VM, decide explicitly whether `run` stays VM-backed or becomes compiled-only. Verify the suite + examples still build after removal. Surfaced while writing docs/ios-testing.md.
- [ ] #518 iOS M1a — build wgpu_native.xcframework for iOS (device arm64 + simulator arm64). ~/.local/wgpu-native is macOS-only (.a/.dylib). Either fetch gfx-rs/wgpu-native prebuilt iOS release libs matching the local version, or rustup target add aarch64-apple-ios aarch64-apple-ios-sim + cargo build the wgpu-native source, then xcodebuild -create-xcframework. Script: tools/ios/build-wgpu-ios.sh → tools/ios/frameworks/wgpu_native.xcframework. Gating dep #520+.
- [ ] #519 iOS M1b — build SDL3.xcframework for iOS (device + simulator). Homebrew SDL3 3.4.10 is macOS-only; build the matching SDL tag from source via SDL's create-xcframework/Xcode project. Script: tools/ios/build-sdl3-ios.sh → tools/ios/frameworks/SDL3.xcframework. Gating dep #520+.
- [ ] #520 iOS M2 — hand "hello iPhone": one example (102_gpu2d_animated first, then 114) as a single iOS app target linking the two xcframeworks + rae_runtime.c + emitted app.c, on device. Proves clang-for-iOS + static SDL3 + wgpu Metal surface + a WGSL shader on real hardware. Resolve SDL_main/SDL_MAIN_HANDLED entry, chdir(SDL_GetBasePath()) asset shim, and the busy-loop-vs-iOS-lifecycle question (SDL_AppInit/Iterate callbacks vs pause-on-background). Requires the user for code signing / provisioning in Xcode.
- [ ] #521 iOS M3 — the generator: tools/ios/gen.sh + CMakeLists.txt (cmake -G Xcode) producing build/ios/RaeExamples.xcodeproj with 1 shared RaeRuntime static-lib target + 1 app target per featured example (scans examples/*/*.raepack featured:"true", runs rae build --emit-c, reads .deps for RAE_HAS_SDL3/WEBGPU). Scheme dropdown = the launcher. Parameterise by project dir for standalone Rae apps.
- [ ] #522 iOS M4 — asset bundling generalised across examples (bundle assets/ + shared lib data preserving relative paths; chdir(SDL_GetBasePath()) at launch) + touch→pointer input mapping + safe-area insets (ties to #207/#209).
- [ ] #523 iOS M5 — RaeWebLauncher.xcodeproj: a WKWebView app hosting the existing emcc WASM builds of the featured examples with a gallery picker (breadth/iteration; not perf-representative).
- [ ] #524 iOS M6 — Instruments profiling pass on 114 (deferred + grass) at varying density/LOD on device; feed results into the renderer perf work. See docs/ios-testing.md.
- [ ] #525 iOS UI/portrait polish (after #520 device runs): examples render but assume desktop landscape. DONE: (a) portrait orientation locked in gen-ios.sh (UISupportedInterfaceOrientations + generated launch screen for native-res); the gpu2d design resolution + gpu3d camera aspect already follow the real drawable, so 114 fills the portrait screen. (b) 114 settings panel starts collapsed and toggles from a small hamburger button top-left (walker_hud.rae), clear of the debug D. Plus a shared touch joystick (lib/ui/virtual_joystick.rae) driving movement, and clones 20→5. REMAINING: the settings panel itself, when opened, is still desktop-styled/wide — reflow/stack it for the tall iPhone aspect, larger touch targets, and respect safe-area insets (notch/home indicator) for the joystick + menu + panel; consider a device/portrait profile examples can query. Also applies to 102 (landscape aspect fix — same orientation lock once run through gen-ios). Ties to #207/#209 and touch #522. (a) Aspect/orientation: honor the device's real drawable size + orientation (portrait on iPhone) instead of a fixed landscape aspect — both the gpu2d design-resolution/viewport (setDesignResolution/Viewport) and the gpu3d camera aspect; decide portrait-locked vs responsive. (b) 114: do NOT open the settings menu at startup on a phone (start collapsed); the panel currently opens on launch. (c) Make 114's UI (control panel, camera bar, log overlay, settings) suitable for a tall narrow iPhone portrait aspect — reflow/stack, larger touch targets, respect safe-area insets (notch/home indicator). Ties into the existing lib/ui coordinate/responsive work (#207/#209) and touch→pointer mapping (#522). Also surfaces on 102 (landscape aspect). Consider a device/portrait profile the examples can query.
- [ ] #526 MULTITOUCH — joystick + camera at the same time (iOS). Today gpu2d exposes one pointer (SDL mouse, emulated from the primary touch), so the virtual joystick and the camera drag fight over it. Add real multitouch: (1) runtime — in runtime_gpu2d_platform.c pollClose, track SDL_EVENT_FINGER_DOWN/MOTION/UP into a small finger array (SDL_FingerID + normalized x/y), and expose rae_ext_gpu2d_touchCount / touchX(i) / touchY(i) converting finger-normalized coords to DESIGN units with the same xform as rae_g2d_pointer_design (px = norm*g_sdl_w/h then (phys-offset)/scale); optionally touchId(i). (2) gpu2d.rae externs. (3) route: the joystick claims a finger in its activation zone (track by finger id across frames), the camera uses a finger NOT in the joystick zone — likely a new updateCameraRig variant taking explicit pointerDown/x/y (keep the old one delegating to gpu2d.pointer so 110/113 are unchanged). (4) DESKTOP fallback: touchCount==0 → use the mouse pointer as today, so both paths work. Verify on device that one thumb drives the stick while another rotates the camera, and that desktop mouse still works. Also add a small lib/ui touch helper (touchInRegion) reusable by other touch UIs.
- [ ] #527 PROFILER — iOS capture trigger: passing RAE_PROFILE env to a side-loaded app isn't simple. Add a launch-arg or on-device trigger (e.g. run-ios.sh passes env/args via `xcrun devicectl device process launch --environment-variables`, or a debug-UI button that calls profile.profileStartCapture). Trace lands in Documents/ (cwd) and is pulled like rae.log. See docs/profiling.md.
- [ ] #528 PROFILER — GPU timestamp queries per pass: add a second (GPU) track to the Chrome-trace capture with real per-pass GPU milliseconds via WebGPU timestamp queries (wgpuCommandEncoderWriteTimestamp / wgpuComputePassEncoderWriteTimestamp already bound). Write timestamps at pass boundaries into a QuerySet, resolve to a buffer, map back async (read next frame), emit as tid=1 events in lib/profile.rae. Stays Rae-over-WebGPU — NO new renderer C surface. This is the definitive view for the 114 sunset fps drop and the real payload for the Instruments pass (#524). See docs/profiling.md.
- [ ] #529 PROFILER #528 blocker — GPU timestamps read back 0 on wgpu-native Metal: query set + pass-descriptor timestampWrites are accepted (no validation error, feature present) but resolve→copy→map returns 0/0. Readback pattern matches the proven rae_gpu_readback, so the query set is not being written. Run down whether wgpu-native's Metal backend supports render/compute-pass timestampWrites in v29 (macOS version? GPU family? needs draws?), or drop to MTLCounterSampleBuffer directly. Harness: examples/zz_gpu_timing_check. Until resolved, DRS (#530) uses CPU present-time as the GPU-bound proxy.
- [ ] #530 DYNAMIC RESOLUTION — render gbuffer+lighting at a fraction of native, upscale at present; control loop off smoothed GPU/present time with a target budget BELOW the frame wall (~10-12ms of 16.7) for thermal headroom, scale 0.5-1.0/axis, ease up slow / panic-drop fast, UI full-res. Add iOS ProcessInfo.thermalState tier ladder + a frame cap. See modern-engine research (Unreal DRS/TAAU, MetalFX, Fortnite power talk). Driven by #528 once real GPU ms land; can start on present-time.
- [ ] #531 BIOME/TERRAIN EPIC (master) — a spatial world system deciding what the ground is at any (x,y) — water/sea/lake, beach/sand, mud/swamp, grass, rocky hills, greater biomes — driving terrain surface + height, water bodies, and vegetation from ONE dual-ported (CPU+GPU) field, top-down mobile first but all-3D so 3rd person is just a freer camera. Hybrid: procedural noise field + classifier (material WEIGHTS, smoothstep-blended) as the base, plus authored BiomeRegion ECS entities that override it. Full design + rationale in docs/biome-terrain-system.md. Build in phases #532-#538, each buildable/verifiable against 114 before the next. Reuse lib/noise.rae + lib/noise.wgsl (already aligned) for the field; keep everything Rae-over-bindings per the renderer gate. Later folds in terrain textures and greater-biome variety.
- [ ] #532 BIOME phase 1 — shared field + material classifier (dual CPU/GPU). lib/world_biome.rae + lib/world_biome.wgsl (kept aligned like noise): worldElevation (fbm), worldMoisture (decorrelated fbm), worldSlope (elevation gradient), a BiomeConfig (seed, scales, waterLevel, beachBand, rockSlope, swamp thresholds), and classify -> soft material weights {water,sand,mud,grass,rock} + a dominant id + a coarse biome bucket. Deterministic; add #493-style headless tests (water below level, sand in the beach band, rock on steep slope, CPU==GPU intent). NO rendering — the data model every other phase consumes. Elevation units decided here (normalized x amplitude), shared by terrain height + waterLevel.
- [ ] #533 BIOME phase 2 — terrain material splat. The terrain fragment shader samples the classifier and blends the material weights into a ground colour (grass/sand/mud/rock), replacing 114's flat green ground; the terrain height comes from worldElevation (unify with terrainHeightAt). The grass gradient bottom (#492) tracks the LOCAL classified ground colour instead of one global constant. Top-down base coverage per docs/top-down-vegetation.md (a). Splat COLOURS only here; textures are #537.
- [ ] #534 BIOME phase 3 — water bodies. A water surface plane at waterLevel wherever worldElevation is below it; sand beach shoreline falls out of the classifier; a basic water material (colour + fresnel; transparency/reflection/foam later); terrain forms basins under water; gameplay marks water non-walkable (walker can't enter). Verify lakes/sea + beaches read correctly top-down AND in 3rd person.
- [ ] #535 BIOME phase 4 — grass/vegetation biome integration. Per-blade biome/material from the classifier drives grass density/height/species/colour and SUPPRESSES grass on water/sand/rock; extends #492 GrassViewSettings into per-biome vegetation params (meadow grass, swamp reeds, sparse rocky highland). No grass in lakes or on beaches; lushness scales with moisture.
- [ ] #536 BIOME phase 5 — authored biome REGIONS (ECS). A BiomeRegion component (shape: circle centre+radius first; polygons later) with overrides (force biome / stamp lake / raise rocky hill / drop beach) on entities; collected each frame into a small GPU storage buffer the classifier consults (contained/nearest wins) AND queryable on the CPU for gameplay. Hand-place features over the procedural base. Closes #492's biome-region half. No per-blade/per-texel entities — region shapes + params only.
- [ ] #537 BIOME phase 6 — terrain textures & detail. Replace the flat splat colours with tiled albedo+normal per material (triplanar or UV), detail normals, distance tiling break-up, optional parallax/POM for apparent depth (top-down). Layers over the same material weights from #532.
- [ ] #538 BIOME phase 7 (later) — greater biomes & polish. Temperature axis + more biomes/species/palettes (desert, tundra, coastal, highland); coastal foam, swamp fog, terrain deformation (footprint render targets), region polygons + spatial accel for many regions.
- [ ] #539 PROCEDURAL TEXTURE GENERATOR (in code) — generate material textures (albedo + normal + roughness) procedurally from noise for the biome materials (grass, sand, rock, mud, water) and props, uploaded as GPU textures for the terrain splat (#537) and instanced props. Parameterised by resolution, world tiling, per-material detail params, and seed; deterministic (reuse lib/noise). MUST support a detail/quality knob: low-res cheap textures for mobile/low-poly up to high-detail, so the same generator serves LOD tiers. No external image files — all generated in code. Consider generating on GPU (compute) or CPU→upload; decide per cost.
- [ ] #540 PROCEDURAL ROCK GENERATOR (in code) — generate low-poly faceted rock meshes in code, adjustable vertex/triangle count for LOD (low-poly mobile → detailed). Technique: start from a base convex solid (icosphere or subdivided cube at a chosen subdivision = vertex budget), then repeatedly SLICE off corners/edges with cutting planes at controlled-random orientations/depths (clip the convex hull), accumulating angular facets until it reads as a rock — NOT fully random: seeded, bounded slice count + angle/depth ranges so results stay rock-like and plausible. Params: baseSubdivision (LOD), sliceCount (detail/angularity), size, seed, angularity/irregularity. Flat/faceted normals for the low-poly look; output a mesh the instanced prop system (PropInstancer, #485) can draw + shadow-cast. Ties into biome placement (#535) and #493-style deterministic tests.
- [ ] #541 PROCEDURAL TREE GENERATOR (in code) — generate low-poly stylized tree meshes in code, adjustable vertex/triangle count for LOD. A tapered trunk + recursive branches (bounded depth/count, seeded) + a low-poly canopy (faceted blob / icosphere or a small cluster of leaf cards). Params: branchDepth/branchCount (detail), trunkHeight/taper/curve, canopySize/shape, leafDensity, seed, and an explicit vertex/triangle budget so the SAME generator yields cheap distant/mobile trees and detailed near ones (LOD tiers). Deterministic; output meshes for the instanced prop system + shadow casters. Ties into biome vegetation species (#535). Replaces/augments the current authored tree props in 114.
- [ ] #542 DRS viewport-not-realloc (#530 leak fix): allocate deferred G-buffer + shadow targets once at max drawable size and apply render-scale as a viewport/scissor sub-rect instead of destroying+recreating targets; composite already samples the scaled sub-rect (uv*=renderScale). Root cause of the mobile memory growth: each DRS scale/shadow-tier change reallocates targets and wgpu-native does not promptly reclaim the freed textures, so the count ratchets up during movement (13→247 in 2min) and does not drain. Our own Rae/C release logic is correct (verified: live texture count constant).
- [ ] #543 114 REFACTOR (systems architecture) — implement docs/app-systems-architecture.md. Reference: examples/97_tetris3d. Guardrail: headless screenshot parity (RAE_SDL_HEADLESS_MS=1000 RAE_GPU2D_SCREENSHOT=/tmp/walker.bmp) after each step. Umbrella for #544-#555.
- [ ] #544 114: scaffold app.rae — App + Frame types, move all of main()'s setup into createApp(), the loop into runWalkerApp(), update/render into updateApp()/renderApp(); main.rae still calls it. Verify screenshot parity.
- [x] #545 114: extract input.rae — InputState + InputIntent (moveX/moveY/run/actions); inputUpdate fills intent from WASD + virtual joystick. Movement becomes intent-driven (not device-direct).
- [ ] #546 114: extract players.rae — PlayerRoster (player->character mapping + input source), PLAYER_LOCAL; prepares multiplayer + input switching. controlled-character selection lives here.
- [ ] #547 114: extract character.rae — Character + CharacterSystem as a ROSTER of N characters (NOT a hardcoded hero): shared skeleton/clips/mesh parts once, per-character controller/anim/model/owner. characterUpdate applies the controlled character's intent; characterAnimate samples per-character pose/palette; characterRenderGbuffer/Shadow draw the roster. Fold the crowd/clones into the roster.
- [x] #548 114: extract camera.rae — CameraState (rig + camera + bar); cameraUpdate (rig + mode transitions + follow controlled character), cameraApply.
- [x] #549 114: extract ui.rae — UiState (uiWorld, clip/transport panels, settings dialog, scroll, debug + log overlays, hud); uiUpdate (clicks/settings/scroll) + uiRender (overlay + HUD). Fold walker_hud.rae.
- [x] #550 114: extract terrain.rae — TerrainState wrapping InfiniteTerrain; terrainUpdate + terrainRenderGround/Props/Shadow. walker_terrain.rae stays as the impl or folds in.
- [x] #551 114: extract grass.rae — GrassState wrapping GrassField + grass_compute; grassUpdate + grassRender. walker_grass.rae stays as impl or folds in.
- [x] #552 114: extract render.rae — RenderState (DeferredRenderer, sky, light, hosek, DRS, shadow); renderScene owns the render-graph walk and calls each system's render hook per tag.
- [x] #553 114: add physics.rae — PhysicsState + fixed-step accumulator in updateApp (physicsFixedStep) + physicsStep() no-op stub (Jolt/Box2D seam) + JobSystem (jobSubmit/jobAwaitAll, inline now, runtime_threads seam later).
- [x] #554 114: add net.rae — NetState (offline/server/client) + netPublishIntent + netApplyRemote stubs; wire intent publish + remote apply into updateApp as no-ops. Multiplayer seam.
- [x] #555 114: thin main.rae to the entry point; fold/retire walker_*.rae; final screenshot parity; cross-link docs. Confirm no file near the 1000-line cap.
- [~] Deprecate raylib: PARKED (2026-07-01) — decided NOT to remove raylib for now (wasted effort). raylib (98_mobile_ui + raylib examples) and the gpu2d/WebGPU stack (106_mobile_ui + lib/ui/render_gpu2d) coexist side by side; lib/ui keeps its raylib path and gains a gpu2d backend. Revisit only if raylib becomes a real maintenance/deploy burden. Build the Rae Platform/Render/2D/Audio layers as additive, not as replacements.
- [~] Incremental raylib removal — PARKED (see the Deprecate-raylib note): raylib and gpu2d coexist; we're not retiring raylib examples now.

<!-- SUMU_QUEUE_END -->
