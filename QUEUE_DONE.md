# Queue done (archived)

## Archived 2026-08-21 11:28

- [x] #1 Task 1: Emit collection literals (`let x: List(Int) = { 10, 20, 30 }`) — handle AST_EXPR_COLLECTION_LITERAL in c_backend emit_expr by emitting createList + add calls. Fixes 325_list_literal, 337_list_method.
- [x] #2 Task 2: Fix identity/newtype lowering (`let pid: User = 123`) — detect `is id`/`is key` types and emit underlying primitive type. Fixes 341_identity_behavior + smoke 10_memory_basics, identity_fix, store_mapping.
- [x] #3 Task 3: Fix toString() method resolution — `s.active.toString()` emits `rae_toString` undeclared. Emit as type-specific rae_ext_rae_str call. Fixes 350_auto_init (partial).
- [x] #4 Task 4: Fix output format mismatches for ref types — 336 outputs `7` instead of `view 7`, 351 outputs `80` instead of `mod 80`. Decide: update test expectations or add prefix in C log. Also finish 350_auto_init string eq chain.
- [x] #5 CHECKPOINT 1: Re-run all tests after tasks 1-4. Evaluate results. Update remaining tasks.
- [x] #6 Task 5: Fix opt T return type unboxing — `get(T)` returns RaeAny but callers expect rae_String. Add .as.s/.as.i unboxing at call sites when return type is opt T. Fixes 371_string_overhaul + ~10 smoke tests.
- [x] #7 Task 6: Fix createIntMap/createInt64Map specialization chain — inner specialization not discovered. Improve C backend discovery pass transitive following. Fixes 370_map_basic.
- [x] #8 Task 7: Fix rae_any macro for struct types — _Generic has no case for List/custom structs. Add struct handling or avoid boxing. Fixes multiple smoke tests.
- [x] #9 Task 8: Fix view restriction diagnostic for compiled target — test expects compiler errors but compiled target emits nothing. Wire sema view checks for compiled path. Fixes 357_view_restriction.
- [x] #10 CHECKPOINT 2: Re-run all tests after tasks 5-8. Major smoke test re-assessment.
- [x] #11 Task 9: Add raylib C function forward declarations — emit declarations for rae_ext_initWindow etc. when raylib is imported. Fixes ~8 raylib smoke tests.
- [x] #12 Task 10: Add crypto function declarations and C wrappers — rae_ext_rae_crypto_lock/unlock using monocypher. Fixes 19_file_ops, 27_file_locking, 28_crypto_demo.
- [x] #13 Task 11: Fix random(min,max) overload in imported modules — 2-arg random not found in import context. Extend overload search. Fixes 04_random, 92_pong_import, 94_tetris.
- [x] #14 Task 12: Implement defer statement emission — collect defers, emit before returns and at function end. Fixes 15_defer_cleanup.
- [x] #15 CHECKPOINT 3: Re-run all tests after tasks 9-12. Full re-assessment.
- [x] #16 Task 13: Fix external C function linking — compile user .c files alongside generated output. Fixes 20_external_c.
- [x] #17 Task 14: Fix JSON serialization (toJson/fromJson) — implement C-side or mark VM-only. Fixes 17_json_demo, 18_complex_json.
- [x] #18 Task 15: Fix method this parameter passing (value vs pointer) — add & for struct this args in method calls. Fixes 25_timer_demo + others.
- [x] #19 Task 16: Fix float modulo operator — emit fmod(a,b) instead of a%b for doubles. Fixes 93_raylib_3d.
- [x] #20 CHECKPOINT 4: Final evaluation. Plan next phase.
- [x] #21 Fix opt T unboxing — UNBOX emits `.as.s` on `rae_String` when extern returns concrete type not RaeAny. Skip unbox for concrete return types. Fixes 19_file_ops, 21_stdlib_demo, 27_file_locking, 28_crypto_demo, 371_string_overhaul.
- [x] #22 Fix List member `->` vs `.` in interpolation/non-method contexts — `xs->length` should be `xs.length` for value-type List variables. Fixes 06_list_basic, 07_list_loop, 08_list_benchmark.
- [x] #23 Add compound literal cast for c_struct types in function call args — bare `{ .r=20 }` needs `(Color){ .r=20 }` cast. Fixes 93_raylib_3d, 95_easing_2d, 96_easing_3d.
- [x] #24 CHECKPOINT 5: Re-run all tests after #21-#23. Evaluate progress.
- [x] #25 Fix Legacy List2 buf ops type mismatch — old List2 API passes RaeAny where int64_t expected. Fix buf_set/get for non-generic List2. Fixes list_library_any, list_library_int, list_native_any.
- [x] #26 Fix map method T substitution — `m.set(...)` / `m.get(...)` on StringMap/IntMap emit `rae_T` instead of concrete type. Fixes 370_map_basic.
- [x] #27 Fix 337_list_method output — element 20 stored as none. Debug buf_set index in add specialization. Fixes 337_list_method.
- [x] #28 Fix 18_complex_json — add uint8_t to rae_ext_rae_str _Generic macro for raylib Color fields. Fixes 18_complex_json.
- [x] #29 Fix 94_tetris2d — cross-type specialization chain (grow for List(String) inside List(Int) context). Fixes 94_tetris2d.
- [x] #30 Fix generic_test Box(T) — Buffer field typed as uint8_t* gets int64_t. Needs monomorphised struct field types. Fixes generic_test.
- [x] #31 CHECKPOINT 6: Final phase 2 evaluation.
- [x] #32 Add AST_EXPR_INTERP handling to sema — sema doesn't walk string interpolation sub-expressions, so method calls like `xs.get(index: 0)` inside `"{...}"` never get `decl_link` resolved. Add INTERP case to `sema_analyze_expr` to recurse into interp parts. HIGH IMPACT: fixes 06_list_basic, 07_list_loop, and similar interpolation-heavy tests. LOW RISK.
- [x] #33 Clear `generic_params` on specialized decls in `specialize_decl()` — specializations inherit `generic_params` from the template via struct copy, confusing C backend code paths that check `fd->generic_params` to decide if a function is generic. A specialization has `specialization_args`, NOT `generic_params`. Set `generic_params = NULL` on specializations, use `generic_template->generic_params` where param names are needed. MEDIUM RISK: must audit all `generic_params` checks.
- [x] #34 Generalize sema generic inference beyond `this` parameter — sema only infers generic args from the first param if named `this`. Extend to try ALL param/arg pairs: if an arg has a resolved struct type with generic args, try `infer_generic_args` against the corresponding param type. Fixes generic_test (`setValue(T)(b: mod Box(T), val: T)`). MEDIUM RISK.
- [x] #35 CHECKPOINT 7: Re-run all tests after #32-#34. Evaluate which of the 9 failures are fixed.
- [x] #36 Add receiver-type inference to C backend fallback for non-generic contexts — safety net for cases where sema still fails. When fallback finds a generic function and the first arg has a known struct type with generic args, infer concrete types and emit the specialized name. LOW RISK.
- [x] #37 Fix map entry type emission — `StringMapEntry(V)` and `IntMapEntry(V)` referenced by StringMap/IntMap fields are not discovered by the generic type emission pass. Walk struct field types recursively during type discovery. Fixes 370_map_basic (partial). MEDIUM RISK.
- [x] #38 CHECKPOINT 8: Final phase 3 evaluation. Target: 0 unit failures, ≤3 example failures.
- [x] #39 UI Phase 0: Scene-driven UI design doc + music-player screen PNGs in place. See `rae/examples/98_mobile_ui/DESIGN.md` and `rae/docs/royal-blush-ruics-ecs-reference.md`.
- [x] #40 UI Phase 1a: Add `lib/json.rae` — `JsonValue` tree (`Null/Bool/Number/String/Array/Object`) plus `parseJson(source)` recursive-descent parser and `jsonField/jsonInt/jsonFloat/jsonString/jsonBool` helpers. Tests covering primitives, nested arrays/objects, malformed input. ~150 LOC.
- [x] #41 UI Phase 1b: Add `lib/ui/ecs.rae` (`Entity = Int`, `ComponentTable(T)` sparse-set, `UiWorld` with named tables) + `lib/ui/components.rae` (all authored + derived component types from the design doc) + `lib/ui/registry.rae` (component name → deserialiser/applier table). World boots; can `add`/`get` components by hand.
- [x] #42 UI Phase 1c: Add `lib/ui/scene.rae` (`.raescene` JSON → in-memory `Scene` struct, validates `type=="Scene"`/`version==2`/root exists/children exist) and `lib/ui/scene_loader.rae` (Scene → ECS entities + components + ParentLink hierarchy). No layout yet — just dump the entity list.
- [x] #43 UI Phase 1d: Add `lib/ui/layout.rae` (two-phase `measure → compute` for `None/Horizontal/Vertical/Stack` with `Hug/Fill/Fixed` resolution, padding, `Align`, `Constraints`) + `lib/ui/transform.rae` (`ComputedRect + parent → WorldTransform`) + minimal `lib/ui/render.rae` painting `Shape:Rect` only. Album scene shows as a tower of grey boxes positioned correctly. **First user-visible milestone.**
- [x] #44 UI Phase 1e: Extend `lib/ui/render.rae` to `Sprite` (raylib texture), `Text` (`drawTextWithFont`), `Shape:RoundedRect`, `Shape:Circle`. Add `lib/ui/text_measure.rae` for hug-text using `measureText`. Add `lib/ui/textures.rae` with lazy texture loading from `assets/`. Ship 14 placeholder icons + Roboto. Album scene now reads as the real screen 2 (track rows still hand-laid, no scene instance yet).
- [x] #45 UI CHECKPOINT 9: Album scene renders correctly without scene instances; snapshot screenshot for regression watching.
- [x] #46 UI Phase 1f: Add `lib/ui/scene_instance.rae` — load nested `.raescene`, preserve mounting entity placement (`Rect/Size/Align/Offset/Constraints/OverflowPolicy/TransformFx`), apply `params.overrides` (target by `nodeId`/component/field). Replace hand-laid track rows in album scene with five `SceneInstance`s of `track-row.raescene`. Wire `nav-tabs.raescene` and `mini-player.raescene` similarly.
- [x] #47 UI Phase 1g: Add `lib/ui/input.rae` — gather pointer state, hit-test by `HitArea`/`PointerEvents`, dispatch `OnClick.actionId` to a registered action handler. Tabs and play button become tappable.
- [x] #48 UI Phase 2: Add `music-player-now-playing.raescene` (screen 1). Tab navigation switches between two scenes via `loadScene` + `prepareLayoutRootForLoad`-style root reuse.
- [x] #49 UI CHECKPOINT 10: Both screens reachable; review for stdlib gaps before committing to workout app screens. Decide what `lib/ui/list_view.rae` needs.
- [x] #50 UI Phase 3a — Material Symbols icon font: ship Google's Material Symbols variable `.ttf` to `examples/98_mobile_ui/assets/`, load into a new font slot (slot 1), add `lib/ui/icon_codepoints.rae` with name → codepoint mapping for the ~30 icons the music player needs (`arrow_back`, `more_horiz`, `play_arrow`, `shuffle`, `repeat`, `skip_previous`, `skip_next`, `favorite`, `favorite_border`, `add`, `download`, `search`, `home`, `library_music`, `whatshot`, `equalizer`, etc.). Extend `Sprite.textureKey` resolver in `render.rae` to detect a `mat:` prefix and route to `drawTextWithFont` via the icon-font slot. Drop the 14 placeholder PNGs once the music player no longer references them.
- [x] #51 UI Phase 3b — Album view structure rework: replace the current full-width hero with the reference layout — small square cover thumbnail (~80x80) on the left + title block (`Charcoal` h1, `Brambles` artist, `Album · 8 songs · 2012` caption) on the right. Add the three small action circles (add to playlist, download, more) below the row. Add the prominent black `Play` pill + outline `Shuffle` pill row. Match the reference's track-row layout: index OR mini-waveform indicator, song title with `Brambles · 2:52` artist+duration subtitle, trailing more icon. Bottom mini-player gets album thumb + title + heart + play (matching reference). All from `track-row.raescene` overrides.
- [x] #52 UI Phase 3c — Now-playing rework: shrink the hero to a square ~250x250 with rounded corners, position centred above the title row. Move the heart icon to the LEFT of `Charcoal` + artist subtitle, with `more_horiz` on the right. Replace the flat `ProgressBar` shape with a thin waveform line (Phase 1 just an SVG-style traced PNG sprite or hand-rolled; full audio waveform comes later). Add the big-circle `Play` button (centred ~64dia) flanked by smaller `skip_previous`/`skip_next` (~40dia) and `shuffle`/`repeat` outboard.
- [x] #53 UI Phase 3d — Bottom navigation: extend `nav-tabs.raescene` to four tabs (`Home`, `Search`, `Library`, `Hotlist`) using Material icons (`home`, `search`, `library_music`, `whatshot`). Each tab entity carries `OnClick: nav.<id>`; dispatcher in `main.rae` swaps screens accordingly. Active-tab styling lands as a Phase 4 polish pass.
- [x] #54 UI Phase 3e — Typography swap: drop in **Inter** (or **SF Pro fallback**) `.ttf` for the body font slot, retire Roboto for the music player. Verify hi-res atlas (96px) renders cleanly at the actual sizes used (h1 36, h2 28, body 18, caption 12). No SDF needed for fixed-size mobile screens — stay with raylib's bilinear-filtered glyph atlas.
- [x] #55 UI Phase 3f — Padding + sizing pass: extract pixel measurements from `screens/music_player_screen_1.png` and `_2.png`, update the size constants in `examples/98_mobile_ui/main.rae` and the dimensions in each `.raescene` to match. Goal: the snapshot baselines visually match the reference within ~2px tolerance on key landmarks (hero size, title baseline, button heights, padding).
- [x] #56 UI CHECKPOINT 11: Both music-player screens visually match the reference within tolerance. Capture new baselines; review what's missing before starting the workout app.
- [x] #108 Fix qualified-call Live gap: `module.func(...)` (e.g. `checksums.crc32(...)`) compiles in Compiled but fails in Live with "unknown identifier '<module>' in VM". The sema method-call→call rewrite (sema.c:1395-1444) isn't taking effect on the VM/live path; prime suspect is the `!symbol_table_lookup(symbols, lhs)` guard at sema.c:1404. Repro: isolated project, `import compress/checksums` + `checksums.crc32`, run --target live vs compiled. Workaround: `open` + bare call. Blocks Live-mode namespacing.
- [x] #122 Remove raylib from lib/ui — PARKED (2026-07-01): NOT removing raylib. lib/ui keeps its raylib path (98 + raylib examples) alongside the gpu2d backend (lib/ui/render_gpu2d, 106). Both coexist; no module-by-module removal.
- [x] #136 [compiler bug, found fixing tests] Implicit project import loads the ENTRY file twice — when run without --project from a folder, the sibling-scan of the entry's own directory re-loads main.rae, so a type defined in the entry gets its auto-derived methods (toJson/fromJson/to_str + spawn thunk) emitted twice → C "redefinition" errors. Repro: a single-file program defining a type, run as `bin/rae run --target compiled tests/cases/337_list2_int/main.rae` from compiler/. Fix: exclude the entry file from the implicit sibling scan. Worked around in 337 with `run --no-implicit`.
- [x] #137 [compiler bug, found fixing tests] Under --no-implicit, calling a core function (e.g. overloaded `log(value: Any)`) mangles to bare `rae_log` (undeclared) instead of the overload-mangled `rae_log_RaeAny_`, unless `open core` is present — so the C symbol name depends on open-ness. Fix the overload mangling to be independent of implicit-prelude/open state. Worked around in 383/384/398 with `open core`.
- [x] #138 C backend ownership fix: opt String / opt T cleanup must be representation-aware; root cause is an `is_opt`-blind base-type-only drop classifier applying a `rae_String` destructor to a `RaeAny` representation.
- [x] #139 [compiler robustness] Parser SIGSEGVs (finish_call → parse_postfix) instead of erroring cleanly when a call argument name is invalid (e.g. a keyword used as an arg name). After printing "expected ':' after argument name" it crashes rather than recovering. Make finish_call fail gracefully.
- [x] #195 Make tests and examples for DATA hot reload, or UI LAYOUT hot reload, which ever term is better.
- [x] #196 Implement docs/ownership-model.md: `own` keyword (expression `own x` + parameter mode `own T`), move detection only on `own` / `ret`, then re-enable the two commented-out `emit_implicit_drops_for_body` call sites in `compiler/src/c_backend.c`. Unblocks `416_memory_leak_check`; must keep `411_json_parser` / `412_ui_ecs` / `413_scene_loader` / `414_ui_layout` passing. See `docs/ownership-model.md` for the full spec and test list (A–F).
- [x] #197 Do option 1 first: verify the codegen ownership path before applying the fix.  This is very likely the right fix, but because pool registration changes ownership/lifetime semantics globally, please confirm the existing `take` behavior first.  Specifically verify:  1. `let s: String = a + b`    The concat result must be detached from the string pool with `rae_string_pool_take(...)`, so it survives statement-scope flush and is later dropped as local owned String.  2. `ret a + b`    The return epilogue must detach the concat result with `rae_string_pool_take(...)`, so it survives function cleanup/flush.  3. `foo(a + b)` where `foo` takes `view String`    The concat result should remain pool-owned and be flushed after the statement.  4. Nested concat / int-to-string:    `"x" + i.toString()` should not leak intermediate concat or int_to_str results.  If those ownership paths are already correct, apply the small fix:  - register `rae_ext_rae_str_concat` results with the string pool - register `rae_str_from_buf_impl` results with the string pool - register `rae_str_from_cstr_impl` results if it creates owned heap strings  Then run: - full compiler tests - all examples - 20K Compiled mobile UI stress with `RAE_MEM_STATS=1`  Success criteria: - concat outstanding goes to ~0 - int_to_str outstanding goes to ~0 - no double-free regressions - mobile UI stress slope drops close to zero  Do not broaden the patch unless verification shows missing `pool_take` codegen.
- [x] #198 Investigate where post-pool_take concat results escape ownership. After #197, int_to_str outstanding went 370K → 50K (-86%) but concat stayed at 801K. The pool_take codegen IS firing on concat RHS (verified in c_stmt.c:438/528/596) so the result IS removed from the pool — but the downstream owner (struct field assignment, list element add, ret-from-helper returning String, etc.) never frees it at its own scope exit. Diagnose by: (a) running mobile UI stress at 5K with extra logging from inside rae_mem_str_untag tracking which call path frees the few concats that DO get freed; (b) inspecting generated C for one buildScreenWorld helper to see whether `component.style = concat(...)` lowers to a path that loses the str_free at component drop; (c) check whether Layer 5 synth'd drop is missing String fields. Likely outcome: cascade-drop gap for struct field reassign OR a List<String>.add path that doesn't tag the element as owned.
- [x] #200 that is way too much. maybe halve it to 16?
- [x] #201 Re-enable play-history persistence in 98_mobile_ui — root cause was NOT the buf_get cascade-drop. Two real bugs: (a) `own String` helper params get double-freed (caller emits drop AFTER the call AND callee drops at fn exit), (b) `lib/string.replace` returns an aliasing String on some no-match shapes. Worked around at user-code level (inline concat chains + per-char jsonEscape). Roundtrip verified.
- [x] #202 Compiler fix: `own String` param double-free. Caller-side codegen emits `rae_string_drop(&src); content = pool_take(call_result)` after invoking a function with `content: own String` param, while the callee also drops the param at function exit → "POINTER_BEING_FREED_WAS_NOT_ALLOCATED" abort. Either suppress the caller-side drop when the source variable was move-transferred to a `own` param, or change callee to skip its param drop after the value is consumed. Repro pattern was in examples/98_mobile_ui/history_io.rae's old appendStringField helpers (now removed).
- [x] #203 Compiler/stdlib fix: `lib/string.replace` returns aliasing String. The function has `if old.length() is 0 { ret this }` and the split+join path also produces a String that, in some shapes, ends up sharing storage with `this` (or with whatever was assigned to the concat accumulator most recently). Visible symptom: a long pipeline of `r = r.replace(...).replace(...); ret r` returned values that, when subsequently embedded into another concat chain, contained bytes from the OUTER accumulator. Either make `ret this` deep-copy, or audit the split+join branches for alias return.
- [x] #204 Frames UI architecture: authoritative design doc landed at rae/docs/ui-coordinate-and-responsive-layout.md (named frames + shared entity graph + per-frame overrides + stable identity/reparenting + local ScaleToFit boards; design-unit/unitScale model, DPR separate). Superseded the old viewport plan (annotated, kept) + deleted interim adaptive-layout draft. Committed in rae submodule.
- [x] #205 Coordinate-unit prototype: examples/101_coord_proto — runnable experiment (200du button + 1080x1600 board in phone 1080x2280 vs desktop 5760x3240 frames, unitScale soft/live via UP/DOWN, SPACE toggles frame). Compiles+links. Awaiting Jonas's judgment on whether to lock unitScale=3. See doc "core coordinate decision" + open decisions.
- [x] #206 Coordinate-system decision cleanup: document that 106_mobile_ui is the primary gpu2d/SDL3 target for Model A (`unitScale=3`, 1080-wide phone frame, desktop ~5760du wide), while 98_mobile_ui remains legacy/reference unless explicitly revived.
- [x] #207 Frames S1 remaining — finish the 106_mobile_ui coordinate spine now that `Viewport`, `unitScale=3`, DPR logging, scene `unitScale` metadata, and legacy scene scaling exist: remove remaining module-global layout authority, make pointer/input conversion consistently use Viewport design units, define the approved pixel-snapping policy in gpu2d render, and prove the legacy 498×1079 adapter still works before the full 1080×2280du rescale. See rae/docs/ui-coordinate-and-responsive-layout.md §10 S1.
- [x] #208 106 coordinate migration S1 follow-up: after #207, remove remaining legacy `screenW/screenH` authority, pass Viewport/layout extent explicitly through 106 systems, convert old 498×1079 scene constants toward 1080×2280 du, and keep 98 out of scope unless a separate legacy-backend parity task is opened.
- [x] #209 Frames S2 — safe areas + device presets + simulator for 106_mobile_ui: add safe-area data in du, forcedFrameId/device preset selection, and keep gpu2d screenshots deterministic; §10 S2.
- [x] #210 Frames S3 — frame selection + per-frame component overlay for 106_mobile_ui: frame table, reset-to-base + applyComponentByName overlays, add removeComponentByName, and prove phone frame vs desktop frame without reparenting; §10 S3.
- [x] #211 Frames S4 — active-hierarchy derivation for 106_mobile_ui: authored vs active graph, reparent + order, cycle detection, and phone-stack↔desktop-split demo; §10 S4.
- [x] #212 Frames S5/S6 — within-frame states and art direction for 106_mobile_ui: phone canvas toggle, frame-only entities, Aspect/ScaleToFit, Sprite cover/contain, and GameBoard sample; §10 S5-S6.
- [x] #213 106 scene-driven Home migration: replace the current programmatic Home page builder with `.raescene` templates plus data adapters for album grids and recently played rows; keep only data extraction and list materialisation in code, with layout/styling/actions represented as scene/ECS components.
- [x] #214 106 render/update separation: split per-frame systems into data sync, UI diff/rebuild triggers, layout/transform, animation, input dispatch, and gpu2d render; remove direct component patching from action handlers where a component-state change should trigger a dependent UI rebuild.
- [x] #215 106 typed UI data resources: replace today’s string-key `AppDataSources`/`DataDependency` revision model with typed app-level resources for albums, history, playback, playlists, Spotify status, settings, and debug state so mounted UI regions refresh narrowly without stringly-typed keys.
- [x] #216 Cleanup obsolete `examples/105_gpu2d_mobile_ui`: 106 is now the primary gpu2d/SDL3 mobile UI example, so either delete 105 or reduce it to a tiny renderer smoke example after checking for any snippets not already copied into 106.
- [x] #217 Research cross-platform image decoding for gpu2d: evaluate JPEG/WebP/PNG loaders beyond macOS ImageIO (stb_image, libjpeg-turbo, platform APIs), decide supported formats, licensing, build integration, and fallback/error policy; do not implement until design is chosen.
- [x] #218 Implement robust non-blocking Spotify/history artwork loading for 106_mobile_ui only: restored `spotify-art:*` entries populate from cache/background fetch without blocking frames; 98_mobile_ui and raylib are legacy and do not need feature parity.
- [x] #219 UI optical centering follow-up: `OpticalAlign` exists in gpu2d, but complete the data-oriented visual-bounds system with derived `VisualBounds`/text metrics, glyph-plane or alpha bounds for Material icons, cached measurement, scene-loader support, and removal of per-widget bias hacks where measured centering can replace them.
- [x] #220 C backend follow-up: generic object literals with owned `String` fields still need concrete field substitution before return/drop ownership decisions; `Wrapper(String) { value: ownedString(...) }` can leave the generated field treated as raw `T`, causing invalid String ownership independent of the `opt T` drop classifier. [Confirmed still reproduces 2026-08-21: Wrapper(String) with owned field -> malloc "pointer freed not allocated" crash.]
- [x] #221 Compiler diagnostic location bug: preserve the original source file when reporting immutable-let reassignment errors across imported modules; repro reported `examples/98_mobile_ui/render_decide.rae:46` while the edited binding was in `main.rae`.
- [x] #222 Compiler: diagnose silent move of an aliased String into an `own` param — `let x: String = someGlobal` then `f(pageId: x)` where the param is `own String` frees the global's buffer and dangles it (bit 106's mini-player pageId; garbage reads/SIGABRT far from the cause). Require explicit `own x` at call sites for own params, and reject/copy when the moved value shallow-aliases another binding.
- [x] #223 Compiler: parser SIGSEGV on a bare `ret` statement followed by unreachable declarations inside a func body (found while bisecting 106); should parse and at most warn about unreachable code.
- [x] #224 UI library backend split: raylib UI code now lives under explicit `ui/legacy_raylib/*`, 106 imports backend-neutral UI core plus `ui/render_gpu2d`, and future apps no longer need the old “do not import ui/render” audit rule; remaining 98 legacy cleanup should be separate.
- [x] #225 106 data-oriented widget model: ECS widget identity/state/dependency/action/style foundations landed (`WidgetId`, `WidgetState`, `DataDependency`, `ActionBinding`, `StateStyle`) and controls/rows/debug toggles are tagged; follow-up typed resources remain in #215 and update separation remains in #214.
- [x] #226 106 widget parity polish after data migration: mini-player root, debug toggles, bottom-sheet actions, playback/like controls, track rows, album/playlist tiles, loading placeholders, hover scale, and all-screen gpu2d snapshots are covered by recent 106 widget commits; remaining architectural cleanup belongs in #213/#214/#215.
- [x] #227 on desktop it seems that web UI has buttons that accept button presses have a finger cursor. Adapt that cursor idea to Rae UI so that some input components automatically take on certain standard kind of mouse cursors on desktop, and switch back to arrow when no longer hovering them. This should be automatic and data oriented based on some component types that fit this idea.
- [x] #229 Implement docs/ui-theme-system.md — SPLIT into #230-#237 below (gpu2d/106 only; NO raylib/98 back-compat — if 98 blocks a step, remove example 98 instead of maintaining its style tables).
- [x] #230 Theme M0 — theme sections + loader: NO new file format — theme data lives in a `.raescene` as top-level sections (palette/text/space/radius/shadow/padding), parallel to how scenes already carry `frames`/`states`; read them out of a scene JsonDoc with the existing flat-pool parser into a ResolvedTheme of flattened parallel-list tables; single-parent `extends` flattened at load with depth cap 4 and loud cycle/unknown-parent errors; `themeDumpResolved()` debug dump; a default `theme.raescene` transcribing today's hardcoded values; nothing consumes it yet. See docs/ui-theme-system.md §3-§5, M0.
- [x] #231 Theme M1 — single text-style resolver: text_style.rae resolves styleId through ResolvedTheme; delete the duplicated g2dTextStyleSize/Color/IsCentered/IsRightAligned tables in render_gpu2d.rae; ui/legacy_raylib keeps its own copy untouched (out of scope, no back-compat work). Gate: byte-identical 106 screenshots. §10 M1.
- [x] #232 Theme M2 — shadows into styles (repairs e39a378): shadow token table; miniTitle/miniMuted styles reference textOnAccent/textOnAccentSoft shadow tokens in the theme file; delete the raw TextShadow blocks from mini-player.raescene; keep the TextShadow component only as the last-resort override and add the scene-loader lint line for raw usage. Gate: 106 screenshots identical. §2, §7, M2.
- [x] #233 Theme M3 — radius + padding tokens: category-aware token resolution in the scene deserialiser (extend the optFloatOrToken pattern to CornerRadius.radius, Shape.radius, Padding insets); add radiusChip/radiusCard/radiusPill + cardPadding-class presets to the theme file; sweep 106 scenes' repeated raw radii/paddings onto tokens with identical values. §5, M3.
- [x] #234 Theme M4 — 106 spacing sweep: replace raw gap/padding floats in 106 scenes and builders with the existing spaceXS-spaceXL tokens (values identical; keep the literal+comment convention only where module-level lets force it). Gate: byte-identical screenshots. §8, M4.
- [x] #235 Theme M5 — live theme switching: store palette slot NAMES instead of parse-time-baked RGBA in scene-authored colors; resolve appearance through the single accessor keyed by a theme generation counter, so dark/light toggle and theme-scene hot-reload become a repaint (no scene re-parse / world rebuild). Largest slice; land last of the value-neutral ones. §6, M5.
- [x] #236 Theme — explicit partial overrides: `styleOverride` sparse per-entity block over the resolved style (base styleId always kept); document + lint the override friction ladder (derive a style >> styleOverride >> raw component). §7.
- [x] #237 Theme M6 — composite button/container styles: named bundles referencing palette slots + radius/padding/text/icon tokens (`primaryButton`-class); StateStyle variants reference theme styles per widget state instead of carrying raw values; depends on widget-model tasks #214/#215. §5 tier 6, M6.
- [x] #238 Compiled backend: `List(Task(T))` element access lowers to `RaeAny` instead of `RaeTask*` — can't hold task handles in a `List` (generic-container element monomorphization). Workaround: use individual `Task` locals. Found building examples/40_raytracer.
- [x] #239 Compiled backend: `List(Struct).get()` returns boxed `RaeAny` and isn't auto-unboxed to the struct (must use low-level `rae_ext_rae_buf_get`). Generalize typed-struct-list element get(). Workaround in raytracer: flat `List(Float)` struct-of-arrays. [Confirmed still reproduces 2026-08-21: List(P) emits undeclared rae_List_rae_P.]
- [x] #243 Strategy: execution targets & deployment decided — see docs/execution-targets-and-deployment.md. No TypeScript target; WASM (via the C backend) is the primary app/game target; C backend stays for native/desktop/real-time/hardware; Live VM kept on probation.
- [x] #249 Strategy: cross-platform tech stack & dependency policy decided — see docs/tech-stack-and-dependencies.md. Preference ordering Rae > C > any-lang-behind-clean-C-API > Rust > C++ > Obj-C; own small/controllable, buy hard domains, all foreign code behind Rae-owned APIs. Relax strict no-C++/no-Rust. Intended stack: SDL3 (platform) + WebGPU-everywhere/WGSL (render) + miniaudio (audio) + Rae-owned layers.
- [x] #250 Deprecate raylib: PARKED (2026-07-01) — decided NOT to remove raylib for now (wasted effort). raylib (98_mobile_ui + raylib examples) and the gpu2d/WebGPU stack (106_mobile_ui + lib/ui/render_gpu2d) coexist side by side; lib/ui keeps its raylib path and gains a gpu2d backend. Revisit only if raylib becomes a real maintenance/deploy burden. Build the Rae Platform/Render/2D/Audio layers as additive, not as replacements.
- [x] #253 Incremental raylib removal — PARKED (see the Deprecate-raylib note): raylib and gpu2d coexist; we're not retiring raylib examples now.
- [x] #260 ECS E0a — establish `ComponentTable(T)` (lib/ui/ecs.rae) as THE ECS table primitive (the design's conceptual "Table(T)"): it already carries the table-level revision as its `generation` counter (kept under that name — more general than "revision", agnostic to insert/remove/reorder; no rename just to mirror the doc). Added unit test 534_component_table_revision asserting `generation` bumps once on set/replace/mod/remove and NOT on a no-op remove; documented in ecs.rae + doc §2 that ComponentTable IS the primitive and `generation` IS the table revision. No separate `Table(T)` type, no duplication.
- [x] #261 ECS E0b — instance + system revision conventions: component instances carry `revision: Int` bumped EXPLICITLY via `bumpRevision(...)`/bump-after-change helpers (no implicit bumping on `mod`); a system is a plain `type` of Table fields + `revision: Int` + free `markChanged(sys)`; eager bump chain component→(table if structural)→system. Document the convention + unit test with a toy system. §5/§7 of the doc.
- [x] #262 ECS E1a — `SystemRegistry` + `SystemId`: registry owns registered systems, handles are IDs (never pointers); access surface exposes only `view` reads and command sends — `mod` never crosses a system boundary. Decide the Rae shape for heterogeneous registration (per-type fields first; compiler feature only if genuinely needed). §6/§8 of the doc.
- [x] #263 ECS E1b — generic `Observe` + `refreshObservers`: `Observe { system: SystemId, kind: component|table|system, target ids, observedRevision }` component + ONE generic refresh pass (`if current != observed { rebuild; observed = current }`) replacing the per-domain refreshXViewsIfNeeded pattern; runs in the observation phase; old DataDependency path stays in parallel until #268. §6/§8.
- [x] #264 ECS E2a — frame phases + command inbox + SYSTEM LIFECYCLE contract: define the 106 frame pipeline Input → Apply(A) → Observation refresh → Apply(B) → Layout → Render (two apply passes per frame, §4/§4.1); add a simple `Queue(T)`-style inbox primitive; and define the shared lifecycle every system implements in a fixed execution order — initialize / processCommands (drains own inbox holding `mod self`) / update(dt) (timers, animation, polling, async harvest — own tables only) / shutdown, with observation driven externally by the refresh pass (§6). One documented contract so no system invents its own lifecycle later. Restructure 106's main loop onto these named phases without behavior change.
- [x] #265 ECS E2b — carve out `PlaybackSystem` (first actor): plain `type` with inbox + the #264 lifecycle contract (first real implementer — validates the contract); `nowPlaying` table (track/art/position/isPlaying); Spotify becomes integration functions called from `update()` that write ONLY PlaybackSystem's own tables + bump revisions; play/pause/like become commands. 106's playback state migrates off PlaybackState/AppState. §3/§5.1/§6 of the doc.
- [x] #266 ECS E2c — carve out `MediaLibrarySystem`: albums/tracks/artists/thumbnail tables out of AppState; loaders write via its own functions; library/history views read via `view`. Split eagerly — history may become its own PlayHistorySystem if it grows. §5.1.
- [x] #267 ECS E2d — per-feature UI systems: split 106 UI into UiPlaybackSystem (mini-player + now-playing widgets) first, then others (UiMenuBarSystem for the dock/nav) as follow-ups; persistent widget state (focus/selection/scroll) lives here; they observe data systems and send commands (never write foreign tables). §5.2.
- [x] #268 ECS E3 — port consumers to Observe (FIXES the stale mini-player cover bug): cover + play/pause icon observe PlaybackSystem.nowPlaying instance revision and re-point by identity; delete rebindAlbumCoversToSpotify + pollSpotify's direct UI writes; title/artist/time labels go the same way; verify: play track A, then B from same screen → cover follows. §1/§9 of the doc.
- [x] #269 ECS E4 — delete the resource enums: remove DataResourceId/DataFieldId/AppDataResources/bumpXResource once every consumer is on Observe; suite green. §8/§10.
- [x] #270 ECS E5 — retained render cache (dependency-driven): ComputedRect pool with per-entry input-revision dependencies (window size, parent layout, text, theme tokens) → dirty on dependency change, reuse otherwise; glyph layout + GPU buffer reuse under the same rule; list virtualization (render only visible rows, O(visible) not O(total)). Largest slice; land after E3 proves observation. §5.3/§11.4. Effort: large.
- [x] #271 ECS E6 — threading proof (one system off-thread): move one suitable system (asset loading or a compiler-worker stand-in) onto a spawn'd thread; its inbox becomes a cross-thread channel with the SAME send API (transport hidden, §3.2); observation stays read-only snapshots/revisions. Builds on the raytracer spawn machinery; drives whatever compiler work real cross-thread channels need — no hacks. Effort: large.
- [x] #272 Compiler papercut — passing an rvalue (e.g. an enum literal `SystemId.playback`) to a `view` param fails to compile: "cannot take the address of an rvalue". The C backend lowers `view` args to `&expr` without materialising a temp for rvalues. Either materialise an lvalue temp for rvalue `view` args, or diagnose clearly. Workaround today: pass small enums/handles BY VALUE (the correct idiom anyway), or bind to a local first. Low priority; surfaced in #262.
- [x] #278 Compiler — emit all struct forward-declarations before any function prototypes (C backend), so a struct defined in one module can appear in another module's function SIGNATURE regardless of module emission order. Today a newly-created module is emitted LAST, so moving a widely-used type (e.g. UiWorld) into a new lib/ui file makes it an "unknown type name" in earlier-emitted consumers' prototypes (hit while trying to split lib/ui/ecs.rae for #270a — worked around by trimming comments to stay under the 1000-line cap). Once fixed, split lib/ui/ecs.rae (storage primitives vs UiWorld/world helpers) into two files. No hacks.
- [x] #280 WONTFIX/false-premise — INVESTIGATED 2026-07-13: the premise was wrong. `List(T).get` (core.rae:93) returns `rae_ext_rae_buf_get(...)` — a raw ALIAS, NOT a copy — so `is_owned=0` is CORRECT and there is NO get-String copy leak. Forcing `owns=true` frees the list's own element → double-free at UiWorld teardown (reproduced 3×; isolated to `history.entries` textureKeys via drop-trace). The dominant real leak (~1MB/5s, paint-path theme lookups) was already fixed via the buf_get idiom (317fdab). REMAINING real leak is DIFFERENT: string-interpolation (`"{...}"`) results, ~600KB/min + a smaller `copy` leak ~80KB/min (measured 106 headless: interp outstanding 3497→9065 over 6s→18s). New investigation: find the per-frame `"{...}"` sites (~8/frame) whose results escape statement-scope pool flush (likely stored via pool_take into a persistent String/field and never dropped). Superseded by a new interp-leak task.
- [x] #283 MOOT/WONTFIX (investigated 2026-07-13): NOT a leak. The UiWorld is rebuilt+dropped every loop iteration via full rae_drop_struct_rae_UiWorld (verified in emitted C, line ~55968), which cascades through every ComponentTable → element struct → String field (Layer 5 complete). So per-widget component token copies are freed each frame — 0 residual. The token-name content in the exit dump is theme globals (#284) + the theme JsonDoc (fixed in #285/commit 57495c6), not world components. Verified via RA/dladdr + content instrumentation (temp, reverted).
- [x] #286 Runtime migration R0 — audit `compiler/runtime/rae_runtime.c` into permanent C kernel vs temporary C policy layers per `docs/runtime-migration-to-rae.md`; produce a table of functions grouped by allocator/raw memory, strings/buffers, OS/sys, raylib legacy, SDL3/gpu2d, WebGPU, image decode, Spotify/macOS, and migration candidacy.
- [x] #287 Runtime migration R1 — design the permanent runtime-kernel ABI (`docs/runtime-kernel-abi.md`): allocator, raw buffers, atomics/threads, crash/signal, opaque native handles, platform imports, and rules for marking APIs permanent vs temporary migration bridges.
- [x] #288 Runtime migration R2 — split `rae_runtime.c` into smaller domain-specific C runtime modules after #286/#287, without behavior changes; update compiler runtime-copy/link paths so emitted apps still build; keep the public ABI stable and run full compiler/examples tests.
- [x] #289 Runtime migration R3 — design Rae-owned opaque native handle ownership/drop semantics for platform resources (SDL window/cursor, WebGPU device/buffer/texture/pipeline/bind group, image handles); ordinary Rae apps must benefit, no runtime-only magic.
- [x] #290 Runtime migration R4 candidate — move path utilities and filesystem convenience policy into Rae (`lib/filesystem`/path helpers), keeping only minimal platform calls in C; select timing after #286 audit/#287 ABI design, depends on module namespacing/filesystem tasks, and must include compatibility tests.
- [x] #291 Runtime migration R5 candidate — migrate JSON parser/serializer policy fully to Rae and reduce C JSON helpers to either nothing or small compatibility bridges; select timing after #286 audit/#287 ABI design, gate with existing scene/theme/package JSON tests and memory diagnostics.
- [x] #292 Runtime migration R6 — design and migrate HashMap/StringMap/List policy toward Rae implementations over raw buffer primitives, after generic container/codegen ownership bugs are fixed; keep C raw allocation/copy primitives only.
- [x] #293 Runtime migration R7 — evaluate `String` layering: keep allocation/drop kernel in C, but move UTF-8/string algorithms (trim/contains/split/replace/case/index) to Rae where performance and ownership tests pass; do not change `String` representation yet.
- [x] #294 Runtime migration R8 — move image registry, failed-load throttling, asset cache metadata, and resource-cache policy from runtime/app glue into Rae; C remains decoder/upload boundary only.
- [x] #295 Runtime migration R9 — design WebGPU resource management in Rae: descriptors, ownership, resize/reconfigure policy, bind tracking, validation, render graph, and debug labels over thin C raw WebGPU calls.
- [x] #296 Runtime migration R10 — define stdlib bootstrap tiers (`core`, `alloc`, `std`, `platform`) and build ordering so Rae stdlib modules can dogfood the language without circular dependencies; document migration gates and success metrics.
- [x] #297 Runtime migration architecture — write the standard-library ownership model design: which types remain compiler-known, which become ordinary Rae code, which need runtime support forever, which are opaque native handles, and where the permanent ABI boundary sits; depends on #286/#287.
- [x] #305 Combined-branch bookkeeping — three seed 3D examples now have distinct tabs: `107_gpu3d_raymarch` (raymarch), `108_gpu3d_procedural` (procedural raymarch), `109_gpu3d_pbr` (raster PBR). Roundtable evaluation + unification architecture written up in `rae/docs/unified-3d-renderer.md`; scores 107=6.6, 108=7.35, 109=7.9.
- [x] #306 QUEUE hygiene — removed the obsolete #298-#304 duplicate 3D track, retained completed work under #317/#323, preserved unique SSAO/showcase outcomes as #325/#326, and corrected documentation that named the removed `107_gpu3d_minimal` folder.
- [x] #316 3D draw-cap guard — documented the hard 4096 per-frame storage limit, safely discard excess submissions with one renderer-lifetime error, added a lower diagnostic/test limit, and extended example 109's smoke gate to prove non-blank output plus exactly one overflow report; strict build, 175 compiler tests, and 69 example smokes pass.
- [x] #317 3D arc M1 — unified 3D renderer foundation (was mis-numbered #286/#295; those belong to the Runtime-migration track). Z-up conversion of lib/math3d.rae + lib/mesh3d.rae per docs/coordinate-system.md; lib/scene3d.rae backend-agnostic scene (Vec3 components, entity indirection, typed generation-carrying Mesh/MaterialHandle, MeshRenderer vs SdfPrimitive tagged geometry split); typed gpu3d seam (beginScene/renderScene replacing the 36-float block + 9-positional-float draw); runtime_gpu3d.c (wgpu-native, MSAA 4x + Depth32Float resolving into the gpu2d offscreen, Cook-Torrance GGX + hemisphere ambient + ACES); examples/109_gpu3d_pbr ported and verified headless. Architecture + scores in rae/docs/unified-3d-renderer.md.
- [x] #318 Compiler fix — binary-op RHS lost parentheses in emitted C (`a - (b + c)` became `a - b + c`), silent wrong arithmetic in any Rae program; found via mat4LookAt. Fixed in c_expr.c, regression test 548_paren_precedence passing. (Was mis-numbered #287.)
- [x] #319 Compiler bug — reject reserved keywords used as binding names and guarantee parser recovery makes progress. `let view` now reports a focused reserved-keyword diagnostic; missing unary operands preserve enclosing delimiters; call argument recovery terminates at block/EOF boundaries. Regression test 549 passes, along with the full 175-test compiler suite and 69 example smokes. (Was mis-numbered #288.)
- [x] #320 3D: noise library — added deterministic allocation-free CPU hash, value/Perlin/simplex 2D/3D, normalized FBM and domain warp in `lib/noise.rae`; matching `lib/noise.wgsl` is composed once at pipeline creation and now drives examples 107/108; `mesh3d.makeFbmTerrain` caches an FBM height grid and derives smooth normals as the first CPU consumer. Documented CPU-vs-GPU policy; focused terrain/noise tests, 177 compiler tests, and all 70 example smokes pass. (Was mis-numbered #290.)
- [x] #321 3D WASM/web build — added `rae build --target wasm` as an Emscripten browser target with inferred SDL3 + EmdawnWebGPU linkage, canvas surface/runtime portability guards, browser presentation semantics, Asyncify loop compatibility, a Rae demo shell, 109 package target, docs, and a repeatable bundle smoke gate. Verified the real 109 PBR scene in headless Chrome with zero console errors and preserved native screenshot output. (Was mis-numbered #293.)
- [x] #322 3D/UI composition seam — added public gpu2d `beginFrameLoad` using `LoadOp_Load` on the shared resolved color target plus gpu3d `endPass` for submit-without-present, establishing explicit 3D -> ECS UI -> one-present ownership without changing standalone `endFrame`; strict compiler/runtime rebuild passes, with full visual/native/WASM acceptance covered by #328. (Was mis-numbered #294.)
- [x] #323 3D: headless screenshot regression test for the raster renderer — the Compiled example gate now runs 109 with bounded SDL3 auto-exit, captures its offscreen WebGPU frame through `RAE_GPU2D_SCREENSHOT`, validates BMP structure, and rejects blank/clear-only output using color and foreground thresholds. Real and synthetic-blank validation passed; all 69 example smokes pass. Closes the missing third of #301.
- [x] #324 Restore the compiler `MODE=CI_STRICT` build gate: fixed byte-string/C-text boundaries, completed strict enum/initializer/unused-code handling, and scoped vendored stb_image warnings without weakening `-Werror` for Rae-owned code; forced strict rebuild, 175 compiler tests, and 69 example smokes pass.
- [x] #328 3D application example with ECS/.raescene UI — added `examples/112_metaballs_deferred` with separate persistent `Scene3d`/`UiWorld`, authored HUD/pause/settings scenes and theme, typed revision-driven narrow refresh, generic paint-ordered GPU2D input capture ahead of orbit controls, and explicit 3D -> load-preserving ECS UI -> one-present composition. Compiled and asset-packaged browser-WASM targets pass; deterministic normal/pause screenshots prove 3D, translucent shapes, MSDF text/icons and ordering; 175 compiler tests and all 70 example smokes pass.
- [x] #329 3D `.raescene` authoring — define `Scene3d` transforms, material colors/PBR values, mesh-key renderers, SDF primitives, camera and light as validated scene components; resolve stable mesh/material keys through registries, support reload without rebuilding renderer resources, and migrate example 110 scenes 1–3 out of programmatic builders into authored `.raescene` assets.
- [x] #331 Render graph v1 — a REAL graph, not a renamed pass list: logical resources with descriptions (kind/format/sizing/lifetime), pass nodes with explicit read/write declarations and access intent, dependency derivation, topological execution order, validation of invalid dependencies, raster AND compute node kinds, single command encoder. Lifetime classes Transient/Persistent/External are in v1 even though pooling is not — TAA history and SDF cascades are semantically cross-frame and a graph that cannot express that is wrong, not merely unoptimised. Record Access precisely (AttachmentWrite/SampledRead/StorageRead/...) even though v1 only uses it for ordering; barrier inference later needs intent and retrofitting means revisiting every pass. NOT in v1: aliasing, transient pooling, async compute, multi-queue, dead-pass elimination, feature registries. The graph must never contain the tokens gi/ssao/shadow/sdf/taa. DONE: lib/rendergraph.rae — resources with kind/format/sizing/lifetime, pass nodes with read/write/modify declarations and access intent, Kahn topological order, validation; raster+compute kinds; Transient/Persistent/External all present. Tests 552/553.
- [x] #332 Port the existing three passes into the graph with NO visual change — 3D raster, SDF metaball fragment pass, 2D UI overlay. Acceptance test is byte-identical headless screenshots (RAE_GPU2D_SCREENSHOT) before/after, so the refactor is provable and any later regression is attributable. This is the safety mechanism for the whole migration; do not skip it. DONE: 109 and 110 ported, verified by byte-identical headless screenshots; required adding RAE_FIXED_DT + RAE_HEADLESS_FRAMES for deterministic capture, and passModifies (read-modify-write edges) so the load-preserving UI overlay could be declared truthfully.
- [x] #333 Depth/normal/velocity prepass; DROP MSAA. Single-sampled Depth32Float + a real normal target (not depth-reconstructed — reconstruction is wrong exactly at the edges where AO/GI matter) + rg16f velocity. Dropping MSAA is what makes depth sampleable (WebGPU guarantees no depth resolve), which unblocks SSAO, GI gathering, SSR and volumetrics in one move. Metaballs move INTO the prepass (raymarch writes frag_depth + normal) so implicit and triangle geometry unify for shading/AO/GI/TAA instead of every technique special-casing them. DONE: single-sampled Depth32Float (stored, sampleable) + rgba16f real world normals + rg16f velocity, MRT from both the raster and SDF passes so implicit and triangle geometry are indistinguishable downstream. MSAA removed.
- [x] #334 HDR scene colour + tonemap as a pass — rgba16f scene target; move the ACES tonemap OUT of the material uber-shader into its own pass; UI composites in LDR after tonemap. This is a prerequisite, not polish: accumulating GI on tonemapped values is mathematically wrong (light does not add after a nonlinear curve), so any indirect lighting built on the current pipeline would be incorrect regardless of algorithm quality. DONE: rgba16f linear HDR scene target; ACES+exposure+gamma moved out of both material shaders into a dedicated tonemap pass that is the only writer of the LDR offscreen; UI composites after it in display space.
- [x] #335 TAA — jittered projection (Halton 2,3 sub-pixel offset in mat4Perspective, removed before reprojection maths), motion vectors, persistent rgba16f history, neighbourhood/variance clamping. Not just antialiasing: jitter+history+reprojection are the same temporal substrate GI accumulation needs, so this is shared infrastructure. Known costs to manage: ghosting on disocclusion, softening, care with thin geometry. Depends on #336. DONE: Halton(2,3) clip-space jitter (velocity computed from UNJITTERED positions), per-object motion vectors via a prevModel matrix per draw, ping-ponged rgba16f history, neighbourhood-clamped luminance-weighted resolve on linear HDR before tonemap. taaColor is the graph’s first Persistent resource. RAE_NO_TAA=1 opts out; captures stay deterministic.
- [x] #336 scene3d prerequisites for the renderer — `lib/scene3d.rae` needs (a) previous-frame transforms per entity, required for motion vectors in #335, and (b) a change/revision signal so dirty-region GI updates in #338 are possible. Neither exists today. Small change to the scene model, blocks two later items. DONE: Scene3d gained prevTransforms (advanced once per frame by snapshotPrevTransforms) and a monotonic revision counter bumped by the sanctioned setTransform path. GPU wiring for prev-model matrices landed with #335.
- [x] #337 SSAO — DONE: horizon-search AO with a 32-bit VISIBILITY BITMASK per slice (GTAO, Jimenez 2016 + Jover 2023) rather than hemisphere sampling — a horizon search integrates a whole angular sector per sample so it converges with far fewer taps, and the bitmask records each occluded sector independently instead of guessing what is behind an occluder, which is what removes GTAO's thin-geometry over-darkening. Sector boundaries are uniform in SIN so a plain popcount approximates the cosine-weighted integral (no weight table, no arccos inner loop). Half-res generation + depth-weighted 3x3 bilateral upsample; contact-scale radius by design so it stays valid alongside #339 GI; interleaved-gradient noise rotates the basis per pixel and per frame so TAA integrates the samples. Applied to the INDIRECT term only — the scene pass now writes ambient to its own MRT attachment (raster AND SDF, so both geometry kinds split identically) and the composite adds ambient*AO back into hdrColor as a graph read-modify-write. One quality knob: `RAE_SSAO_QUALITY=mobile|low|desktop|high` (2x4 .. 4x8 slices x steps); plus RAE_SSAO_RADIUS, RAE_NO_SSAO, RAE_SSAO_DEBUG (raw AO view). KNOWN: faint stipple remains on mid-tones at half-res; a wider spatial denoise or a dedicated AO-history TAA channel would be the next step.
- [x] #343 COMPILER BUG — adding an INTERPOLATED string to a `List(String)` corrupts the element and double-frees on teardown (SIGABRT). Minimal repro: `let m: String = "gamma"` then `plain.add(value: "{m}")` into a `List(String)` reads back as garbage/empty and aborts at exit; `add(value: m)` (plain variable) and `add(value: "literal")` both work. Not struct-related — reproduces with a bare local List. Almost certainly the string pool: an interpolated string is pool-registered/owned, `List.add` stores the pointer without taking ownership, the pool flush frees it, leaving a dangling element and a second free at list drop. Worked around in lib/rendergraph.rae by storing names without interpolation; the workaround is commented at the call site so it is not "cleaned up" later. Related to the known interp-`{...}` residual-leak note. FIXED: root cause was a missing AST_EXPR_INTERP case in infer_expr_type_ref, which returned NULL and so silently disabled the pool_take wrap in c_call.c that already existed for exactly this. Regression test 554 covers all five paths through that argument slot.
- [x] #344 QUEUE hygiene — removed #309 (superseded: HDR half by #334, AA half by #335, and its "retain 4x MSAA" contradicts #333) and #325 (duplicate of #337, which merged the earlier SSAO entries and carries all its unique requirements); annotated #310 for the graph work already done in #331.
- [x] #345 Window geometry persists across hot reload, for every app automatically — runtime saves size+position on closeWindow and restores on initWindow. Guardrails: only restores if the file is <=10s old (a cold start still honours the size the app asked for), disabled entirely under RAE_HEADLESS_FRAMES/RAE_SDL_HEADLESS_MS/RAE_GPU2D_SCREENSHOT/RAE_UI_DEVICE so screenshot captures cannot silently change resolution, and RAE_NO_WINDOW_RESTORE=1 opts out. Example 106's hand-rolled version was removed, not left alongside — it set position AFTER initWindow and would have overridden the runtime.
- [x] #346 Metaball clusters with per-ball colour blending (110) — SdfPrimitive gained `cluster` and `smoothing`; only primitives sharing a cluster fuse, and stickiness is per cluster. Albedo is per PRIMITIVE: the weight that fuses two distances also mixes their colours, so a cluster can hold a gradient. 110 ships three deliberately contrasting clusters (blue k0.62, near-black obsidian k0.30 slow, emissive ember k1.15 fast) plus periodic breakaway droplets. Found and fixed en route: one shared SDF buffer set cannot serve multiple draws per frame — wgpuQueueWriteBuffer applies at submit, so every cluster rendered the last one's data.
- [x] #347 Reusable on-screen log overlay widget (`lib/ui/log_overlay.rae`) — fixed-capacity ring buffer, 5 levels (trace/debug/info/warn/error) with warnings yellow, errors red, normal white; draws at most `maxLines` and never more than half the window height. Drawn directly through gpu2d rather than as ECS entities so a diagnostic cannot perturb the layout/component tables it is meant to observe, and the panel width is a window fraction so no per-frame text measuring. Anchored bottom-left in 110, top-left in 106 (configurable margins clear a status bar). `revision` lets a change-driven loop treat a new line as a reason to redraw. First consumer: `nextReloadGeneration()` in lib/hot_reload.rae logs "hot reload N" at startup, so a reload that silently failed to relaunch is distinguishable from one that changed nothing.
- [x] #348 110 state survives hot reload — selected scene, pause/speed/debug toggles and the FULL camera (orbit-vs-WASD mode, yaw/pitch, distance, free position, target) persist through `lib/hot_reload.rae`'s state file. Saved on CHANGE and throttled to 4/sec (110 does not watch for a reload signal, so an exit-only save would lose everything to a hard kill; one file write per frame during a camera drag would be exactly the hidden cost this app measures), plus a final flush on clean exit. New app-scoped helpers `readHotReloadStateFor`/`writeHotReloadStateFor` keep it out of `state.json`, which belongs to whichever app's schema wrote it (106 stores playback state there). Payload is one version-tagged line of numbers so a stale/corrupt/newer snapshot degrades to a clean start rather than half-applying a camera — verified: garbage, truncated, future-version and empty all boot at defaults without crashing.
- [x] #349 COMPILER BUG — FIXED: a CHAINED call on a generic method's result mis-resolved silently. `parts.at(i).toInt()` on a `List(String)` bound to a same-named overload for another receiver type (List2Int.length), surfacing as an inscrutable C type error rather than an overload diagnostic. Cause: sema built the receiver type-ref for `infer_generic_args` from the INSTANTIATED struct's mangled name (`List_String`) while a generic method's param is written against the template (`view List(T)`), so inference bailed and the call got neither decl_link nor resolved_type — leaving the next link in the chain resolving against nothing. Fixed in the BACKEND's `infer_expr_type_ref` METHOD_CALL case, NOT sema's overload selection: three attempts there each regressed the same 8 generic-container tests (412/413/414/415/420/423/440/442) because changing which candidate is chosen perturbs ECS/StringMap resolution. The fix recovers the callee by name+receiver base type when decl_link is NULL, then substitutes the receiver's generic arg for a returned type param POSITIONALLY (slot of the param in the `this` type, so a method declaring its type params in a different order than the struct still maps correctly). Test 555; 110's `string.toInt(this: ...)` workaround removed.
- [x] #351 READINESS TRACK A0 — design doc for fixed-size value aggregates + checked ownership transfer, co-designed. The two foundations from `rae/docs/language-readiness-for-renderer.md` (§3, §4, Track A). Settle on paper BEFORE implementing: spelling of `[N]T` (const-expression N?), value semantics (trivially copyable when T is primitive, no cascade-drop), indexing bounds policy (checked debug / unchecked release?), whether it lowers to a distinct type kind or a compile-time-length Buffer; AND the container-boundary ownership rules — most importantly whether passing a non-owned value to an `own T` parameter becomes a COMPILE ERROR instead of a silent alias/double-free (the root of the log_overlay, List(Struct) deep-copy and #225 hazards). Deliverable: a short design doc + the test list for #353. Do not write compiler code under this item.
- [x] #352 READINESS TRACK A1 — implement fixed-size value aggregates per #351: parser, sema, C backend lowering to a plain C array/struct by value, ownership integration, drop semantics for heap-element T. Acceptance: a `[16]Float` embeds in a struct, passes by value, stores in List elements, and a transform microbench shows ZERO allocations — but per Track B the REAL exit criterion lives in #356, not here. DONE as `Array(T, cap: N)` (spelling revised in #351 rev 2/3) across four slices: (1) compile-time value generic arguments via an interned TYPE_CONST_INT, folded through sema's existing const_eval; (2) TYPE_ARRAY lowering to `struct { T v[N]; }` — never a bare C array, which would decay to a pointer and reintroduce aliasing — plus indexing and compile-time constant bounds checks; (3) drops exactly when the element type does, per-element loop for Array(String), ZERO drop code for Array(Float); (4) List(Mat4) with verified value-copy into the container. 194 compiler cases + 70 examples green. Remaining gap filed as #361.
- [x] #354 READINESS TRACK A3 — value math types on top of #352: `Vec2`, `Vec4`, `Quat`, `Mat4`, `Mat3x4` as allocation-free value types in lib (Vec3 exists; keep its API shape). Mat3x4 matters: affine transforms need no fourth row and the reference engine ships mat3x4 across its render boundary for 25% less transform traffic. Port `lib/math3d.rae` OFF List(Float) as a consequence — note per the doc this port is NOT the exit criterion, #356 is. DONE: Mat4 = Array(Float, cap: 16) in a struct, Mat3x4 with converters both ways, and Vec2/Vec4/Quat as named-field structs (matching Vec3 — Array is for large homogeneous counts, not 4 components, so #361 does not block this). mat4Mul and modelMatrix verified allocation-free in the emitted C. gpu3d stages through two process-lifetime scratch lists at the extern boundary; retyping the externs to `view Mat4` is a deliberate follow-up. Filed #362 (cross-module symbol collision) hit on the way.
- [x] #355 READINESS TRACK C — cheap unblocking language items, safe to do anytime/in parallel: (a) `continue`/`break` (only subtlety: interaction with defer + scope-exit drops), (b) `List.removeLast()`/`truncate(len)`/`removeAt(i)` running element drop via the existing rae_ext_rae_buf_drop_at path, (c) `popcount`/`leadingZeros`/`trailingZeros` intrinsics. Exit: the ring-buffer workaround in lib/ui/frame_stats.rae and the loop restructurings in 110 can be (and are) deleted.
- [x] #360 Compiler: `rae format` SIGSEGVs on ANY module-level binding — `let`, `var` AND `const` (my original note claiming let/var were unaffected was wrong; all three share AST_DECL_GLOBAL_LET). pp_print_decl had no case for that kind, so it fell through to pp_print_func_decl and read func fields out of a let union. FIXED: added pp_print_global_let_decl. Root cause of the long survival: lex/parse/format/pack tests were skipped on the compiled target — they are frontend commands that used to run under the now-frozen live target, so 32 cases silently stopped executing. Those are re-enabled in the same change.
- [x] #361 Compiler: a raw `Array(T, cap: N)` in a generic type-argument slot (`createList(Array(Float, cap: 4), cap: 2)`) does not resolve — type params live outside `fd->params`, so the call never matches the overload, and it fails as a wall of C compiler errors instead of a Rae diagnostic. Consequence of #352's type-doubles-as-constructor rule. Options: (a) classify a call argument as a type when it fills a `T: type` slot, (b) require a named wrapper type (works today, what the renderer wants anyway), or (c) type aliases `type Vec4 = Array(Float, cap: 4)` — which also gives #354's math types their natural spelling. Add a proper diagnostic either way.
- [x] #362 Compiler: two functions in DIFFERENT modules with the same name, arity and parameter types mangle to the same C symbol — `vec4.create(x,y,z,w)` and `quat.create(x,y,z,w)` collided as `rae_create_rae_View_Float_x4` ("conflicting types"). The mangler includes neither the module nor the return type. Worked around in #354 by renaming quat's to `fromXYZW`, but this will keep biting as lib/ grows value types with parallel APIs, and it silently constrains every module to globally-unique signatures. Fix: include the defining module in the mangled symbol. DONE: defined functions mangle as rae_<module>_<name>_<params>, extending the pattern namespaced stdlib externs already used. Externs untouched (they name real C entry points); the entry/project module has no module_name so `main` and single-file programs are unchanged. quat.create restored. 229 cases + 70 examples green.
- [x] #363 Compiler: the own-argument rule (#353) cannot see generic-container method calls — `list.add(value: someViewParam)`, the log_overlay shape it was designed around. Sema never binds those calls to a decl (`decl_link` is NULL), the same structural limitation that forced #349's fix into the backend; the method-table, generic-overload and UFCS-rewrite paths were all hooked and none see them. Two routes: (a) resolve generic methods in sema — the work that regressed eight tests during #349 — or (b) a second check in c_call.c where the callee IS resolved, trading the doc's "sema runs on all paths" rationale for actually catching the main hazard. DONE via (b): narrow backend check (bare view/mod into `own`), `own x` as the explicit per-argument escape, and NO stdlib exemption — which found four real latent double-frees in lib/string.rae, lib/rendergraph.rae (x2) and 106.
- [x] #382 Shadows M1 — cascaded shadow map, minimum viable, per `rae/docs/shadow-system-design.md` §8.1: 3 ortho cascades for the sun, TEXEL-SNAPPED in world space (the anti-shimmer rule, and the thing that reads as "pixelation" if omitted), fitted to frustum-slice bounding spheres so resolution does not change with camera yaw, normal-offset bias rather than depth bias alone, one stochastic sample per pixel at a fixed radius, depth-aware spatial denoise, resolved into a full-res r8unorm shadowMask that multiplies DIRECT light only. Gate: the sphere in example 110 stops hovering — that is #350's motivating bug. Do NOT use reverse-Z for the cascades (ortho depth is linear; it buys nothing). Blocked on #359.
- [x] #384 Shadows M3 — variable penumbra and temporal denoise (design doc §3 A2/A3): blocker search sets the sample disc radius from the sun's true 0.53 degree angular diameter, and the mask accumulates through the existing TAA history plus the shared depth-aware blur from #372. Gate: measured penumbra width grows linearly with blocker distance and matches the 0.53 degree prediction within a few percent — a 2 m blocker gives ~1.9 cm. Needs shadow-specific history rejection on mask delta; the colour clamp alone ghosts on moving occluders.
- [x] #387 DEFERRED MIGRATION, blocker 1 — SSAO on the deferred frame. #337 writes indirect into the forward frame's separate ambient target; the G-buffer has no equivalent, so the deferred lighting pass must produce an indirect term SSAO can attenuate without touching direct light (docs/shadow-system-design.md §5 invariant 2). Slots between lighting and composite on hdrColor.
- [x] #388 Example gates must assert SHADOW CASTER COUNTS, not just non-blank pixels. A shadow pass that silently draws nothing still produces a lit, non-blank frame, so 110's and 113's gates would stay green through a total regression — the exact false-green this arc has been flagging for three commits. Log the caster count from both examples and grep it, the way 113 already pins its panel button counts.
- [x] #389 DEFERRED MIGRATION, blocker 2 — UI overlay pass in renderer_deferred. 110 and 113 both need a load-preserving tagUiOverlay between composite and present, declared with passModifies so the graph derives it rather than asserting it, exactly as renderer3d does.
- [x] #390 DEFERRED MIGRATION, blocker 3 — TAA and motion vectors on the deferred frame. #335 reads the forward velocity target; the G-buffer already packs motion into its C channel (#366), so this is wiring TAA to read from there plus a persistent history resource, not new motion maths.
- [x] #391 DEFERRED MIGRATION, blocker 4 — skinned geometry writing the G-buffer. IMPLEMENTED but UNEXERCISED: the pipeline, palette binding and draw path exist and compile, but no example feeds skinned geometry to the deferred frame yet, so nothing has rendered through it. Stays unchecked until #393 migrates 113 and proves it on a real character. Original scope: #374's pipeline targets the forward pass's four attachments; it needs a G-buffer variant writing the same packed layout, sharing the joint palette. Without it example 113 cannot migrate at all.
- [x] #392 DEFERRED MIGRATION, blocker 5 — SDF/metaball G-buffer path. IMPLEMENTED but UNEXERCISED, like #391: the raymarch pipeline writes the packed G-buffer and reverse-Z frag_depth and compiles, but 111 has no SDF primitives and 110 is still on the forward path, so nothing has rendered through it. Stays unchecked until #393 migrates 110 and proves it against real metaball clusters. Original scope: Example 110 leans on smooth-union metaball clusters, which today raymarch in a forward material shader. The deferred equivalent writes the packed G-buffer (albedo/normal/material/motion + frag_depth) from the raymarch. Largest of the five, and it shares machinery with Layer C shadows (#386).
- [x] #393 DEFERRED MIGRATION — 110 and 113 both migrated and proven. Move examples 110 and 113 onto renderer_deferred once #387-#392 are all done. Gate: both look at least as good as their forward screenshots, shadows still land, and their example gates pass. Do not migrate an example while any gap on its list is open; a half-migrated example is worse than an unmigrated one.
- [x] #396 DEFERRED TOON SHADING mode, plus a persistent style toggle in example 113. Deferred is the right place for it: the G-buffer already stores surface attributes independent of how they are lit, so a shading STYLE is a different read of the same buffer rather than a different set of material shaders — which is exactly the separation the forward path could not offer. Two ways in, and the choice matters: (a) a PER-MATERIAL mode, using the one free slot in the G-buffer's 2-bit material_mode field (LIT=0, EMISSIVE=1/3, UNLIT=2/3 are taken; 3/3 is free), so toon and PBR objects can coexist in one frame; or (b) a WHOLE-FRAME lighting-pass switch via a uniform, which is one branch and no G-buffer change but cannot mix styles. Prefer (a) — it costs no extra bits and the mixed case is the interesting one. Toon itself: quantise NdotL into bands, keep the shadow mask multiplying direct only (§5 still holds — a banded shadow edge must come from quantising the LIGHT, not from quantising the mask), specular as a hard thresholded highlight, and a rim term. NO edge outlines in this task: they need a depth/normal discontinuity pass and are their own increment. Example 113 gets a style toggle button in its control panel, and the choice must survive a hot reload — it goes in the #377 state line, which means bumping state113Version.
- [x] #397 Deferred TAA sub-pixel JITTER, the missing half of #390. Without it TAA is a temporal blur rather than antialiasing: every frame samples the same point inside each pixel, so accumulating frames adds no new information and only risks ghosting. Add a Halton(2,3) clip-space offset to the G-buffer frame uniform and apply it AFTER o.clipNow is captured — motion must stay unjittered or every static pixel appears to move. The forward path does exactly this in runtime_gpu3d.c; the constants and the sequence can be lifted directly.
- [x] #398 Optimise the metaball shadow caster (#392 follow-up). It works but costs: each cluster is a FULLSCREEN raymarch per cascade, so example 110 runs 3 clusters x 3 cascades = 9 full 2048x2048 marches per frame and drops from ~23 to ~16 fps. Two obvious bounds, in order of value: (a) restrict each cluster to the screen extent of its bounding sphere projected into the cascade, instead of a fullscreen triangle — most of a 2048 map is empty for a 2-metre blob; (b) skip distant cascades for small clusters, since a blob smaller than a few far-cascade texels contributes nothing. Neither changes the image, so both are verifiable by byte-comparing a headless screenshot before and after.
- [x] #399 SKY M0 — the Sky interface (docs/sky-system-design.md §5) plus the existing hemisphere constants as its fallback. No visual change; the acceptance test is that lighting output is IDENTICAL before and after, which is what proves the seam is a seam.
- [x] #400 SKY M1 — procedural physical sky (Preetham/Hosek-Wilkie) from a sun direction. Asset-free, animatable (time of day, which a static HDRI cannot do), and testable in CI with no binary dependency. Assert zenith bluer than horizon and the sun disc where it was placed.
- [x] #406 DEVTOOLS multi-run — run and WATCH several example apps concurrently, with a fixed running-apps dock (Open / Restart / Stop per app, Stop all) that stays visible across tabs and survives reload. Server keeps a Map of runs; stop is per-run.
- [x] #407 106 idle churn part 2: with the progress-fill write fixed, an idle app STILL trips renderCacheDirty every frame in some launches (ComputedRect+WorldTransform rewritten wholesale) and not others — the difference tracks restored state (.rae/state.json / which page is mounted), which is why it looks unreliable. Find the remaining per-frame component write; the loop trace now prints decide/assets/hover.
- [x] #408 SEMA HOLE: the no-implicit-numeric-conversion check is skipped when the value comes from a `view` or `mod` parameter (or a field reached through one). `func f(now: view Float64) { let e: Float = now }` and `s.field = now` through `mod` both compile silently; the same code with a local, plain or `copy` param is correctly rejected. This is what let the four epoch-timestamp narrowings of #407 through — every one was `now: view Float64` into a `Float` field. Fix the check to run on reference-mode sources, then re-run the suite: the silent narrowings it starts catching are exactly the bugs worth finding.
- [x] #409 WITHDRAWN as filed (`as` on a parenthesised expression): the real cause was arithmetic and casts not seeing through `view`/`mod`, fixed with #408. Parens were irrelevant.
- [x] #410 SEMA HOLE: call ARGUMENTS are not conversion-checked. `f(d: someFloat)` into a `Float64` parameter compiles silently, as does a Timestamp into a Duration parameter — the `ensure_type_match` call in the argument loop either does not run for same-module calls or runs before the argument has a resolved type (debug showed `val=NULL` rows). Pre-existing and independent of #408; a function boundary is where caller and callee are most likely to disagree about what a number means. Expect fallout in lib/examples when it starts firing.
- [x] #411 Timestamp/Duration landed as ORDINARY Rae types in lib/time (two `type` definitions with a Float64 `seconds` field, operations as `this`-taking functions for dot-calls, no operator overloading, no compiler machinery), and example 106's Spotify poll migrated — the field the #407 bug lived in. A first attempt made them compiler-level nominal scalar types; that was the wrong direction and was reverted in full. Remaining adoption is #413.
- [x] #414 SEMA HOLE: struct assignment compatibility is not checked. `var t: Timestamp = ...; t = someDuration` passes sema and is caught only by the C compiler, against generated code — so the diagnostic names a .c line instead of the .rae line. Affects every user-defined type, not just Timestamp/Duration; same family as #410. Fixing it improves diagnostics for all Rae types at once.
- [x] #415 C-BACKEND GENERIC GAPS found porting the sky: (a) `List(List(Float))` does not monomorphize; (b) `createList(T)` written directly in a struct-literal field emits an unmonomorphized `rae_createList()`; (c) the no-capacity `createList(T)` spelling does the same. All three pass sema and only fail in gcc, so a legal-looking spelling produces broken C — (c) especially, since `createList(Float, cap: 0)` works and `createList(Float)` does not.
- [x] #419 in 114 example, many files are named like ”walker_grass.rae”. Don’t usually put example (or app) names into filenames of type/struct/system names. These will often be re-used and shared and often moved to stdlib, so more generic names make more sense also in filenames.
- [x] #420 Would adding a switch or match pattern be usefull for app architecture in Rae and 114 etc. Investigate if Rae already has switch or match and check where this would be good. - Also i hear many times ”no continue in Rae”. I think we need break and continue possibly, but they have memory drop affects? - Then suggest the best syntax and rules for switch/match, affected by Rust/Swift/Odin/Jai and other modern languages. I think we want to make it a hard error to not have a switch case handled. But we want it modern in other ways too. Sum types might be needed and error handling too? A result type? Tuples?
- [x] #433 SUPERSEDED by #483: `491_wait_events_smoke` exercised the retired Raylib path; archived Raylib code is non-gating and the default compiler no longer links it, so the initialization crash is intentionally not being debugged
- [x] #435 Browser WASM: the settings dialog fails to mount in 111 and 112 (log says "settings dialog failed to mount", panel renders empty); same in both, so it is a shared browser-path limitation, not renderer-specific
- [x] #437 Example 49 errors in the embedded WASM window and the 3D WASM examples fall back to the external Chrome window; find why the embedded path rejects them (46 is reportedly the only one that works embedded) — RESOLVED: the viewer canvas was not named "canvas", which Emscripten special-cases; any other id makes the WebGPU surface present nothing silently. Example 49 still worth a separate check.
- [x] #440 Delete the stray tracked file `rae/{}.tmp` (39 KB of duplicated example source, committed by accident in 95ea117, Jan 2026) — confirm nothing references it first
- [x] #441 Second stray tracked artifact: `rae/compiler/src/c_backend_any.c.tmp` (6 KB, Feb 2026) — no c_backend_any.c counterpart exists and no Makefile or source references it, so it looks like the same class of accident as #440; confirm and delete, and consider a `*.tmp` gitignore guard to stop a third
- [x] #442 [opt/alias arc 1/6] Implement `opt view T` / `opt mod T` per spec 4.1: permitted in parameters, return types and local bindings, bound with `=>`, excluded from type members by 4.3. Today the Compiled target accepts them and emits C that will not build, while the frozen VM rejects them outright (vm_compiler.c, "opt view/mod not allowed") — that rejection must go, it enforces a rule the spec no longer has. Tests 382_reject_opt_alias and 385_reject_opt_alias_return pin the removed rule and must be retired or inverted in the same change
- [x] #443 [opt/alias arc 2/6] `=>` onto a produced value must materialise. Per spec 2.3, `let a: view T => f()` is semantically valid whether or not f returns a place; the implementation must give the value storage for the binding's lifetime. Today accepted and emits broken C (same for `list.at(index: 0)`). Functions returning `view T` / `mod T` already work and must keep working
- [x] #444 [opt/alias arc 3/6] Fix `view`/`mod` locals of primitive type. `let v: view Int => x` binds, but reading it back (`let r: Int = v`) or interpolating it emits broken C — rae_View_Int64 passed where int64_t/rae_String is expected. Struct views already work
- [x] #445 [opt/alias arc 4/6] Enforce the generalised 2.2 rule: `=>` is REQUIRED, not merely permitted, whenever the target type contains `view` or `mod` at any level — including `opt view T` / `opt mod T`. This is what stops `=` ever producing a shared handle instead of an independent value. Needs a diagnostic on `let x: opt view T = …`
- [x] #446 [opt/alias arc 5/6] Implement `if let` per spec 4.2: `if let x: view T => optExpr` / `if let x: mod T => optExpr`, binding scoped to the if-branch, bind-once, `=` REJECTED with a diagnostic citing 2.3. Narrows `opt T`, `opt view T` and `opt mod T`. Depends on 2/6 and 3/6, since the common right-hand side is a call like `map.get(k:)`
- [x] #447 [opt/alias arc 6/6] Two loose ends: the spec/implementation divergence on assignment through a `let`-bound mod alias (spec 2.2's example says legal, the compiler demands `var`), and `is some`, which parses but fails with "use of undeclared identifier 'some'" — implement it as the mirror of `is none` or remove it. Also decide whether `match` gains a `none` arm
- [x] #448 Pre-existing before the opt/alias arc and still failing: 338_reject_ref_to_local and 339_reject_ref_to_temporary. Both are about references that DANGLE — bound into a local, or into an argument temporary that dies with the call — which is a lifetime check the compiler no longer performs. Distinct from 443's materialisation, which gives a binding's OWN value storage
- [x] #449 `if let` cannot yet narrow a VALUE optional: `if let t: view T => f()` where f returns `opt T` (a RaeAny box) emits `&(...).as.ptr` and fails to build. The binding must point at the box's PAYLOAD, not the box. I tried two fixes in c_stmt.c's ref-bind paths and neither branch was the one taken — find the actual emission site first. Optional REFERENCES narrow correctly (603). Until then the failure is a C error, not a Rae diagnostic, and not silent
- [x] #450 `opt T` as a struct FIELD plus `if let` emits broken C — same payload-narrowing gap as the item above, reached through a member access rather than a call
- [x] #451 INVESTIGATED, not a codegen bug: `ret view tracks.at(index: i)` is a genuine dangle, because List.at is declared `ret T` — a VALUE — so the reference points at a temporary that dies with the function. The C error is a correct rejection with a bad message. Split into the two real items below
- [x] #452 stdlib: no List accessor returns a reference. `at` gives `ret T` and `get` gives `opt T`, so every lookup COPIES the element — for a big struct that is the deep copy optional references exist to avoid. Wants `viewAt`/`modAt` returning `view T`/`mod T`. Easier than first thought: no new extern is needed, because `rae_ext_rae_buf_get` lowers to element INDEXING (an lvalue), which is exactly how lib/ui/ecs.rae componentView and componentMod already spell it — `ret view rae_ext_rae_buf_get(buf: this.data, index: i)`. Same for StringMap and IntMap
- [x] #453 Returning a reference to a temporary should be a RAE diagnostic, not a C error. Test 339_reject_ref_to_temporary already pins the wanted message and currently FAILS, together with 338_reject_ref_to_local — the lifetime checks they describe are not being performed. Same root as #448
- [x] #454 Spec 2.3.1: REJECT `let/var view T => <owned-producing expr>` — a call returning T (free or method, e.g. `l.at(index: 0)`) or an object/collection literal. Reverses the materialisation half of #443: a view binding must not invent a hidden owner for a fresh value. Diagnostic must name both honest spellings: `let t: Track = makeTrack()` (move, costs nothing) or a reference-returning accessor (`viewAt` etc, added in #452). Rework test 606: first case becomes a rejection test, second case switches to viewAt. Literal RHS currently emits broken C (`__auto_type` with initializer list), so this also converts a C error into a Rae one — done: sema rejects calls+literals, backend catches UFCS; tests 615/616; 606 replaced
- [x] #455 Spec 2.3.1: REJECT `mod T => <owned-producing expr>` — currently silently ACCEPTED and runs (probe: `let m: mod Track => makeOwned(); m.seconds = 99` prints 99 against a temporary nobody else can observe). Same rule and diagnostic as #454; worse in practice because writes vanish — done with #454 (mod variant pinned in 615)
- [x] #456 Spec 2.3.1: Rae diagnostic for `let t: T = <view/mod-returning call>` — currently emits broken C (initializing rae_Track with const rae_Track*). Message should spell the two-step form: bind the view first (`let source: view T => call()`), then copy from the named binding, so the deep copy is visible next to the declared viewness. Copying from a NAMED view/mod binding stays legal — done: Rae diagnostic spelling the two-step form; test 619; copy from named view binding stays legal
- [x] #457 Spec 4.2 (revised again — construct-ownership rule WITHDRAWN): `if let` applies the 2.3.1 matrix unchanged. Implement: (a) `if let x: T = <call returning opt T>` = ownership transfer of the payload, owned by the binding, dropped at branch end — currently REJECTED (the old blanket '= not permitted' rule) and must be accepted; (b) `if let x: view/mod T => <call returning opt T>` must be REJECTED — currently ACCEPTED via materialisation (test 607 pins this and must be rewritten to the `=` form); (c) `=>` stays valid for reference optionals (opt view/opt mod) and for owned optionals in PLACES (fields/elements/named bindings); (d) rework tests 603/607 and any stdlib/example use of view-narrowing produced owned optionals — grep for `if let .* => .*get(` shapes; List/Map `get` returns opt T (produced) so those sites become `=` or switch to viewGet — done: '=' ownership transfer (moves payload out of the box, branch-scoped drop); the three matrix mismatches rejected in sema + backend (UFCS shape crashed before); tests 617/618; 607 rewritten, 603 header updated
- [x] #458 Semantic liveness tests per the observable-behavior philosophy: pin (a) a `view T` binding observes mutations made through the original while the view is alive, and (b) writes through a `mod T` binding are visible in the original. Both hold today (probed) but nothing pins them; a future 'optimise views to copies' change must fail these tests unless it proves no observable change — done: pinned in test 612 (view sees mutation, mod write-back), plus param-alias liveness
- [x] #459 Writes through a `view` binding must be REJECTED — currently ACCEPTED and they write through to the original (probe: `let v: view Track => t; v.value = 42` mutates t). Read-only is the meaning of view, not a convention. sema knows mutation legality (it already rejects reassigning let bindings), so this is a missing check on member-assign through a TYPE_REF whose ref is a view — done: member/index writes and assignment through view bindings rejected, nested access walked to base; tests 613, 357/486 updated
- [x] #460 `let y: mod Track => x` where x is a `view Track` PARAM must be REJECTED — you cannot mint mutable access from read-only access. Currently accepted and the write silently VANISHES (worst case: no error, no effect). Diagnostic: cannot take mod of a view — done: sema rejects minting mod from any view shape; test 613
- [x] #461 `let y: mod Track => x` where x is a `mod Track` PARAM must write back to the caller — currently accepted and the write silently VANISHES (probe: mutate() through the alias leaves caller at old value). Aliases of LOCALS write back fine (test 604 passes); the param case likely aliases a copy — same double-indirection bug class as the #452 binding fix. Related memory: mod String/heap write-back no-ops — done: binding emitter aliases the referent when the source is already a reference (struct ptr and primitive .ptr); test 612
- [x] #462 `var y: view Track => t` is currently ACCEPTED, but aliases are bind-once (rebinding `y => other` is already rejected), so the var-ness is unreachable — a lie in the source. Either reject `var` with `=>` (recommended: bind-once means aliases are always `let`) or define alias rebinding; pick one and pin it — done: parse error, aliases are bind-once; test 614; six older var-alias tests flipped to let; example 90 fixed
- [x] #463 GATE before next public push of rae/: the README 'Refactoring stability' section claims three behaviours the compiler does not yet enforce — writes through a view rejected (#459), mod-of-view rejected (#460), mod-alias-of-mod-param writes back (#461). Land those three first, and rerun the README's own examples as probes, or soften the section. Written ahead of the compiler deliberately (2026-08-14, not pushed) — done: all five README code blocks re-run as probes against the enforcing build — outputs and errors match the README exactly
- [x] #466 FINDING (bounds-design investigation): the range loop `loop x: T in collection` is fully PARSED (parser.c is_range) but lowered only in the frozen VM (vm_emit_stmt.c); the C backend ignores is_range entirely, so the syntax is dead on the supported target. It is the natural check-free iteration form — implemented for Compiled with value/view/mod bindings, single collection evaluation, direct data iteration, and loop-body mutation rejection
- [x] #471 Example 04_random — remove its accidental Raylib/graphics dependency and make it a small deterministic console demonstration of Rae's random API; random-number semantics are worth keeping, but a window full of random circles duplicates GPU2D examples and is not worth porting.
- [x] #472 Example 90_raylib_basic — ARCHIVE, do not port: its entire purpose is demonstrating Raylib window/drawing calls, while SDL3 presentation is already demonstrated by raytracer 40 and GPU rendering by 101/104; move it to `examples/legacy/raylib/90_raylib_basic` with its README and remove it from active screenshots/catalogues.
- [x] #474 Example 92_pong_import — ARCHIVE as redundant rather than porting a second Pong: 91 remains the game, while explicit imports and `.raepack` behavior already have focused examples/tests (13_multifile and 22_raepack_demo); move the complete source to `examples/legacy/raylib/92_pong_import` and update the package docs that currently cite it.
- [x] #475 Example 93_raylib_3d — ARCHIVE, do not mechanically port: its moving primitive scene exists to showcase Raylib's immediate 3D API and is superseded by Rae renderer examples 109-114; move it to `examples/legacy/raylib/93_raylib_3d` and point readers to the minimal current Rae 3D example instead.
- [x] #480 Example 100_code_hot_reload — remove the unused `open raylib`; it is already a console/file-watcher example and needs no SDL3 or renderer migration, then gate that it builds without linking Raylib.
- [x] #485 GRASS phase 1 — GPU INSTANCING capability. Add a per-instance storage buffer + an instanced GBuffer pipeline variant whose vertex shader reads @builtin(instance_index), and one DrawIndexed(icount, instanceCount=N). Expose a reusable Rae API (e.g. drawInstanced / an instance-buffer handle) designed to be reused by vegetation, rocks, particles, crowds — NOT grass-specific. Verify by instancing the existing terrain props (rocks/trees) as a proof, and confirm 110-114 still render. Foundational: #486-#495 all depend on this. BUILT IN RAE over the bindings per #506 — the instanced pipeline variant, the per-instance storage buffer, its bind-group, and the single DrawIndexed(icount, instanceCount=N) are all created + encoded from Rae (lib/gbuffer.rae / a new lib/gbuffer_instanced.rae); no new C helper (gate: make c-surface-gate). DELIVERED: the instanced pipeline reading draws[instance_index] already existed (#502), so phase 1 is the reusable API — lib/gbuffer_instanced.rae InstanceBatch (createInstanceBatch/resetInstanceBatch/addInstance[+Transform]/drawInstances), zero per-frame alloc. PROOF: 114's 49 per-cell rocks now draw as one DrawIndexed(instanceCount=49) (walker_terrain.drawTerrainRocks); rock MeshRenderers hidden so renderScene skips them, rock shadow casters re-supplied (drawTerrainRocksShadow, casters 355->404). Gate green, 114 builds/links/runs clean. EXTENDED: ALL terrain props (trees broad/tall, rock, bush, flower, grass) instanced via PropInstancer bundles — ~8 instanced draws replace ~392 per-cell draws, shadows re-supplied (casters 404). ALSO skinned instancing: drawSkinnedRecords/drawSkinnedInstances (skin shader applies model after palette) + a 20-clone idle crowd in 114 (one instanced draw per part, shared palette=shared pose; casters 644). Caveat: single shared palette means no per-instance animation phase without an array-of-palettes mechanism. VISUALLY CONFIRMED in 114 (props + 20-clone crowd). Fix landed: clones were anchored at world origin but 114 restores the hero's saved position after clone setup, so they were stranded off-screen for a moved player; now anchored to characterController.position after restore. Follow-up (optional): auto-batch renderScene by (mesh,material); array-of-palettes for independent skinned poses.
- [x] #486 GRASS phase 2 — FRAME-INTEGRATED COMPUTE + generation. Add a persistent compute pipeline recorded into the 3D frame's command encoder (no per-call encoder, no CPU readback, no map), reusing the render-graph's compute node kind. Write the phase-1 instance buffer on the GPU from terrain height (walker_terrain terrainHeightAt / a height source the renderer can sample) + deterministic hash/noise + biome params. No ECS entity per blade. DELIVERED: lib/grass_compute.rae — a compute pass in its own encoder submitted before the geometry pass writes one DrawU record per blade (24x24 grid around the player, hash-jittered, terrain height via a WGSL perlin2 port matching terrainHeightAt, wind sway) into a dedicated storage buffer; grassDraw issues one instanced DrawIndexed on the static pipeline with a bind group pointing at that buffer. No CPU per-blade work, no readback. New C is shader-source (rae_gb_grass_wgsl/entry) + a rae_gb_grass_set/get pointer-slot store (both rae_gb_ ABI seam, gate-clean). Verified headless (grass on terrain, no validation errors); awaiting visual confirmation. CPU walker_grass stays as render-dirty signal until #494.
- [x] #487 GRASS phase 3 — PROCEDURAL BLADE GEOMETRY + WIND + INDIRECT + STREAMING. Generate blade geometry in-shader (bent strip / crossed planes, low vertex count) with per-instance height/width/rotation/lean/bend/phase/colour variation; base-anchored progressive bend; coherent WORLD-SPACE wind (global direction + strength + gust noise + per-instance phase + biome wind response) entirely in the vertex shader. Consume a compute-written indirect args/count buffer via DrawIndexedIndirect. Chunk into world cells, stream camera-relative around the player, deterministic so a revisited chunk is identical (no placement pop); distance LOD/density with smooth fade (no rings). Use alpha-test/masking (not transparent blending), correct depth test/write, minimal overdraw. IN PROGRESS: (part 1) procedural in-shader blade geometry landed — a dedicated grass render pipeline (lib/grass_compute.rae makeGrassPipeline, 0 vertex buffers, no cull) draws each blade as a tapered quad from @builtin(vertex_index), no mesh; the compute writes per-blade non-uniform scale (independent height + width) and a colour/brightness spread; fragment writes the same G-buffer channels (octahedral normal + motion). (part 2) world-space placement — blades hash on their WORLD cell so the player walks through the field instead of dragging it. WGSL in C (rae_gb_grass_render_wgsl, gate-clean). DELIVERED (all parts): procedural in-shader tapered blades with per-blade sharp/flat tip variation; per-blade height/width/colour; coherent world-space progressive per-vertex wind bend (base planted, tip leans, TAA-correct); world-space cell placement (walk through the field, no pop); smooth radial edge fade; GPU cone/distance CULLING that compacts survivors and drives a DrawIndirect from a compute-written count; distance LOD density thinning; field extends to a 60-unit radius at 262k candidates, 62 fps. Alpha-test masking is N/A for opaque procedural blades. Visually confirmed in 114 (dense far-reaching wind-swept meadow).
- [x] #497 WEBGPU phase 0 — FFI CONVENTION SPEC (the mapping the generator obeys, write before generating): decide the Rae representation for each WebGPU C construct — opaque handles (WGPUDevice etc.) as an opaque Ptr/handle type; enums as Rae enums or Int consts; bitflags (WGPUBufferUsage…) as Int consts + combinators; structs/descriptors as Rae structs with C-identical layout (verify field order/size/alignment); WGPUStringView and (ptr,count) array pairs; optional/nullable pointers; the chained-struct nextInChain (SType) convention; callbacks/WGPUFuture (map*/request*). Follow Rae's ACTUAL FFI/type conventions — do NOT paste C syntax into Rae. Output a short docs/webgpu-bindings.md convention section this and #498/#499 build on.
- [x] #498 WEBGPU phase 1 — BINDING GENERATOR TOOL. A small purpose-built, dependency-light generator (in-repo, e.g. tools/webgpu-bindgen/) that parses webgpu.h + wgpu.h and emits deterministic, reproducible lib/webgpu/webgpu.rae following the #497 conventions. Cover as much as practical: enums, flags, structs, handles, descriptors, function declarations, callbacks, constants, pointer/optional conventions, chained structures. No huge external C-parser dependency if a small robust parser suffices. Document the exact regenerate command (and pin the header version/source). Generated files clearly marked generated; check them in so builds don't require the generator.
- [x] #499 WEBGPU phase 2 — LIST/BUFFER/VIEW ↔ FFI, general language decision (NOT a WebGPU hack). Findings to formalize: List(T)={data,length,cap} contiguous; .data (a Buffer(T)) already crosses as a bare pointer but DROPS length (caller passes count separately); view T today = pointer-to-one-T; view→const*, mod→mutable* already distinguishes read-only at the boundary; wgpuQueueWriteBuffer COPIES synchronously (pointer need not outlive the call → safe zero-copy), whereas mapped/async buffer usage keeps the pointer live (unsafe if the List reallocs). HAZARD: rae_ext_rae_buf_resize→realloc moves memory, dangling any outstanding pointer/span (same class as #467). DECIDE the cleanest GENERAL feature: a length-carrying span / `view [T]` (ptr+len fat value) obtainable from a List without copy, read-only vs mutable distinct, plus a stated temporal rule (mutating/resizing the container invalidates outstanding spans) and ideally a debug-mode revision check. Answer all 11 questions in docs/webgpu-bindings.md; if view T alone suffices, justify; if List can cross whole, pin its exact ABI.
- [x] #501 WEBGPU phase 3 — DEVICE/QUEUE/SURFACE bootstrap over bindings. HANDOVER: read docs/webgpu-bindings-handover.md FIRST — #497 (FFI features: extern("symbol"), cheader, c_struct, Ptr, UInt8/16) and #498 (rae bindgen + generated lib/webgpu/*.rae) are LANDED (commits 21fff5f6, e54d24c1, 5f5a58b8). Now: make the build auto-detect webgpu usage (a cheader / importing lib/webgpu) and pass the wgpu-native include+lib flags automatically — today that is still manual (generalise the uses_webgpu detection in main.c ~2340). Then expose the existing device/queue/adapter/instance/surface (and the frame's current command encoder / swapchain view) to Rae so Rae code can call createBuffer/createTexture/createRenderPipeline/createBindGroup/createCommandEncoder/beginRenderPass/etc. directly. Keep genuinely-platform surface configuration and instance/adapter/device REQUEST (async) in C; everything downstream becomes Rae calls. Verify a trivial Rae-constructed buffer + queueWriteBuffer round-trips, and add a headless harness test that imports webgpu.
- [x] #502 WEBGPU phase 4 — PROOF PORT: eliminate drawRecords. Reimplement the instanced GBuffer draw entirely in Rae over the bindings — Rae owns the draws storage buffer, uploads via a queueWriteBuffer binding (zero-copy from the records span), sets pipeline/bind group/vertex+index buffers and issues drawIndexed(icount, N, 0,0, base) through bindings. Remove rae_ext_gbuffer_drawRecords (and its stub). Preserve identical behavior on example 114. Establishes the pattern for the rest.
- [x] #503 WEBGPU phase 5 — MIGRATE GBuffer geometry pass to Rae over bindings: pipeline/bind-group/buffer/texture/sampler creation and render-pass encoding for the gbuffer; WGSL stays as source (Rae string constants + createShaderModule). Eliminate draw/drawSkinned (keep only genuine ABI/platform/shader-infra in C). Verify 110-114 unchanged.
- [x] #504 WEBGPU phase 6 — MIGRATE remaining passes to Rae over bindings: deferred lighting/ssao/taa/composite/depth-pyramid, shadow, sky, sdf/metaballs, and the gpu2d frame path; move render-graph orchestration, resource management, command encoding into Rae. Shrink runtime_gbuffer_stubs.c and the gpu3d/gpu2d C to genuine ABI + platform + shader infrastructure only.
- [x] #505 WEBGPU phase 7 — FULL C-SURFACE AUDIT + GATE + report. Enumerate every rae_ext_ renderer function; for each ask "genuine WebGPU/native ABI, or renderer logic that belongs in Rae?"; confirm no bespoke high-level graphics C helper remains (drawInstanced/drawRecords/GBuffer ops/material packing/matrix/render-graph all in Rae). Add a guideline + a gate rejecting NEW renderer-specific C functions (so future needs — compute pipelines, indirect draws, storage buffers, texture arrays, query sets, timestamps — are reached through the bindings, not a new C helper). Verify zero-copy upload path has no needless List→temp-C-array→memcpy→upload; document any copy that is legitimately required (async/mapped/lifetime).
- [x] #506 WEBGPU phase 8 — RE-SEQUENCE GRASS on the bindings. Gate grass #486+ (frame-integrated compute, storage-buffer instance data, DrawIndexedIndirect, GPU culling) to be expressed through the Rae WebGPU bindings with NO new renderer-specific C helper; update #484/#485's notes to reference webgpu.rae/gpu.rae instead of adding externs. Grass resumes only after #502 (proof port) lands so it is not built on the to-be-removed drawRecords.
- [x] #507 BUG: List(Int32) codegen is broken — a `List(Int32)` literal lowers to `rae_List_void` and references an undeclared `rae_List_int32_t` (compile fails: "unknown type name 'rae_List_int32_t'"). Only Int64 lists monomorphize. Fix the Int32 (and likely other fixed-width) List monomorphization in the C backend so `List(Int32)`/`List(UInt32)` etc. work; add a test. Surfaced during #501 (worked around with List(Int)); WebGPU vertex/index/instance upload buffers will want Int32/UInt32/UInt16 lists.
- [x] #508 WEBGPU groundwork — List(c_struct) codegen: the container deep-copy/alloc helpers spelled a c_struct element with its mangled name (rae_WGPUColor) instead of the bare C name (WGPUColor), so List(<c_struct>) failed to compile. Fixed in c_backend.c (element C-type positions use the bare c_struct name). Unblocks building WebGPU descriptor arrays (WGPURenderPassColorAttachment[], bind-group entries, vertex layouts) in Rae for #503/#504. Test 631_list_cstruct.
- [x] #509 WEBGPU groundwork — c_struct fields whose real C name is a Rae keyword: the bindings generator escapes them with a trailing underscore (WGPU*.view -> view_), but field access and compound-literal designators emitted the escaped name, so C rejected `.view_`. Fixed in c_expr.c: for c_struct fields, strip one trailing `_` when the remainder is a reserved keyword (exact inverse of the generator's safe_name). Unblocks building render-pass/bind-group descriptors in Rae for #503/#504.
- [x] #510 WEBGPU groundwork — bindgen skipped 3 handle-returning entry points (wgpuRenderPipelineGetBindGroupLayout, wgpuComputePipelineGetBindGroupLayout, wgpuCommandEncoderBeginComputePass): a parse glitch in the full header left the return type's handle name unregistered, so map_type failed and the function was dropped. Added a fallback: an unmapped non-void return naming a WGPU type is a handle -> Ptr. Regenerated lib/webgpu; functions 223 -> 226. Unblocks Rae-side bind-group creation (needs the pipeline layout getter) for #503 resource creation.
- [x] #511 BUG (shadows): in 114 (and any deferred scene) directional shadows VANISH while the sun is up (~06:45–17:12) and only appear when the sun is below the horizon — inverted. Root cause is the cascade depth-range fit in lib/shadow3d.rae (~line 165-193): the light view is a PURE ROTATION anchored at the world origin (eye=0), and the ortho near/far come from `centreLs.z` (scene centre transformed into light space) as zNear=-centreLs.z-radius-backExtent, zFar=-centreLs.z+radius. When the sun is HIGH the light travels DOWNWARD, so the origin-anchored down-looking light camera has the scene centre BEHIND it → centreLs.z > 0 → both z values go negative → the `if zNear < 0.01 { zNear = 0.01 }` clamp pins near but leaves far possibly < near → degenerate/empty ortho depth range → the cascade renders nothing → no shadows. When the sun is DOWN the light points up, the centre is in front, the range is valid, so shadows appear (wrongly, at night). FIX (needs visual verification): make the depth range enclose the centre regardless of the sign of centreLs.z — e.g. anchor the light eye along the light axis behind the centre (eye = centre - light*backDist) so depth is always positive, OR shift near/far together to keep near>=0.01 AND far>near AND still bracket [centreLs.z-radius, centreLs.z+radius]. Verify shadows are present and correct across a full day cycle (noon included) in 114/110, and that texel-snapping (test 579) still holds. Not a #504 migration item; a standalone correctness fix.
- [x] #512 WEBGPU #504 (part 8): gpu2d 2D frame command lifecycle moved to Rae (lib/gpu2d.rae) — beginFrame/beginFrameLoad create the encoder + offscreen colour render pass over the bindings; endFrame flushes the batched primitives, submits, and hands the platform present/screenshot tail back to C. C keeps rae_g2d_frame_reset (batch counters), rae_g2d_set_frame/pass/encoder/frame_active accessors, rae_g2d_present_and_cleanup, rae_g2d_tick, and the flush() draw batching (genuine platform/ABI). Verified example 102 + 114 build+link+run clean. Bug found+fixed en route: set(index) is a no-op past List.length, so the one-element colour-attachment list must be grown with add() first (as gbuffer.rae does) or the pass is malformed (CommandEncoderFinish validation error).
- [x] #556 Similar to how Rae has hard error for over 1000 lines files, we want to be very opinionated on variable names. I think single letter like ”t” for time or (and possibly even 2 letter abbreviations like ”dt” for deltaTime), should be hard errors. Only loops should allow i j k l m n style single letter variables. Investigate how this could be added to the language with some pragma to opt-out line per line or per file or per project. (Not sure about per project pragmas.)
- [x] #621 COMPILER: match `default` by subject type (docs/match-and-sum-types.md). Enum match -> exhaustive, `default` DISALLOWED (error), so adding a variant forces every match to be reviewed. Int/String match -> `default` allowed and REQUIRED (open-ended value space: HTTP codes, commands, file-format tags, ids); an Int/String match with no `default` is an error. Bool is closed like an enum (no default). Migrate the one `default` site (lib/core.rae StringMap.has, an optional) to `is not none`.
- [x] #622 COMPILER: match or-patterns — `case A, B, C { }` (one arm matches several enum variants). Natural extension of Rae's optional commas + `is A or is B`; the recommended companion to disallowing `default` on enums (group variants while still naming every one). See docs/match-and-sum-types.md Proposal B. Do NOT add guards/range-patterns/leading-dot (rejected there).
- [x] #623 COMPILER: break / continue in `loop` (docs/match-and-sum-types.md Proposal A). Reuse the existing early-`ret` ownership-drop machinery (compiler/src/ownership.c): `break` drops `own` values in scopes up to the loop then exits; `continue` drops the current iteration's `own` values then jumps to next iteration. Reverse-construction drop order, same as scope exit. Removes `var done` flag-guard patterns.
- [x] #624 Dispatch enums + exhaustive match in 114 (no language change): make the deferred render-pass tags a real `enum RenderTag` (today plain Int consts in lib/renderer_deferred.rae) and the clip/mode dispatch `ClipKind`, then convert the `if tag is X` / `if clipHit.equals(...)` chains to exhaustive `match`. Payoff: adding a pass/clip becomes a compile error until every site handles it. Do as part of the 114 systems refactor (#543). [Done: RenderTag f49be6b3, ClipKind + compiler drop-scope fix follow-up.]
- [x] #625 COMPILER: add Int and String literal `case`s to `match` — e.g. `match code { case 200 {…} case 404 {…} default {…} }` and `match cmd { case "get" {…} default {…} }`, for open-ended dispatch (HTTP codes, commands, file-format tags, ids). Today a `case` value is an enum member (or `none`). Open-domain subjects (Int/String) require a `default` and disallow exhaustiveness (paired with #621). String cases compare by value (rae_str_eq).
- [x] #626 COMPILER BUG: a `let` of an OWNED value inside a `match case` mis-scopes its drop. The match-statement C codegen (compiler/src/c_stmt.c AST_STMT_MATCH, ~line 2045) emits each case body without opening a drop scope, so an owned local declared in a case gets its `rae_drop_*` emitted at the ENCLOSING scope → invalid C ("use of undeclared identifier"). Fix: mirror emit_if per case body — save local_count, emit case stmts, emit_implicit_drops_for_body(saved), restore local_count, before the case's closing brace. Check the match-EXPRESSION form (AST_EXPR_MATCH) too. Surfaced by #624; worked around there by extracting renderShadowPass (owned ShadowCascades).
- [x] #627 COMPILER: automatic enum toString + Int↔enum casts (follow-on from #624). Enum names resolve to Int in sema, so `enumVal as Int` and `Int as Enum` both type-check via the numeric-cast path and enums interoperate with Int storage; exhaustiveness is unaffected (it keys off case patterns, not subject type). `.toString()` and `{enum}` interpolation emit the member name via a generated `rae_enum_toString_<Enum>` — no hand-written index→name tables (deleted clipButtonId/walkerPassName in 114). Tests: 632_enum_int_cast; enriched examples/03_enum; 360/361/413 goldens now show names. [Done.]

## Archived 2026-08-27 (opt/reference/container semantics epic #641–#661)

- [x] #641 Inline representation for `opt T` (drop the RaeAny box). Motivation: `opt T` for a non-reference value type is realized as a heap-boxed `RaeAny` (malloc(sizeof(payload)) + copy + rae_any_owned_ptr, emitted around compiler/src/c_stmt.c:308-338; C type RaeAny at c_backend.c:603). Only the `if let` peepholes on `at`/`viewAt`/`modAt` avoid the malloc, and they are keyed on library function-name strings (emit_list_if_let, c_stmt.c:32-120), so every new accessor is another shape to recognize and every unrecognized shape is a heap cliff (the §8 bug); the representation is the root cause, the peepholes treat a symptom. Scope: lower `opt T` for non-reference `T` to a per-type monomorphized `struct rae_opt_T { bool has; T value; }` (the mechanism `List(T)` already uses); `opt T` needs a drop only if `T` does (type_needs_cascade_drop); reserve `RaeAny` for `Any` only. NOTE (verified, corrects the report): `opt view T`/`opt mod T` are ALREADY plain 8-byte nullable reference wrappers (null .ptr = none, c_backend.c:596-603) — they are NOT boxed and need NO change; this task is only about `opt T` for non-reference T. Then delete the name-keyed `at`/`viewAt`/`modAt` peepholes, or demote them to a pure optimization behind a test proving the generated C is equivalent without them, so the accessors become small ordinary functions the C compiler inlines. Acceptance: no `malloc` in generated C for any call shape of `at`/`viewAt`/`modAt`, including `let maybe: opt Particle = particles.at(index: index)` stored and passed to another function; a user function returning `opt Vec3` produces no heap traffic; the report's layout table still holds; peephole code removed or demoted with the equivalence test in place. Deps: none required; if done after #645 the peephole removal is cleaner; blocks the peephole-removal half of #644. DONE 2026-08-26: struct/aggregate `opt T` now lowers to a monomorphized `struct rae_opt_<T>{ rae_Bool has; T value; }` (0 malloc in emitted C for `at`/`viewAt`/`modAt` and for a user `ret opt Vec3`; `opt Vec3`→`struct rae_opt_rae_Vec3{has,value}` verified; case suite 317/0). Scoped to aggregate payloads: scalar/String `opt` stay on the already-malloc-free `RaeAny` union, and the `at`/`viewAt`/`modAt` peephole is kept as a pure optimization. Follow-ups filed: #651 (finish "RaeAny for Any only" — move scalar/String opt off the union), #652 (pre-existing generic-`get`/`at` double-free on heap-owning struct payloads, surfaced here).
- [x] #642 `opt T` to `T` direct assignment is a sema error for every `T`. Motivation: `let particle: Particle = particles.at(index: index)` emits non-compiling C (a RaeAny box assigned into a struct-typed C variable; emit_opt_unbox_suffix, c_call.c:21-59, emits no suffix for struct payloads), while `let count: Int = list.at(index: index)` does NOT trap on none — the scalar path unconditionally reads the union (.as.i/.as.f/...) so a `none` silently yields zero with no diagnostic (this corrects the report's "auto-unwraps trivially": it is a silent-zero bug, not benign). Two different wrong behaviors for one situation. Scope: one rule — assigning `opt T` to a non-optional binding requires `if let`, a default-value operator, or an explicit unwrap that panics; Rae has no default/unwrap operator yet, so PROPOSE the syntax in this task rather than inventing it during implementation (candidates to decide in review: `list.at(index: index) else 0` for a default, `list.at(index: index)!` for a panicking unwrap); the error message must name the fix. Acceptance: scalar and struct forms produce the same sema error with the same message; the silent-zero scalar path is gone; tests for both forms; the report's §8 bug entry closed. Deps: independent of #641. DECISION 2026-08-26 (approved): NO new language features (no `!`, no `else`, no `[]`, no trap/panic, no `.first()`/`.last()`, no enumerate loop). Rule: every optional ends in `if let`; the else branch returns a value the PROGRAMMER wrote, never a compiler-invented one. Investigation (2 classifiers + Array/Buffer probe): the ~299 grep lines are ~48–265 real sites collapsing to ~15–30 distinct changes; 34 are false positives (`Task.get()` joins + already-`opt` map gets — SKIP), dominated by the `ResolvedTheme` struct-of-arrays. Migration (batched by file, in order): (1) parallel-lists-by-key (ResolvedTheme, frames.rae, gltf channel SoA, legacy textures) → `StringMap` or a `List` of `{name,value}` structs iterated with `loop`+`view` where order matters — `themeSpaceValue` → one `if let` on `StringMap.get(key:)` + `ret fallback`; (2) fixed-size lists unpacked by constant index (`quadTile`, window_geometry parts, color parts, nowPlayingTimes) → a struct returned by value; (3) flat-buffer samplers (`mesh3d`, `gltf_skin`, `compress/*`, tetris grids, json cursor, gltf palette) → keep the data a `List`, one helper per buffer doing `copyAt` in `if let` with an explicit programmer fallback in else (e.g. `ret 0.0`), callers keep clamping; (4) loop-by-counter read-only → range `loop` with `view` (`mod` where the body mutates); (5) genuinely optional + the misc guarded-first/last/single-char reads → `if let` with an explicit else value. Fix `examples/106_mobile_ui` and `legacy/raylib/98_mobile_ui` twins identically. THEN the compiler flip: remove the accessor `opt T` return-stripping in method-call resolution AND the silent `UNBOX` in `ensure_type_match` (sema.c:2009-2021); make `opt T`→non-optional a uniform hard sema error for every T, message "optional not unwrapped — use `if let`"; update negative test 616's expected output; add tests for scalar accessor, struct accessor, and a user function returning `opt`; close report §8. Filed SEPARATELY (not #642): #654 Array `mod`/`view` element-binding copies instead of aliasing (writes silently no-op), #655 unchecked dynamic Array index (docs claim debug-checked).
- [x] #643 Rename `List.at` → `copyAt` and remove `get`. The accessor triad becomes `copyAt` / `viewAt` / `modAt` — the leading verb states copy-vs-alias intent at every call site. Scope: rename `at` → `copyAt` (lib/core.rae:102) and delete the `get` compatibility alias (lib/core.rae:95); drop the parallel `viewGet`/`modGet` aliases (lib/core.rae:113/121) in the same pass. Migrate ALL callers in lib/ and tests (`.at(index:)` → `.copyAt(index:)`, `.get(index:)` → `.copyAt(index:)`); update docs. NOTE: the #641 `emit_list_if_let` peephole and #644 both match the method-NAME string `"at"`/`"get"` (c_stmt.c:39, emit_optional_boxed_expr method list) — update those name checks to `"copyAt"` in the SAME change or the fast path silently stops firing. Acceptance: `at` and `get` no longer resolve; `copyAt`/`viewAt`/`modAt` are the triad; full suite green. Deps: do after #641/#642/#644 so accessor churn settles first. DONE 2026-08-27: `List.at`→`copyAt`; `get`/`viewGet`/`modGet` removed. Compiler/VM name-checks retargeted to `copyAt` (c_stmt.c peephole + boxing passthrough, sema.c `sema_is_list_value_accessor`, vm_emit_stmt.c list-get mapping, and the List-`[]` diagnostic message). ~130 caller files migrated (`.at(index:)`/`.get(index:)` → `.copyAt(index:)` for List; also the plain-call UFCS form `get(list, index:)` in `string.join` + tests 375/376/377/380/381/389). String.at (returns Char32) is a SEPARATE method and stays `at` — over-rename reverts in string/sdf_text/gpu2d_text/json/msdf + tests 371/597; `List2`/`List2Int`/`List2Generic` keep their own `get`. Docs updated (list-indexed-access.md, value-types, ownership-model, native-list-plan). Suite green: cases 321/0, examples 62/3 (3 pre-existing GPU). NOTE: a stray `list.at(...)` now misresolves via generic UFCS to String.at/List2.get and surfaces as a C-level type error rather than a clean "no method `at` on List" sema diagnostic — pre-existing UFCS looseness; filed as #657 for a cleaner message.
- [x] #644 `view`/`mod` binding on `copyAt()` is a sema error. Motivation: `copyAt` (formerly `at`, see #643) is declared to return `opt T` by value, but the `=>` peephole (emit_list_if_let, c_stmt.c, matches the copy accessor and `get`) makes `if let particle: view Particle => particles.copyAt(index: index)` alias live list storage instead of copying — the declared return type and the actual semantics disagree, and the accessor's name and return type must agree. Scope: binding `=> view T` or `=> mod T` to an expression of type `opt T` (i.e. to `copyAt`/`get`) is an error whose message is exactly: "`copyAt` returns a copy; use `viewAt` or `modAt` to alias storage"; remove the cross-form peephole (subsumed by #641 if that runs first). Acceptance: test that the form errors with that message; `viewAt`/`modAt` forms unchanged. Deps: cleaner after #641; coordinate the accessor rename with #643. DONE 2026-08-27: binding `=> view/mod T` to a `copyAt` List value-accessor (method form `list.copyAt(index:)` AND plain-call `copyAt(list, index:)`, in both `let` and `if let`) now errors with exactly "`copyAt` returns a copy; use `viewAt` or `modAt` to alias storage". Two sema sites: `ensure_type_match` emits it (instead of the #642 "optional not unwrapped") when the target is a reference (TYPE_REF) fed by a copyAt accessor — the plain-`let` form; and the `if let` binding validator (AST_STMT_IF) checks `(view||mod)` binding to a copyAt source, with a callee-name fallback since the plain-call decl_link isn't resolved there yet. The cross-form is now rejected at sema BEFORE the peephole, so `emit_list_if_let` only ever sees matching name/binding shapes (value+copyAt, view+viewAt, mod+modAt) — the aliasing cross-form is gone. `viewAt`/`modAt` bindings unchanged (not List value accessors). Tests: new 662_reject_alias_bind_copyat asserts the message; 616 updated (it is now the #644 case). Suite green: cases 322/0, examples 62/3 (3 pre-existing GPU).
- [x] #645 Aliasing rule for `view`/`mod` bindings into containers (INVESTIGATE FIRST — deliverable is a design note plus a follow-up implementation task, not code). Motivation: a `viewAt`/`modAt` binding is a raw pointer into `list->data`; any grow reallocates it, and nothing in the report says what stops a mutating call on the same list (`add`/`set`/`remove`) inside the body of a live `modAt`/`viewAt` binding — if nothing does, this is a dangling-pointer bug class that value copies do not have, and it must be settled before `view`/`mod` become the recommended idiom. Scope: (1) write a test that mutates the list inside a live `modAt` body and inspect generated C and runtime behavior; (2) if currently allowed, write a design note with options — borrow the container for the binding's scope so no `mod` use of the list is permitted inside the body (Rust-style, the safe default); or a runtime generation-counter check on the list; or document as undefined — and recommend one; (3) file the implementation as a follow-up task. Acceptance: note with recommendation exists; follow-up task filed. Deps: blocks the `view`/`mod` half of #650 for any site whose body mutates the container. DONE 2026-08-27: hazard CONFIRMED and currently ALLOWED — `if let p: mod Int => xs.modAt(index: 0) { xs.add(...) ; p = 99 }` compiles with no diagnostic; generated C captures `&data[0]` once, a later `add`→grow→realloc leaves it dangling, and `p = 99` is a silent use-after-free (write lost; `copyAt(0)` stays 5). `copyAt` value bindings are immune (detached copy). Existing machinery: the range-loop borrow check (`sema_expr_mutates_collection`/`sema_stmt_mutates_collection`, sema.c:1283/1688; diagnostic "cannot mutate a List while a collection loop borrows its elements") already rejects add/set/insert/remove/clear/drop/grow/pop in `loop element in list` bodies — it just isn't applied to `viewAt`/`modAt` `if let` binding bodies. Design note: docs/viewat-modat-aliasing.md, comparing (A) borrow-the-container Rust-style / (B) runtime generation counter / (C) document-undefined. RECOMMENDATION: (A) — extend the existing loop-body check to `viewAt`/`modAt` `if let` bindings; compile-time, zero runtime cost, reuses shipped machinery + diagnostic, needs no trap primitive (Rae has none per #642), conservative-lexical per the codebase's stated intent. Follow-up implementation filed as #658. No code changed.
- [x] #646 Make the existing `copy` mode semantically enforced for cascade-drop types (INVESTIGATE FIRST — note plus follow-up task, not code). Motivation: requiring `copy` on every binding would be noise on `Int`/`Float`/`Vec2` and would duplicate the `=` vs `=>` distinction; types where `type_needs_cascade_drop` is true own heap, so copying them is a deep copy with allocation and should be spelled out. NOTE (verified, corrects the report): a `copy` mode ALREADY EXISTS as a keyword (TOK_KW_COPY, lexer.c:54) and a valid parameter/binding mode (parser sets is_copy; sema accepts it alongside view/mod/own) — BUT it is Stage-A annotation-only (ast.h:38: codegen treats `copy T` identically to bare `T`); so this task is "make `copy` semantically enforced with real deep-copy codegen", NOT "add copy". Scope: (1) determine what happens today when a struct that owns a `List` is bound by value (`let copyOfOwner: Owner = owner`): move, deep copy, or shallow copy with a later double free; (2) design: require `copy` in the binding for cascade-drop types, implicit copy for everything else, and give `copy` real deep-copy codegen; (3) file implementation as a follow-up. Acceptance: note plus follow-up task; the follow-up's tests cover — owning struct without `copy` errors; with `copy` deep-copies; no double free; `Particle` still binds without `copy`. Deps: relates to #641 (drop generation). DONE 2026-08-27 — investigation (emitted C): (1) binding a cascade-drop struct by value (`let x: Owner = owner`) is ALREADY an implicit DEEP copy (`rae_deep_copy_<T>`), unconditional (even on a dead/last-use source — no move optimization); it is SAFE — independent heaps, both dropped, no double-free/leak (RAE_MEM_STATS outstanding=0). NOT a shallow copy. (2) `copy T` and `own T` LOCAL bindings emit the SAME deep copy as bare `T` — `is_copy`/`is_own` are annotation-only for locals (ast.h:38), affecting only parameter-passing codegen. (3) Non-cascade-drop types (Point/Vec2/Particle-no-heap) are a plain `rae_T b = a;` struct copy — no deep copy, no allocation. CORRECTION to premise: the deep-copy codegen already exists AND bare `T` already uses it; there is no missing codegen and no shallow-copy double-free — the gap is a SEMANTIC REQUIREMENT (nothing forces `copy`, so the heap allocation is hidden). Design note docs/copy-mode-cascade-drop.md: RECOMMEND requiring `copy` on cascade-drop value bindings (bare → sema error; write `copy T` to deep-copy or `view`/`mod` to alias — mirrors #643 copyAt/viewAt/modAt and #644 `=`/`=>`), non-cascade-drop keep implicit copy. HONEST CAVEAT recorded: with no move-of-locals, `copy` deep-copies exactly as the implicit bind does today, so the payoff is explicitness/auditability, NOT perf or correctness; the higher-value alternative is move-on-last-use for the bare case (separate, larger). Follow-up + decision point filed as #659.
- [x] #647 Add an `alias` type alias (LOW PRIORITY). Context (verified): `alias` is not a keyword today (lexer.c keyword table) and `sema_resolve_type_internal` (sema.c:258) has no type-alias resolution — "alias" in sema currently means only import aliases. Scope: `alias Size = Vec2` resolving to the target's `TypeInfo` in `sema_resolve_type_internal` before the nominal decl lookup, as report §7 describes; document it as a naming convenience, not type safety (`func area(size: Size) ret Float` is callable on any `Vec2` via dot syntax); decide whether error messages print the alias name or the canonical name (prefer the name the user wrote where cheap). Acceptance: `type Rect { position: Vec2  size: Size }` compiles to 16 bytes and `vec2.*` works on `size`; test covering the error-message decision. Deps: none. DONE 2026-08-27: `alias Name = Type` implemented. CONTEXTUAL keyword (NOT reserved — `alias` is still a valid identifier, e.g. `let alias: String` in gltf_retarget.rae; recognized only as `alias <Ident> = <type>` at top-level decl position). New AST_DECL_ALIAS (ast.h) + parse_alias_declaration (parser.c). Resolution in sema_resolve_type_internal: a bare alias name is REWRITTEN in place to its target's `parts` before name-based lookup (a loop resolves alias-of-alias chains, guarded), so not only sema but also the mangler and C-type emission (which read `parts->text`, not `resolved_type`) see the canonical type — no `rae_Size` leaks into the C. find_alias_target scans the module + its imports (all_decls isn't populated until the backend). AST_DECL_ALIAS handled in sema_analyze_decl (validates target) and pretty.c (round-trips; the `else` fallthrough would otherwise misread it as a func decl). ERROR-MESSAGE DECISION: prints the CANONICAL name (cheap — the alias rewrites away at the use site), verified `let v: Vec3 = someSize` → "cannot convert Vec2 to Vec3". Verified: `struct rae_Rect { rae_Vec2 position; rae_Vec2 size; }` (16 bytes inline, 0 rae_Size), `r.size.x` / `area(size: view Size)` / `vec2.*` all work. Tests: 663_type_alias (positive), 664_type_alias_error_canonical (error decision). Doc: docs/type-aliases.md (naming convenience, NOT type safety). Suite green (432_mobile_ui_leak_regression is a pre-existing flaky leak test — passes in isolation; #647 is compile-time only).
- [x] #660 Do not make stupid decicions like allowing alias to be a variable name. Of course it has to be a real keyword. I saw you wrote: "**Important snag:** `alias` IS used as an identifier today — `lib/gltf_retarget.rae` has `let alias: String = ...` (4 sites). So a hard keyword would break shipped code and reserve a common word forever. A **contextual keyword** (recognize `alias Ident = Type` only at top-level decl position) avoids that." That is absolutely stupid. No such contextual keywords are good. Don't allow alias to be a keyword. DONE 2026-08-27 — `alias` is now a REAL reserved keyword (reversing #647's contextual-keyword hack). Added TOK_KW_ALIAS to lexer.h (inside the TOK_KW_TYPE..TOK_KW_AS keyword range that `is_keyword` checks) + the lexer.c KEYWORDS table + TOKEN_KIND_NAMES (designated initializers, so the enum insertion doesn't misalign the name array). parse_declaration now dispatches on `parser_match(TOK_KW_ALIAS)` (removed the IDENT-"alias" + lookahead detection); parse_alias_declaration reads the keyword via parser_previous. Renamed the 7 identifier uses of `alias`: lib/gltf_retarget.rae (4 → `aliasKey`) and test 551_as_conversion (3 → `aliasF32`; the string label `alias=` and expected.txt are unchanged). Verified: `let alias: Int = 5` now errors "'alias' is a reserved keyword"; the alias-decl tests 663/664 still pass; gltf_retarget compiles. Doc docs/type-aliases.md corrected ("reserved keyword", not contextual). Suite green: cases 324/0, examples 62/3 (3 pre-existing GPU).
- [x] #648 Decision record: no `using`/field promotion for now (NOT a code task). Scope: add an entry to the design-decisions doc (docs/, wherever such notes live) recording that field promotion is deferred; if ever added it must ship with per-field rename in the same change (`using size: Vec2 as { width: x, height: y }`), because `alias` guarantees two promoted `Vec2` fields collide on `x`/`y`; dot-syntax functions already give read-only promotion (`func width(rect: Rect) ret Float { ret rect.size.x }`). Acceptance: the decision record exists. Deps: relates to #647. DONE 2026-08-27 — decision record written at docs/field-promotion-deferred.md: field promotion deferred; if ever added, per-field rename is mandatory in the SAME change (`using size: Vec2 as { width: x, height: y }`) because aliases (#647/#660) make same-type field collisions unavoidable (`Rect { position: Vec2  size: Size }` → bare promotion makes `rect.x` ambiguous); read-only promotion is already available via dot-syntax accessor functions (`func width(rect: view Rect) ret Float { ret rect.size.x }` → `rect.width()`). Doc-only, no code.
- [x] #649 `Array[i]` vs `List` indexing consistency (INVESTIGATE FIRST — note plus follow-up if `List` gets `[]`). Context (verified): `arr[i]` on `Array(T, cap: N)` is accepted and typed by sema (AST_EXPR_INDEX, sema.c:2624-2625, with compile-time bounds checking for constant indices) while `list[i]` on `List(T)` is explicitly rejected with a diagnostic (sema.c:2604-2621, "a List is not indexed with []"); the asymmetry is the issue, not the missing sugar. Scope: pin down what `arr[i]` means today (value read, in-place lvalue, or both by context) and what it does out of bounds; then decide — either `List` gets `[]` with identical semantics (bounds-checked, panics), or document why the two containers differ. Acceptance: note with decision exists; follow-up task filed if `List` gets `[]`. Deps: none. DONE 2026-08-27 — DECISION: `List` does NOT get `[]`; the asymmetry is intentional. docs/array-vs-list-indexing.md. Verified: `arr[i]` is an lvalue (value-read copy + in-place write `arr[i].field = v`), returns `T` (not opt), constant OOB = compile error (`index 7 is out of bounds for Array(cap: 3)`), dynamic OOB currently unchecked (Array-internal gap, #655). `list[i]` is rejected with a message already pointing to `copyAt`/`viewAt`/`modAt` + `if let`. RATIONALE: Array's size is in its type so `arr[i] -> T` is statically bounded; a List's length is a runtime value so indexed access is inherently fallible — giving List `[]` would either return `opt T` (uglier than copyAt AND still inconsistent with `arr[i] -> T`), or return `T` with a trap (no trap primitive per #642), or return `T` unchecked (the silent-OOB footgun #642/#643/#645 fought); and `[]` cannot express the copy-vs-alias intent the #643 triad names. NO follow-up filed (was conditional on List getting `[]`). Related/separate, already filed: #655 (Array dynamic-index bounds vs docs), #654 (Array mod/view element binding copies instead of aliasing).
- [x] #650 CLOSED 2026-08-26 — now tracked in the downstream game project's own repo; this carry-over placeholder is no longer needed here. (Was: carry over the QUEUE #112 abbreviated-binding + access-mode cleanup.) Scope: keep the naming half — spell out one-letter bindings like `m`, `ar`, `b` into full names (`mesh`, `array`, ...); narrow the access-mode half — convert sites that mutate an element to `modAt`, convert read-only access to large elements or elements with owned heap fields to `viewAt`, leave small plain-data read-only copies (`Particle`, 48 bytes) as value bindings (expect NO gain from converting those: at -O2 the C compiler scalarises the copy and loads only the fields used; verify on one site before converting more); any site whose body mutates the list while a `mod`/`view` binding is live waits on #645; migrate any `get` callers to `at` (#643). This entry only carries the amended text over — do not implement here. Deps: #645 (mutate-while-live sites), #643 (remove get).
- [x] #651 Complete the `opt T` uniform representation: move scalar/String optionals off the `RaeAny` union too (finish "reserve RaeAny for Any only" from #641). Motivation: #641 migrated struct/aggregate `opt T` to `struct rae_opt_<T>{has,value}` but left `opt Int`/`opt Float`/`opt Bool`/`opt Char`/`opt String` on the inline `RaeAny` union (already malloc-free, so out of #641's acceptance); to fully reserve `RaeAny` for `Any` only, migrate scalars/String to `rae_opt_<T>` too. Scope: extend the struct-rep predicate (`rae_opt_is_struct_rep`, c_backend.c) to all non-ref non-Any payloads; update the paths that currently stay on RaeAny — the `.as.i/.as.f/.as.b/.as.s` unbox (c_call.c `emit_opt_unbox_suffix`, c_expr.c UNBOX), `rae_any_is_none` none-checks (c_expr.c binary, c_stmt.c if/match), and especially log/interpolation/JSON/toString, which today rely on RaeAny being self-describing (a `rae_opt_<T>` is not, so these need per-type formatting). Acceptance: no value-`opt` spells `RaeAny` in emitted C (only `Any` does); scalar/String opt round-trip incl. log/interp/json covered by tests. Deps: builds on #641. DONE 2026-08-27 — every non-`Any` value optional (opt Int/Float/Float64/Bool/Char/String, and opt<Enum>→Int) now lowers to `struct rae_opt_<T>{ rae_Bool has; T value; }`; only `Any` uses RaeAny. Extended both manglers' struct-rep predicates (type.c TYPE_OPT + c_backend rae_typeinfo_opt_is_struct_rep) to include TYPE_INT/FLOAT/FLOAT64/BOOL/CHAR/STRING, which auto-routes construction/if-let/none-check/unbox/drop/deep-copy through #641's machinery. Hand-fixed the RaeAny self-describing paths (all c_backend.c): toJson serializes `.value` per concrete type (Int %lld, Float %g, Bool true/false, String "%.*s") when `.has` else null; fromJson gates on a new `rae_json_key_present` runtime helper and fills `.value` or leaves `{0}`; toString/interpolation formats `.has ? <value> : "none"` (scalar/String print the value — verified `log("{rec}")` → `{ hi, 7 }` and `log("{someInt}")` → 42 / none; aggregate payloads keep the `<opt>` placeholder from #641); `emit_to_string_expr` handles `log({optScalar})` at use sites; drop/deep-copy special-case String via rae_string_drop/rae_string_copy. Also strengthened generic opt-returning-call inference (map.get/copyAt/peek/pop) so none-checks/if-let/interp see the concrete `opt T`. VERIFIED independently: a value-opt-only program emits 0 RaeAny box/union ops (`.as.{i,f,b,s}`, `rae_any_{int,float,bool,string}(`) and uses rae_opt_ structs (opt_no_raeany.sh); opt_no_malloc.sh still 0 malloc; 449_opt_owned_drop (String-opt over 3000 iters, RAE_MEM_STATS) outstanding=0 — String dropped once, deep-copied via rae_string_copy. Test 665_opt_scalar_string_roundtrip (if-let present/none, opt String, interp of the optional, toJson+fromJson of opt Int/Float/Bool/String present+none). Suite green: cases 325/0, examples 62/3 (3 pre-existing GPU). Implemented in a worktree, verified + landed to main by patch.
- [x] #652 Fix pre-existing double-free: generic `get`/`at` (non-peephole) on `opt <struct-with-an-owned-heap-field>`. Motivation: surfaced during #641 — calling the generic `get`/`at` as a REAL function (not the `emit_list_if_let` peephole) to extract an `opt <struct that owns heap, e.g. a String field>` and then dropping it double-frees, because those accessors build the optional by MOVING a `buf_get` alias (shallow) instead of deep-copying, while the drop then frees a payload the list still owns; the peephole path deep-copies and is safe. Predates #641 (same shallow-move logic as the old malloc branch). Scope: make `get`/`at` deep-copy heap-owning payloads (or treat `buf_get` as non-movable for owning payloads). Acceptance: a program that stores `let x: opt <StructWithString> = list.at(index:)` via the general (non-peephole) path, drops it and the list, runs clean under `RAE_MEM_STATS` (no double-free, no leak); test added. Deps: relates to #641/#643. DONE 2026-08-27 — REPRODUCED first: `let x: opt Thing = xs.copyAt(index: 0)` (Thing owns a String) SIGABRT'd with POINTER_BEING_FREED_WAS_NOT_ALLOCATED — the opt construction (emit_optional_boxed_expr) MOVED the `buf_get` alias `copyAt` returns instead of deep-copying, so dropping both `x` and `xs` freed the same String twice. Root cause: the move-vs-copy gate `c_expr_can_move_owned_payload` returns true for ANY call/method-call, but `copyAt`'s body `ret rae_ext_rae_buf_get(...)` returns a shallow alias into the list's storage — not a movable owned rvalue. Fix (c_stmt.c): new `c_opt_source_aliases(ctx, value)` reuses the existing alias-returning classifier (`call_is_aliasing`/`func_returns_alias_v`) for the plain-call form AND adds the method-call form (look up the method's func body), and both opt-construction deep-copy decisions now force deep-copy when the source aliases (`needs_deep && (!can_move || c_opt_source_aliases)`). The peephole path already deep-copied and is unchanged. Verified: repro now clean (`[mem:string] alloc=2 free=2 outstanding=0` — the String is deep-copied so each of `x`/`xs` owns an independent heap, dropped once); emitted C now calls `rae_deep_copy_rae_Thing`. Test 666_opt_alias_copyat_no_double_free (3000-iteration RAE_MEM_STATS scenario, verdict OK; added 666_* to the leak-gate in tools/run_tests.sh). Suite green.
- [x] #653 Verify `view`/`mod` on primitives (`Int`, `Float`, `Bool`) are legal types that lower to a plain pointer with NO boxing — prerequisite for the downstream access-mode sweep (formerly #112, now in the game repo). Motivation: that sweep converts read-only element access to `viewAt` and in-place mutation to `modAt`; for a `List(Int)`/`List(Float)`/`List(Bool)` this means bindings like `if let health: view Int => healths.viewAt(index: index)` and `if let health: mod Int => healths.modAt(index: index)`. This must be a real reference into `list->data` (a plain `T*` — the existing `rae_View_Int64`/`rae_Mod_Int64`/`rae_View_Float`/`rae_Mod_Float`/`rae_View_Bool`/`rae_Mod_Bool` wrappers, c_backend.c emit_type_ref_as_c_type), NOT a boxed/copied value. Scope: (1) test that `view`/`mod` on each of Int/Float/Bool is accepted by sema and that `viewAt`/`modAt` on a `List(<primitive>)` binds a pointer to the element — a `modAt` write must persist and be visible to a later read; (2) if any primitive view/mod is rejected today or copies instead of aliasing, make it legal and pointer-lowered. Acceptance: a test per primitive (Int, Float, Bool) for BOTH `view` and `mod`, with the generated C inspected to confirm a `rae_View_*`/`rae_Mod_*` pointer wrapper and no boxing/copy; a `modAt` mutation round-trips. Deps: none; blocks the downstream access-mode sweep. DONE 2026-08-27 — ALREADY WORKS; NO compiler change needed. Verified all 6 combos (view/mod × Int/Float/Bool): `if let x: view/mod <Prim> => list.viewAt/modAt(index:)` is accepted by sema and the emit_list_if_let peephole lowers each to a plain pointer `rae_View_Int64/rae_Mod_Int64/rae_View_Float/rae_Mod_Float/rae_View_Bool/rae_Mod_Bool x = { .ptr = &__rae_list->data[i] }` — a real reference into list->data, 0 malloc, no boxing/copy. `mod` writes in place (`*mi.ptr = 99`, `*mf.ptr = 9.5`, `*mb.ptr = true`) and round-trip (a later viewAt read sees the write: after-mi=99/after-mf=9.5/after-mb=true). Tests: 667_primitive_view_mod (runtime round-trip, all 6) + tests/primitive_view_mod_pointer.sh (emit-c guard: 6 `.ptr = &...->data[...]` pointer bindings, in-place `*m.ptr =` write, 0 malloc). Suite green. The downstream access-mode sweep is unblocked.
- [x] #654 Fix `Array(T, cap: N)` `mod`/`view` element-binding: it copies instead of aliasing. Surfaced during #642 Array/Buffer investigation. `let refv: mod Cell => grid[i]` currently emits `__auto_type __rae_bind0 = grid.v[i];` then takes `&__rae_bind0` (a COPY), so writes through the bound `mod` reference silently no-op and never reach the array storage (unlike `List.modAt`, which aliases real storage). Direct `grid[i].field = value` DOES work (it's only the alias-binding path). Scope: make `view`/`mod` binding to an Array element alias `&grid.v[i]` directly (like List modAt), in c_expr.c/c_stmt.c index-binding lowering. Acceptance: a test where a `mod` binding to `arr[i]` writes a field and a subsequent read sees it; generated C takes the element address, not a copy. Deps: none. DONE 2026-08-27 — REPRODUCED: `let refm: mod Cell => grid[1]` emitted `__auto_type __rae_bind0 = grid.v[1]; rae_Cell* refm = &__rae_bind0;` (aliasing a COPY) so `refm.hp = 99` never reached grid storage (later read stayed 5). Root cause: the `=>` alias-binding lowering (c_stmt.c ~1297) lumped AST_EXPR_INDEX in with rvalue sources (CALL/METHOD_CALL/OBJECT) into the materialise-temp-then-`&temp` path — but an Array/Buffer element `grid[i]` is an ADDRESSABLE lvalue (`grid.v[i]`). Fix: a dedicated AST_EXPR_INDEX branch emits `<T*> name = &(grid.v[i]);` directly. Now `rae_Cell* refm = &(grid.v[1]);` → `refm->hp = 99` writes in place; verified round-trip at runtime (`after mod-bind hp=99 tag=7`, `view reads hp=99`). Test 668_array_mod_view_element_binding. List.modAt/viewAt bindings are unaffected (they take the peephole path, never this one). Suite green. NOTE: found two ORTHOGONAL pre-existing Array bugs while testing — an Array-typed binding constructed with an anonymous `{ }`/`{...}` literal (`var g: Array(Int, cap: 3) = { }`) misgenerates a `List(T)` (undeclared `rae_List_<T>` / `rae_Array_..._N` initialized with `rae_List_...`); the correct `= Array(T, cap: N)` constructor works. Filed as #661.
- [x] #661 Array-typed binding with an anonymous `{ }`/`{ ... }` literal misgenerates a `List(T)` instead of a zero-initialized Array. Surfaced during #654: `var grid: Array(Int, cap: 3) = { }` fails to compile — the `{ }` construction emits `rae_List_int64_t` (initializing a `rae_Array_int64_t_3` with a `rae_List_int64_t`), and `Array(Cell, cap: 3) = { }` emits `createList_rae_Cell` helpers referencing an undeclared `rae_List_rae_Cell` struct. The explicit `= Array(T, cap: N)` constructor works (tests 563/566), so this is only the anonymous-literal construction path. Scope: either make `{ }`/`{ e0, e1, ... }` on an Array-typed binding zero-init / element-init the Array (matching struct-literal ergonomics), or reject it with a diagnostic pointing at `Array(T, cap: N)`. Acceptance: `var g: Array(Int, cap: 3) = { }` either compiles to a zero-inited Array or errors cleanly (no misgenerated List / undeclared-type C error); test added. Deps: none. DONE 2026-08-27 — CHOSE REJECT (the canonical `Array(T, cap: N)` constructor already zero-initializes, so a half-supported `{ }`-for-Array literal would only add a second spelling). Root cause: the collection-literal let-binding path (c_stmt.c:1455) unconditionally emits `createList_<T>(count)` into the declared slot, ignoring an Array/Buffer declared type — so `rae_Array_int64_t_3 grid = rae_core_createList_int64_t_(0)`. Fix in SEMA (early, good location): a non-bind `let`/`var` whose declared type base is `Array` or `Buffer` and whose value is an `AST_EXPR_COLLECTION_LITERAL` is now a hard error naming the fix — "a '{ }' list literal cannot initialize an Array: its capacity is part of its type — construct it with 'Array(T, cap: N)' (which zero-initializes), then assign elements by index" (Buffer worded identically). Empty `{ }` and non-empty `{ 1,2,3 }` both rejected; canonical `= Array(T, cap: N)` unaffected (runs, `v=5`). Test 669_reject_array_list_literal. Suite green.
- [x] #655 Implement (or correct the docs for) dynamic `Array(T, cap: N)` bounds checking. Surfaced during #642 investigation. `docs/value-aggregates-and-ownership.md:214-228` specifies dynamic Array indexing is "dynamic-checked in debug", but codegen (c_expr.c:627-657, AST_EXPR_INDEX) emits raw `grid.v[i]` with NO bounds check in ANY profile — constant indices are compile-time checked (sema.c:2624-2645) but a runtime index is fully unchecked. Scope: either implement the documented debug-gated dynamic bounds check (emit `if (i<0||i>=N) <trap>` around dynamic Array reads/writes — note: needs a trap mechanism, currently absent) OR correct the doc to state dynamic Array indexing is unchecked. Decide which. Acceptance: docs and codegen agree; if implemented, a test with an out-of-range dynamic index traps in a debug build. Deps: none. DONE 2026-08-27 — CHOSE IMPLEMENT (the doc §1.7 treats debug-checked dynamic indexing as an approved, deliberate safety policy; a debug-only C-level abort is a memory-safety guard, not a user-facing construct, so it does not reintroduce the trap/unwrap operator #642 rejected). Mechanism: the dev profile compiles `-O0 -g` and release `-O2 -DNDEBUG`, so an `#ifndef NDEBUG` macro `RAE_ARRAY_IDX(idx, cap, file, line)` (runtime/rae_runtime.h) evaluates `idx` once and, in debug, aborts with `<file>:<line>: runtime error: index N is out of bounds for Array(cap: M); valid indices are 0..M-1` on OOB; in release it expands to the bare index (zero cost in skinning/matrix inner loops). Codegen (c_expr.c AST_EXPR_INDEX) wraps only DYNAMIC subscripts (integer literals stay raw — sema already compile-rejects constant OOB), passing the array's `TYPE_ARRAY` count and the Rae source location via new `emit_c_source_location`. Verified: dev OOB aborts with location, in-range dynamic works, release compiles the check out (no abort). Docs reconciled: array-vs-list-indexing.md's stale "unchecked / not implemented" note updated; §1.7 already matched. Test 670_array_dynamic_index_oob_trap (config `run --profile dev`, REGEX-matched to skip the crash backtrace).
- [x] #656 Investigate: `if let x: view String => <opt String field>` presence-check disagrees with `is not none` after the field is REASSIGNED to none. Surfaced during #642 (test 531_opt_string_ownership `describe`): a plain `if let noteText: view String => card.note` ENTERS its branch even after `card.note = none` (yielding e.g. `alpha/` instead of `alpha`), while `card.note is not none` detects the none correctly; init-to-none works fine (test 607). Worked around in 531 by nesting the `view =>` under an `is not none` guard. Looks like a real none-detection inconsistency for a REASSIGNED `opt String` (RaeAny) field via `view =>` narrowing. Scope: reproduce minimally, find why `view =>` narrowing's presence test misses a reassigned-to-none RaeAny opt-String field, fix so it agrees with `is none`. Deps: none. DONE 2026-08-27 — NOTE: `opt String` is no longer RaeAny; #651 moved it to struct-rep `rae_opt_rae_String{ has, value }`. Root cause (c_stmt.c ~1384, the primitive-ref `=>` binding path): narrowing `view/mod String => <struct-rep opt>` emitted `rae_View_String n = { .ptr = &(src).value };` — the address of the inline `.value` field, which is NEVER NULL — so the presence test `n.ptr != NULL` entered even for none. The struct/owned narrowing paths already gate correctly via the UNBOX form `has ? &value : NULL` (c_stmt.c:1421-1431); the String/primitive-ref path never got that gate, which is why the bug was String-specific (struct opts worked). Fix: in that path, when the source is a struct-rep optional, emit `{ .ptr = ({ rae_opt_<T>* __ov = &(src); __ov->has ? &__ov->value : NULL; }) }` (unwrapping an UNBOX wrapper first). Verified: reassigned-to-none field AND local now read as NONE (agreeing with `is not none`); present values still bind and read through both `view` and `mod`; struct opts unaffected. Simplified test 531 (removed the now-unneeded `is not none` workaround around the narrowing) and added 671_opt_string_view_narrow_reassigned_none.
- [x] #657 Clean "no method on receiver" diagnostic for generic UFCS misresolution. Surfaced during #643: calling a method that doesn't exist on the receiver (e.g. `someList.at(index: 0)` after `at` was removed from `List`) does NOT produce a clean sema error — the generic UFCS matcher instead binds it to a same-named function on a DIFFERENT type (e.g. `String.at`, or the generic `List.copyAt`/`List2.get`), emitting confusing C-level type errors (`initializing 'RaeAny' with int64`, `rae_core_copyAt_rae_List_rae_T_...` undeclared) at the generated-C line instead of the .rae call site. Scope: when UFCS/method resolution finds a candidate whose receiver type doesn't actually match, reject it and emit a sema error naming the receiver type and method ("no method `at` on `List(Int)`; did you mean `copyAt`?"). Acceptance: a `list.at(index:)` (or any wrong-receiver method) errors at the .rae site with a clear message, not a downstream C error. Deps: none. DONE 2026-08-27 — KEY FINDING: sema's method matching intentionally leaves many VALID calls unresolved (a SPECIALIZED receiver `List_int64_t` does not match a method's generic template `this: mod List(T)`), so the C backend is the real resolver; erroring on every unresolved method call flags valid code (`add`, etc.). Fix (sema.c METHOD_CALL, after all resolution paths): when a call is unresolved AND a same-named `this`-method exists but NONE targets the receiver's TEMPLATE base (via new sema_struct_template_name — compares template-to-template, not the mangled specialized name), emit `no method `<m>` on `<Receiver(Args)>``; a case-insensitive prefix/suffix near-miss on the same receiver adds `; did you mean `<x>`?` (`at`->`copyAt`). Gated so compiler intrinsics with no user decl (toString/toJson/fromJson/Task.get) still reach the backend. Added sema_receiver_display (renders `List(Int)` from the template base + resolved generic args) and sema_name_near. Verified: `xs.at(index:0)` -> "no method `at` on `List(Int)`; did you mean `copyAt`?" at the .rae site; String.at/toString/toJson/valid copyAt unaffected; no `add` false positives. Also gated on a CONCRETELY-typed receiver: a chained call sema leaves unresolved types its result Void (`text.at(index:i).toInt()`), and erroring there false-flags valid backend-resolved calls — skip Void/Unknown/Any receivers. Test 672_no_method_on_receiver_diag.
- [x] #658 Implement the `viewAt`/`modAt` aliasing borrow check (from #645; design note: docs/viewat-modat-aliasing.md). Extend the existing collection-loop borrow check (`sema_expr_mutates_collection`/`sema_stmt_mutates_collection`, sema.c:1283/1688, which rejects add/set/insert/remove/clear/drop/grow/pop with "cannot mutate a List while a collection loop borrows its elements") to the then-branch of an `if let x: view/mod T => list.viewAt/modAt(index:)` binding: while the binding is live, reject the eight structural mutators on the ALIASED List (identify it from the `viewAt`/`modAt` receiver), reusing the diagnostic reworded for the binding form. Rationale: today the binding captures `&list->data[i]` once and a later `add`→grow→realloc leaves it dangling — a silent use-after-free (repro + generated C in the note). Compile-time, zero runtime cost, no new primitive. Acceptance: mutating the aliased List inside a live `modAt`/`viewAt` `if let` body is a sema error; mutating a DIFFERENT List and `copyAt`+`if let` still compile; test added; suite green. Deps: none; blocks the `view`/`mod` half of #650 for any site whose body mutates the container. DONE 2026-08-27 — Option A implemented (sema.c AST_STMT_IF, inside the if-let binding block). When the binding source is `list.viewAt/modAt(index:)` (method-call form) or `viewAt/modAt(list, index:)` (plain-call form), the aliased List is the receiver/first-arg; each then-branch statement is run through the EXISTING `sema_stmt_mutates_collection` (reused verbatim, so it catches nested blocks too), and any of the structural mutators (add/set/insert/remove/swapRemove/clear/drop/grow/pop) on that same List is a sema error: "cannot mutate a List while a `viewAt`/`modAt` binding aliases one of its elements" (the loop diagnostic reworded). `copyAt` (a detached value binding, uses `=` not `=>`) and mutating a DIFFERENT List stay legal; verified both plus a no-mutation viewAt body. Compile-time, zero runtime cost, no new primitive. Test 673_viewat_modat_aliasing_borrow.
- [x] #659 Require `copy` for cascade-drop value bindings (from #646; design note: docs/copy-mode-cascade-drop.md). DECISION POINT: adopt the requirement, OR keep status quo and pursue move-on-last-use instead — see the note's "honest caveat" (the benefit is explicitness/auditability, NOT perf or correctness; today's implicit bind already deep-copies safely). If adopting: make a bare value binding `let x: T = y` a sema error when `type_needs_cascade_drop(T)` is true, with message naming the fix (write `copy T` to deep-copy, or `view`/`mod` to alias); `copy T` binding deep-copies (the existing `rae_deep_copy_<T>` codegen — no new codegen needed); non-cascade-drop types (Int/Vec2/Particle) keep the implicit plain copy. Migrate existing `let x: HeapOwningStruct = y` callers to `copy` (or a `view`/`mod` alias). Acceptance: owning struct bound by value WITHOUT `copy` errors; WITH `copy` deep-copies with no double free (RAE_MEM_STATS outstanding=0); `Particle`/`Point`/`Vec2` still bind without `copy`; suite green. Note: `is_copy` is currently annotation-only for LOCAL bindings (only parameter passing honors it), so the change is primarily the sema gate + routing the local `copy` binding through the existing deep-copy path. Deps: relates to #641 (drop/deep-copy helper generation). QUESTION 2026-08-27: This is a language-semantics change flagged as a DECISION POINT, and measurement shows adopting it as literally specified is not viable. I prototyped the exact gate (bare value binding of a `type_needs_cascade_drop` type from a place expr -> error) and ran the full suite: 275 of 333 cases FAIL to compile. Root cause: `String` is cascade-drop, so `let name: String = <place>` is flagged — one of the most common patterns in the language — and it pervades lib/ (string.rae, filesystem.rae, app3d/*, ui/*), so nearly every test aborts at a lib-level String binding before reaching its own code. The note's examples/acceptance only ever cite STRUCTS (Owner/Particle/Point/Vec2) and never bare String, so whether bare String is in scope is a genuine ambiguity that dominates the cost. Per the note's own honest caveat the benefit is ONLY explicitness/auditability — NOT perf (copy deep-copies identically to today's implicit bind) and NOT correctness (already safe, outstanding=0). Which do you want? (A) Adopt as specified incl. String — ~hundreds of `copy String` migrations across lib/tests/examples, high friction; (B) Adopt but EXCLUDE bare String / primitive-ish heap types, gating ONLY user structs/containers that own heap (few sites: SdfFont/List/Holder + a handful) — a usable middle that keeps the auditability win for the aggregates the note actually cites; (C) Keep status quo (implicit deep copy is safe) and instead pursue move-on-last-use (the note's finding #1) as the higher-value follow-up. RECOMMENDATION: (B) if we want the explicitness win now (smallest churn, matches the note's struct-focused examples), else (C). Gate prototype reverted; no code committed. DECISION 2026-08-27 (user): option (C) — KEEP STATUS QUO. Bare value bindings deep-copy implicitly for every type, as today. Reason: options (A) and (B) both make the need for `copy` depend on what a type CONTAINS, so changing `Int` into a heap-owning type would change which code compiles — Rae's rule is that every type follows the same rules. The 275 failures from `String` being cascade-drop are that hazard demonstrated, not a reason to exempt `String`. No compiler change. Higher-value follow-up (move-on-last-use, optimization-only, no type-dependent rule) filed to QUEUE_BACKLOG.md as #662.
