examples/**/build/
.rae/
# Claude Code local scratch (agent worktrees, settings) — never commit
.claude/
examples/**/temp/
examples/**/app_cache/
examples/**/assets/state.json
examples/**/assets/debug_flags.json
.DS_Store

# Editor/tooling leftovers. Two of these were committed as if they were
# source files (#440, #441) before anyone noticed, so guard the class.
*.tmp
*.orig
*.rej
*.bak
/build/ios/
.roundtable/
.gemini/

# Album covers are fetched at run time from each album.json artworkUrl into
# app_cache/ (gitignored); a cover dropped next to an album.json is a local
# override and never committed (third-party artwork stays out of the repo).
examples/106_mobile_ui/assets/albums/*/cover.*
