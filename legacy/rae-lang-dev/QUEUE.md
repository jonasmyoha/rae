<!-- SUMU_QUEUE_START -->
- [x] #1 Task 1: Emit collection literals (`let x: List(Int) = { 10, 20, 30 }`) — handle AST_EXPR_COLLECTION_LITERAL in c_backend emit_expr by emitting createList + add calls. Fixes 325_list_literal, 337_list_method.
- [x] #2 Task 2: Fix identity/newtype lowering (`let pid: User = 123`) — detect `is id`/`is key` types and emit underlying primitive type. Fixes 341_identity_behavior + smoke 10_memory_basics, identity_fix, store_mapping.
- [x] #3 Task 3: Fix toString() method resolution — `s.active.toString()` emits `rae_toString` undeclared. Emit as type-specific rae_ext_rae_str call. Fixes 350_auto_init (partial).
- [x] #4 Task 4: Fix output format mismatches for ref types — 336 outputs `7` instead of `view 7`, 351 outputs `80` instead of `mod 80`. Decide: update test expectations or add prefix in C log. Also finish 350_auto_init string eq chain.
- [x] #5 CHECKPOINT 1: Re-run all tests after tasks 1-4. Evaluate results. Update remaining tasks.
- [x] #6 Task 5: Fix opt T return type unboxing — `get(T)` returns RaeAny but callers expect rae_String. Add .as.s/.as.i unboxing at call sites when return type is opt T. Fixes 371_string_overhaul + ~10 smoke tests.
- [x] #7 Task 6: Fix createIntMap/createInt64Map specialization chain — inner specialization not discovered. Improve C backend discovery pass transitive following. Fixes 370_map_basic.
- [x] #8 Task 7: Fix rae_any macro for struct types — _Generic has no case for List/custom structs. Add struct handling or avoid boxing. Fixes multiple smoke tests.
- [x] #9 Task 8: Fix view restriction diagnostic for compiled target — test expects compiler errors but compiled target emits nothing. Wire sema view checks for compiled path. Fixes 357_view_restriction.
- [x] #10 CHECKPOINT 2: Re-run all tests after tasks 5-8. Major smoke test re-assessment.
- [x] #11 Task 9: Add raylib C function forward declarations — emit declarations for rae_ext_initWindow etc. when raylib is imported. Fixes ~8 raylib smoke tests.
- [x] #12 Task 10: Add crypto function declarations and C wrappers — rae_ext_rae_crypto_lock/unlock using monocypher. Fixes 19_file_ops, 27_file_locking, 28_crypto_demo.
- [x] #13 Task 11: Fix random(min,max) overload in imported modules — 2-arg random not found in import context. Extend overload search. Fixes 04_random, 92_pong_import, 94_tetris.
- [x] #14 Task 12: Implement defer statement emission — collect defers, emit before returns and at function end. Fixes 15_defer_cleanup.
- [x] #15 CHECKPOINT 3: Re-run all tests after tasks 9-12. Full re-assessment.
- [x] #16 Task 13: Fix external C function linking — compile user .c files alongside generated output. Fixes 20_external_c.
- [x] #17 Task 14: Fix JSON serialization (toJson/fromJson) — implement C-side or mark VM-only. Fixes 17_json_demo, 18_complex_json.
- [x] #18 Task 15: Fix method this parameter passing (value vs pointer) — add & for struct this args in method calls. Fixes 25_timer_demo + others.
- [x] #19 Task 16: Fix float modulo operator — emit fmod(a,b) instead of a%b for doubles. Fixes 93_raylib_3d.
- [x] #20 CHECKPOINT 4: Final evaluation. Plan next phase.
- [x] #21 Fix opt T unboxing — UNBOX emits `.as.s` on `rae_String` when extern returns concrete type not RaeAny. Skip unbox for concrete return types. Fixes 19_file_ops, 21_stdlib_demo, 27_file_locking, 28_crypto_demo, 371_string_overhaul.
- [x] #22 Fix List member `->` vs `.` in interpolation/non-method contexts — `xs->length` should be `xs.length` for value-type List variables. Fixes 06_list_basic, 07_list_loop, 08_list_benchmark.
- [x] #23 Add compound literal cast for c_struct types in function call args — bare `{ .r=20 }` needs `(Color){ .r=20 }` cast. Fixes 93_raylib_3d, 95_easing_2d, 96_easing_3d.
- [x] #24 CHECKPOINT 5: Re-run all tests after #21-#23. Evaluate progress.
- [x] #25 Fix Legacy List2 buf ops type mismatch — old List2 API passes RaeAny where int64_t expected. Fix buf_set/get for non-generic List2. Fixes list_library_any, list_library_int, list_native_any.
- [x] #26 Fix map method T substitution — `m.set(...)` / `m.get(...)` on StringMap/IntMap emit `rae_T` instead of concrete type. Fixes 370_map_basic.
- [x] #27 Fix 337_list_method output — element 20 stored as none. Debug buf_set index in add specialization. Fixes 337_list_method.
- [x] #28 Fix 18_complex_json — add uint8_t to rae_ext_rae_str _Generic macro for raylib Color fields. Fixes 18_complex_json.
- [x] #29 Fix 94_tetris2d — cross-type specialization chain (grow for List(String) inside List(Int) context). Fixes 94_tetris2d.
- [x] #30 Fix generic_test Box(T) — Buffer field typed as uint8_t* gets int64_t. Needs monomorphised struct field types. Fixes generic_test.
- [x] #31 CHECKPOINT 6: Final phase 2 evaluation.
- [x] #32 Add AST_EXPR_INTERP handling to sema — sema doesn't walk string interpolation sub-expressions, so method calls like `xs.get(index: 0)` inside `"{...}"` never get `decl_link` resolved. Add INTERP case to `sema_analyze_expr` to recurse into interp parts. HIGH IMPACT: fixes 06_list_basic, 07_list_loop, and similar interpolation-heavy tests. LOW RISK.
- [x] #33 Clear `generic_params` on specialized decls in `specialize_decl()` — specializations inherit `generic_params` from the template via struct copy, confusing C backend code paths that check `fd->generic_params` to decide if a function is generic. A specialization has `specialization_args`, NOT `generic_params`. Set `generic_params = NULL` on specializations, use `generic_template->generic_params` where param names are needed. MEDIUM RISK: must audit all `generic_params` checks.
- [x] #34 Generalize sema generic inference beyond `this` parameter — sema only infers generic args from the first param if named `this`. Extend to try ALL param/arg pairs: if an arg has a resolved struct type with generic args, try `infer_generic_args` against the corresponding param type. Fixes generic_test (`setValue(T)(b: mod Box(T), val: T)`). MEDIUM RISK.
- [x] #35 CHECKPOINT 7: Re-run all tests after #32-#34. Evaluate which of the 9 failures are fixed.
- [x] #36 Add receiver-type inference to C backend fallback for non-generic contexts — safety net for cases where sema still fails. When fallback finds a generic function and the first arg has a known struct type with generic args, infer concrete types and emit the specialized name. LOW RISK.
- [x] #37 Fix map entry type emission — `StringMapEntry(V)` and `IntMapEntry(V)` referenced by StringMap/IntMap fields are not discovered by the generic type emission pass. Walk struct field types recursively during type discovery. Fixes 370_map_basic (partial). MEDIUM RISK.
- [x] #38 CHECKPOINT 8: Final phase 3 evaluation. Target: 0 unit failures, ≤3 example failures.
- [x] #39 UI Phase 0: Scene-driven UI design doc + music-player screen PNGs in place. See `rae/examples/98_mobile_ui/DESIGN.md` and `rae/docs/royal-blush-ruics-ecs-reference.md`.
- [x] #40 UI Phase 1a: Add `lib/json.rae` — `JsonValue` tree (`Null/Bool/Number/String/Array/Object`) plus `parseJson(source)` recursive-descent parser and `jsonField/jsonInt/jsonFloat/jsonString/jsonBool` helpers. Tests covering primitives, nested arrays/objects, malformed input. ~150 LOC.
- [x] #41 UI Phase 1b: Add `lib/ui/ecs.rae` (`Entity = Int`, `ComponentTable(T)` sparse-set, `UiWorld` with named tables) + `lib/ui/components.rae` (all authored + derived component types from the design doc) + `lib/ui/registry.rae` (component name → deserialiser/applier table). World boots; can `add`/`get` components by hand.
- [x] #42 UI Phase 1c: Add `lib/ui/scene.rae` (`.raescene` JSON → in-memory `Scene` struct, validates `type=="Scene"`/`version==2`/root exists/children exist) and `lib/ui/scene_loader.rae` (Scene → ECS entities + components + ParentLink hierarchy). No layout yet — just dump the entity list.
- [x] #43 UI Phase 1d: Add `lib/ui/layout.rae` (two-phase `measure → compute` for `None/Horizontal/Vertical/Stack` with `Hug/Fill/Fixed` resolution, padding, `Align`, `Constraints`) + `lib/ui/transform.rae` (`ComputedRect + parent → WorldTransform`) + minimal `lib/ui/render.rae` painting `Shape:Rect` only. Album scene shows as a tower of grey boxes positioned correctly. **First user-visible milestone.**
- [x] #44 UI Phase 1e: Extend `lib/ui/render.rae` to `Sprite` (raylib texture), `Text` (`drawTextWithFont`), `Shape:RoundedRect`, `Shape:Circle`. Add `lib/ui/text_measure.rae` for hug-text using `measureText`. Add `lib/ui/textures.rae` with lazy texture loading from `assets/`. Ship 14 placeholder icons + Roboto. Album scene now reads as the real screen 2 (track rows still hand-laid, no scene instance yet).
- [x] #45 UI CHECKPOINT 9: Album scene renders correctly without scene instances; snapshot screenshot for regression watching.
- [x] #46 UI Phase 1f: Add `lib/ui/scene_instance.rae` — load nested `.raescene`, preserve mounting entity placement (`Rect/Size/Align/Offset/Constraints/OverflowPolicy/TransformFx`), apply `params.overrides` (target by `nodeId`/component/field). Replace hand-laid track rows in album scene with five `SceneInstance`s of `track-row.raescene`. Wire `nav-tabs.raescene` and `mini-player.raescene` similarly.
- [x] #47 UI Phase 1g: Add `lib/ui/input.rae` — gather pointer state, hit-test by `HitArea`/`PointerEvents`, dispatch `OnClick.actionId` to a registered action handler. Tabs and play button become tappable.
- [x] #48 UI Phase 2: Add `music-player-now-playing.raescene` (screen 1). Tab navigation switches between two scenes via `loadScene` + `prepareLayoutRootForLoad`-style root reuse.
- [x] #49 UI CHECKPOINT 10: Both screens reachable; review for stdlib gaps before committing to workout app screens. Decide what `lib/ui/list_view.rae` needs.
- [x] #50 UI Phase 3a — Material Symbols icon font: ship Google's Material Symbols variable `.ttf` to `examples/98_mobile_ui/assets/`, load into a new font slot (slot 1), add `lib/ui/icon_codepoints.rae` with name → codepoint mapping for the ~30 icons the music player needs (`arrow_back`, `more_horiz`, `play_arrow`, `shuffle`, `repeat`, `skip_previous`, `skip_next`, `favorite`, `favorite_border`, `add`, `download`, `search`, `home`, `library_music`, `whatshot`, `equalizer`, etc.). Extend `Sprite.textureKey` resolver in `render.rae` to detect a `mat:` prefix and route to `drawTextWithFont` via the icon-font slot. Drop the 14 placeholder PNGs once the music player no longer references them.
- [x] #51 UI Phase 3b — Album view structure rework: replace the current full-width hero with the reference layout — small square cover thumbnail (~80x80) on the left + title block (`Charcoal` h1, `Brambles` artist, `Album · 8 songs · 2012` caption) on the right. Add the three small action circles (add to playlist, download, more) below the row. Add the prominent black `Play` pill + outline `Shuffle` pill row. Match the reference's track-row layout: index OR mini-waveform indicator, song title with `Brambles · 2:52` artist+duration subtitle, trailing more icon. Bottom mini-player gets album thumb + title + heart + play (matching reference). All from `track-row.raescene` overrides.
- [x] #52 UI Phase 3c — Now-playing rework: shrink the hero to a square ~250x250 with rounded corners, position centred above the title row. Move the heart icon to the LEFT of `Charcoal` + artist subtitle, with `more_horiz` on the right. Replace the flat `ProgressBar` shape with a thin waveform line (Phase 1 just an SVG-style traced PNG sprite or hand-rolled; full audio waveform comes later). Add the big-circle `Play` button (centred ~64dia) flanked by smaller `skip_previous`/`skip_next` (~40dia) and `shuffle`/`repeat` outboard.
- [x] #53 UI Phase 3d — Bottom navigation: extend `nav-tabs.raescene` to four tabs (`Home`, `Search`, `Library`, `Hotlist`) using Material icons (`home`, `search`, `library_music`, `whatshot`). Each tab entity carries `OnClick: nav.<id>`; dispatcher in `main.rae` swaps screens accordingly. Active-tab styling lands as a Phase 4 polish pass.
- [x] #54 UI Phase 3e — Typography swap: drop in **Inter** (or **SF Pro fallback**) `.ttf` for the body font slot, retire Roboto for the music player. Verify hi-res atlas (96px) renders cleanly at the actual sizes used (h1 36, h2 28, body 18, caption 12). No SDF needed for fixed-size mobile screens — stay with raylib's bilinear-filtered glyph atlas.
- [x] #55 UI Phase 3f — Padding + sizing pass: extract pixel measurements from `screens/music_player_screen_1.png` and `_2.png`, update the size constants in `examples/98_mobile_ui/main.rae` and the dimensions in each `.raescene` to match. Goal: the snapshot baselines visually match the reference within ~2px tolerance on key landmarks (hero size, title baseline, button heights, padding).
- [x] #56 UI CHECKPOINT 11: Both music-player screens visually match the reference within tolerance. Capture new baselines; review what's missing before starting the workout app.
- [ ] #108 Fix qualified-call Live gap: `module.func(...)` (e.g. `checksums.crc32(...)`) compiles in Compiled but fails in Live with "unknown identifier '<module>' in VM". The sema method-call→call rewrite (sema.c:1395-1444) isn't taking effect on the VM/live path; prime suspect is the `!symbol_table_lookup(symbols, lhs)` guard at sema.c:1404. Repro: isolated project, `import compress/checksums` + `checksums.crc32`, run --target live vs compiled. Workaround: `open` + bare call. Blocks Live-mode namespacing.
- [ ] #115 [Tier 0→1] ECS draw-list extract (clip-in-render-walk DONE 2026-07-01, commit d465286; flat DrawList IR + raylib-removal deferred to parked #122): pure O(n) system walking UiWorld alive entities → flat DrawList {boxes,glyphs,images,clips}, sorted by (layer,clip,pipeline); no nested per-entity scans. Introduce lib/ui/draw.rae draw-sink interface (WebGPU impl) so lib/ui stops importing raylib. See doc §9.
- [ ] #116 [Tier 0→1] Example: ui_basic — simple data-driven UI (panel + buttons + list + toggle), render-only/minimal interaction. Proves ECS extract → DrawList, clipping, layering, text+box compositing.
- [ ] #117 [Tier 1] Offscreen render targets + Image pipeline (textured quads for icons/photos AND 3D render-target output) + premultiplied-alpha compositor ordering 2D passes and 3D targets onto the swapchain. See doc §5/§7.
- [ ] #118 [Tier 1] SDF clipping (box pipeline DONE 2026-07-01, commit 2f352c1; text/image rounded-clip follow-up): per-instance clipId indexing a uniform array of rounded-rect clip regions (rounded/nested clips, e.g. rounded card containing a scroll list); scissor stays the axis-aligned fast path. See doc §6.3.
- [ ] #120 [Tier 1, stretch] Example: ui_3d_embed — raytracer/spinning mesh inside a rounded UI card + fullscreen toggle. Proves render targets + compositor + 3D-in-UI/fullscreen-3D.
- [ ] #121 [Tier 2] Polish/effects: stencil arbitrary-path clip, separable blur/glow/shadow passes, compute-driven particles (via lib/gpu.rae) + particle pipeline, per-widget custom fragment shaders, static-subtree offscreen caching.
- [ ] #122 Remove raylib from lib/ui — PARKED (2026-07-01): NOT removing raylib. lib/ui keeps its raylib path (98 + raylib examples) alongside the gpu2d backend (lib/ui/render_gpu2d, 106). Both coexist; no module-by-module removal.
- [ ] #123 [raepack v2, design] Finalize v2 semantics AND syntax — resolve the provisional names + grammar in docs/raepack-v2-and-packages.md (executable/execution/module/embeds/requests/grants/delivery/overrides), the meta-vs-devtools split, and the interface-IDL direction (§15 Q1/Q4). Gate before any implementation. Design/spec only.
- [ ] #124 [raepack v2] Implement v2 parsing + normalized JSON output: parse the v2 schema (package meta, devtools block, dependencies, modules, targets, capabilities) in raepack.c; extend `rae pack --json` to emit the full normalized manifest incl. the devtools presentation block. Keep a v1 parse path temporarily.
- [ ] #125 [raepack v2] Make `rae run` / `rae watch` / `rae build` discover + honor a `.raepack` in the folder: read default target + entry + module composition from the pack (building on zero-config inference); `.raepack` stays optional (no manifest → existing zero-config defaults).
- [ ] #126 [raepack v2] Migrate devtools metadata to the pack + remove devtools.json: devtools consumes `rae pack --json` (incl. the devtools block) instead of readExampleMetadata; one-time script generates minimal `.raepack` (meta+devtools) from each devtools.json; delete devtools.json; folder-derived metadata for packless examples. Examples still list without a pack.
- [ ] #127 [raepack v2] Remove/migrate per-source emit + hybrid-as-backend: drop `emit: live|compiled|hybrid` per source and the `hybrid` target enum; migrate v1 packs (92_pong_import, 22_raepack_demo, 302_auto_app) to module boundaries; hybrid becomes native-host-embeds-wasm composition.
- [ ] #128 [raepack v2] Implement package-level module composition: modules defined once at package level (native|wasm + interface + capability request); targets select native modules and `embeds` wasm modules with per-target overrides (delivery, grants, jit).
- [ ] #129 [raepack v2] Design + implement typed native↔WASM interfaces with generated marshalling (§9): the interface IDL (open question #123), value/handle crossing rules, versioning, host-import/guest-export glue generation. Largest unknown — likely its own sub-design first.
- [ ] #130 [raepack v2] Implement capability validation + runtime enforcement (deny-by-default, §8): the three-way contract (host exposes / module requests / target grants); validate granted ⊆ requested ⊆ exposed at build+load; runtime denies ungranted host imports. A module request must never grant.
- [ ] #131 [raepack v2] Add dependency management: path + Git dependency sources, a `rae.lock` lockfile (pinned rev + content hash), resolver integration with the stdlib/module search (resolve_stdlib_root / try_resolve_lib_module). Registry deferred. Workspaces deferred.
- [ ] #132 [raepack v2] Investigate bundled vs OTA WASM delivery — separately for desktop and iOS (§11): non-JIT interpreted execution, integrity-check, versioning vs interface, rollback, capability-gating. Default bundled; OTA explicit + policy-controlled. No iOS approval claims. Investigation/spec before implementation.
- [ ] #133 [Live soft-deprecation] Formalize the Live test split (docs/live-vm-status.md): designate a tiny Live SMOKE subset (VM starts, basic call, basic alloc/ownership, one hot-reload) runnable as opt-in `TEST_TARGET=live` / a separate non-blocking job; mark the rest as historical-retained-but-disabled. Default `make test` is already Compiled-only.
- [ ] #134 [Live soft-deprecation] Strip `live` from ordinary examples' supportedTargets (and the future .raepack equivalent) so the UI never offers Live for them. Don't delete example code. Rides with the devtools.json→.raepack migration (#126).
- [ ] #135 [Live soft-deprecation] Relocate the Live hot-reload examples (23_code_hot_reload, 24_code_hybrid_hot_reload, 99_data_hot_reload, 100_code_hot_reload) under examples/legacy/live/ and/or hide them in devtools, so in-process VM hot reload no longer influences the normal workflow. Document the temporary capability gap (replacement = native-hosted WASM module replacement). Keep the example code.
- [ ] #136 [compiler bug, found fixing tests] Implicit project import loads the ENTRY file twice — when run without --project from a folder, the sibling-scan of the entry's own directory re-loads main.rae, so a type defined in the entry gets its auto-derived methods (toJson/fromJson/to_str + spawn thunk) emitted twice → C "redefinition" errors. Repro: a single-file program defining a type, run as `bin/rae run --target compiled tests/cases/337_list2_int/main.rae` from compiler/. Fix: exclude the entry file from the implicit sibling scan. Worked around in 337 with `run --no-implicit`.
- [ ] #137 [compiler bug, found fixing tests] Under --no-implicit, calling a core function (e.g. overloaded `log(value: Any)`) mangles to bare `rae_log` (undeclared) instead of the overload-mangled `rae_log_RaeAny_`, unless `open core` is present — so the C symbol name depends on open-ness. Fix the overload mangling to be independent of implicit-prelude/open state. Worked around in 383/384/398 with `open core`.
- [x] #138 C backend ownership fix: opt String / opt T cleanup must be representation-aware; root cause is an `is_opt`-blind base-type-only drop classifier applying a `rae_String` destructor to a `RaeAny` representation.
- [ ] #139 [compiler robustness] Parser SIGSEGVs (finish_call → parse_postfix) instead of erroring cleanly when a call argument name is invalid (e.g. a keyword used as an arg name). After printing "expected ':' after argument name" it crashes rather than recovering. Make finish_call fail gracefully.
- [ ] #141 [gpu2d, render-loop] lib/gpu2d.rae now exposes pointerX/pointerY (design units), pointerDown, wheelMove, nowSeconds, waitEvents, and RAE_GPU2D_SCREENSHOT (surface readback → BMP). example 105 is a self-contained data-driven mobile UI (ECS + scroll physics + hybrid loop + nav routing + theme text). Remaining for true 98 parity: this is NOT yet driven by the same render path as the raylib lib/ui.
- [ ] #151 [responsive] Safe-area Phase 2 — phone-in-window render: when a device preset is active, render the world into a preset-sized offscreen target and centre it in the actual window with a device frame (rounded bezel, faked status bar). See docs/ui-viewport-and-safe-area-plan.md Phase 2. Deferred until a product reason (depends on #117 offscreen render targets).
- [ ] #152 [responsive] Safe-area Phase 3 — editor camera: wrap the phone-in-window blit in a zoom/pan camera (wheel-zoom-around-cursor, middle-drag-pan) with inverse-transform pointer mapping so input still hits the right widget at non-1x zoom. See plan Phase 3. Deferred.
- [x] #195 Make tests and examples for DATA hot reload, or UI LAYOUT hot reload, which ever term is better.
- [x] #196 Implement docs/ownership-model.md: `own` keyword (expression `own x` + parameter mode `own T`), move detection only on `own` / `ret`, then re-enable the two commented-out `emit_implicit_drops_for_body` call sites in `compiler/src/c_backend.c`. Unblocks `416_memory_leak_check`; must keep `411_json_parser` / `412_ui_ecs` / `413_scene_loader` / `414_ui_layout` passing. See `docs/ownership-model.md` for the full spec and test list (A–F).
- [x] #197 Do option 1 first: verify the codegen ownership path before applying the fix.  This is very likely the right fix, but because pool registration changes ownership/lifetime semantics globally, please confirm the existing `take` behavior first.  Specifically verify:  1. `let s: String = a + b`    The concat result must be detached from the string pool with `rae_string_pool_take(...)`, so it survives statement-scope flush and is later dropped as local owned String.  2. `ret a + b`    The return epilogue must detach the concat result with `rae_string_pool_take(...)`, so it survives function cleanup/flush.  3. `foo(a + b)` where `foo` takes `view String`    The concat result should remain pool-owned and be flushed after the statement.  4. Nested concat / int-to-string:    `"x" + i.toString()` should not leak intermediate concat or int_to_str results.  If those ownership paths are already correct, apply the small fix:  - register `rae_ext_rae_str_concat` results with the string pool - register `rae_str_from_buf_impl` results with the string pool - register `rae_str_from_cstr_impl` results if it creates owned heap strings  Then run: - full compiler tests - all examples - 20K Compiled mobile UI stress with `RAE_MEM_STATS=1`  Success criteria: - concat outstanding goes to ~0 - int_to_str outstanding goes to ~0 - no double-free regressions - mobile UI stress slope drops close to zero  Do not broaden the patch unless verification shows missing `pool_take` codegen.
- [x] #198 Investigate where post-pool_take concat results escape ownership. After #197, int_to_str outstanding went 370K → 50K (-86%) but concat stayed at 801K. The pool_take codegen IS firing on concat RHS (verified in c_stmt.c:438/528/596) so the result IS removed from the pool — but the downstream owner (struct field assignment, list element add, ret-from-helper returning String, etc.) never frees it at its own scope exit. Diagnose by: (a) running mobile UI stress at 5K with extra logging from inside rae_mem_str_untag tracking which call path frees the few concats that DO get freed; (b) inspecting generated C for one buildScreenWorld helper to see whether `component.style = concat(...)` lowers to a path that loses the str_free at component drop; (c) check whether Layer 5 synth'd drop is missing String fields. Likely outcome: cascade-drop gap for struct field reassign OR a List<String>.add path that doesn't tag the element as owned.
- [x] #200 that is way too much. maybe halve it to 16?
- [x] #201 Re-enable play-history persistence in 98_mobile_ui — root cause was NOT the buf_get cascade-drop. Two real bugs: (a) `own String` helper params get double-freed (caller emits drop AFTER the call AND callee drops at fn exit), (b) `lib/string.replace` returns an aliasing String on some no-match shapes. Worked around at user-code level (inline concat chains + per-char jsonEscape). Roundtrip verified.
- [x] #202 Compiler fix: `own String` param double-free. Caller-side codegen emits `rae_string_drop(&src); content = pool_take(call_result)` after invoking a function with `content: own String` param, while the callee also drops the param at function exit → "POINTER_BEING_FREED_WAS_NOT_ALLOCATED" abort. Either suppress the caller-side drop when the source variable was move-transferred to a `own` param, or change callee to skip its param drop after the value is consumed. Repro pattern was in examples/98_mobile_ui/history_io.rae's old appendStringField helpers (now removed).
- [x] #203 Compiler/stdlib fix: `lib/string.replace` returns aliasing String. The function has `if old.length() is 0 { ret this }` and the split+join path also produces a String that, in some shapes, ends up sharing storage with `this` (or with whatever was assigned to the concat accumulator most recently). Visible symptom: a long pipeline of `r = r.replace(...).replace(...); ret r` returned values that, when subsequently embedded into another concat chain, contained bytes from the OUTER accumulator. Either make `ret this` deep-copy, or audit the split+join branches for alias return.
- [x] #204 Frames UI architecture: authoritative design doc landed at rae/docs/ui-coordinate-and-responsive-layout.md (named frames + shared entity graph + per-frame overrides + stable identity/reparenting + local ScaleToFit boards; design-unit/unitScale model, DPR separate). Superseded the old viewport plan (annotated, kept) + deleted interim adaptive-layout draft. Committed in rae submodule.
- [x] #205 Coordinate-unit prototype: examples/101_coord_proto — runnable experiment (200du button + 1080x1600 board in phone 1080x2280 vs desktop 5760x3240 frames, unitScale soft/live via UP/DOWN, SPACE toggles frame). Compiles+links. Awaiting Jonas's judgment on whether to lock unitScale=3. See doc "core coordinate decision" + open decisions.
- [x] #206 Coordinate-system decision cleanup: document that 106_mobile_ui is the primary gpu2d/SDL3 target for Model A (`unitScale=3`, 1080-wide phone frame, desktop ~5760du wide), while 98_mobile_ui remains legacy/reference unless explicitly revived.
- [x] #207 Frames S1 remaining — finish the 106_mobile_ui coordinate spine now that `Viewport`, `unitScale=3`, DPR logging, scene `unitScale` metadata, and legacy scene scaling exist: remove remaining module-global layout authority, make pointer/input conversion consistently use Viewport design units, define the approved pixel-snapping policy in gpu2d render, and prove the legacy 498×1079 adapter still works before the full 1080×2280du rescale. See rae/docs/ui-coordinate-and-responsive-layout.md §10 S1.
- [x] #208 106 coordinate migration S1 follow-up: after #207, remove remaining legacy `screenW/screenH` authority, pass Viewport/layout extent explicitly through 106 systems, convert old 498×1079 scene constants toward 1080×2280 du, and keep 98 out of scope unless a separate legacy-backend parity task is opened.
- [x] #209 Frames S2 — safe areas + device presets + simulator for 106_mobile_ui: add safe-area data in du, forcedFrameId/device preset selection, and keep gpu2d screenshots deterministic; §10 S2.
- [x] #210 Frames S3 — frame selection + per-frame component overlay for 106_mobile_ui: frame table, reset-to-base + applyComponentByName overlays, add removeComponentByName, and prove phone frame vs desktop frame without reparenting; §10 S3.
- [x] #211 Frames S4 — active-hierarchy derivation for 106_mobile_ui: authored vs active graph, reparent + order, cycle detection, and phone-stack↔desktop-split demo; §10 S4.
- [x] #212 Frames S5/S6 — within-frame states and art direction for 106_mobile_ui: phone canvas toggle, frame-only entities, Aspect/ScaleToFit, Sprite cover/contain, and GameBoard sample; §10 S5-S6.
- [x] #213 106 scene-driven Home migration: replace the current programmatic Home page builder with `.raescene` templates plus data adapters for album grids and recently played rows; keep only data extraction and list materialisation in code, with layout/styling/actions represented as scene/ECS components.
- [x] #214 106 render/update separation: split per-frame systems into data sync, UI diff/rebuild triggers, layout/transform, animation, input dispatch, and gpu2d render; remove direct component patching from action handlers where a component-state change should trigger a dependent UI rebuild.
- [x] #215 106 typed UI data resources: replace today’s string-key `AppDataSources`/`DataDependency` revision model with typed app-level resources for albums, history, playback, playlists, Spotify status, settings, and debug state so mounted UI regions refresh narrowly without stringly-typed keys.
- [x] #216 Cleanup obsolete `examples/105_gpu2d_mobile_ui`: 106 is now the primary gpu2d/SDL3 mobile UI example, so either delete 105 or reduce it to a tiny renderer smoke example after checking for any snippets not already copied into 106.
- [x] #217 Research cross-platform image decoding for gpu2d: evaluate JPEG/WebP/PNG loaders beyond macOS ImageIO (stb_image, libjpeg-turbo, platform APIs), decide supported formats, licensing, build integration, and fallback/error policy; do not implement until design is chosen.
- [x] #218 Implement robust non-blocking Spotify/history artwork loading for 106_mobile_ui only: restored `spotify-art:*` entries populate from cache/background fetch without blocking frames; 98_mobile_ui and raylib are legacy and do not need feature parity.
- [x] #219 UI optical centering follow-up: `OpticalAlign` exists in gpu2d, but complete the data-oriented visual-bounds system with derived `VisualBounds`/text metrics, glyph-plane or alpha bounds for Material icons, cached measurement, scene-loader support, and removal of per-widget bias hacks where measured centering can replace them.
- [ ] #220 C backend follow-up: generic object literals with owned `String` fields still need concrete field substitution before return/drop ownership decisions; `Wrapper(String) { value: ownedString(...) }` can leave the generated field treated as raw `T`, causing invalid String ownership independent of the `opt T` drop classifier.
- [ ] #221 Compiler diagnostic location bug: preserve the original source file when reporting immutable-let reassignment errors across imported modules; repro reported `examples/98_mobile_ui/render_decide.rae:46` while the edited binding was in `main.rae`.
- [ ] #222 Compiler: diagnose silent move of an aliased String into an `own` param — `let x: String = someGlobal` then `f(pageId: x)` where the param is `own String` frees the global's buffer and dangles it (bit 106's mini-player pageId; garbage reads/SIGABRT far from the cause). Require explicit `own x` at call sites for own params, and reject/copy when the moved value shallow-aliases another binding.
- [ ] #223 Compiler: parser SIGSEGV on a bare `ret` statement followed by unreachable declarations inside a func body (found while bisecting 106); should parse and at most warn about unreachable code.
- [x] #224 UI library backend split: raylib UI code now lives under explicit `ui/legacy_raylib/*`, 106 imports backend-neutral UI core plus `ui/render_gpu2d`, and future apps no longer need the old “do not import ui/render” audit rule; remaining 98 legacy cleanup should be separate.
- [x] #225 106 data-oriented widget model: ECS widget identity/state/dependency/action/style foundations landed (`WidgetId`, `WidgetState`, `DataDependency`, `ActionBinding`, `StateStyle`) and controls/rows/debug toggles are tagged; follow-up typed resources remain in #215 and update separation remains in #214.
- [x] #226 106 widget parity polish after data migration: mini-player root, debug toggles, bottom-sheet actions, playback/like controls, track rows, album/playlist tiles, loading placeholders, hover scale, and all-screen gpu2d snapshots are covered by recent 106 widget commits; remaining architectural cleanup belongs in #213/#214/#215.
- [x] #227 on desktop it seems that web UI has buttons that accept button presses have a finger cursor. Adapt that cursor idea to Rae UI so that some input components automatically take on certain standard kind of mouse cursors on desktop, and switch back to arrow when no longer hovering them. This should be automatic and data oriented based on some component types that fit this idea.
- [ ] #228 Finish docs/image-decoding-design.md (#217) follow-up: stb JPEG is vendored and raylib fallback removed, but valid Spotify JPEGs fail in stb on macOS (`can't merge dc and ac`), so keep ImageIO as the macOS default, gate stb testing behind `RAE_G2D_STB_JPEG=1`, add corrupt-file/error-policy coverage, and decide the non-macOS JPEG strategy before marking complete.
- [x] #229 Implement docs/ui-theme-system.md — SPLIT into #230-#237 below (gpu2d/106 only; NO raylib/98 back-compat — if 98 blocks a step, remove example 98 instead of maintaining its style tables).
- [x] #230 Theme M0 — theme sections + loader: NO new file format — theme data lives in a `.raescene` as top-level sections (palette/text/space/radius/shadow/padding), parallel to how scenes already carry `frames`/`states`; read them out of a scene JsonDoc with the existing flat-pool parser into a ResolvedTheme of flattened parallel-list tables; single-parent `extends` flattened at load with depth cap 4 and loud cycle/unknown-parent errors; `themeDumpResolved()` debug dump; a default `theme.raescene` transcribing today's hardcoded values; nothing consumes it yet. See docs/ui-theme-system.md §3-§5, M0.
- [x] #231 Theme M1 — single text-style resolver: text_style.rae resolves styleId through ResolvedTheme; delete the duplicated g2dTextStyleSize/Color/IsCentered/IsRightAligned tables in render_gpu2d.rae; ui/legacy_raylib keeps its own copy untouched (out of scope, no back-compat work). Gate: byte-identical 106 screenshots. §10 M1.
- [x] #232 Theme M2 — shadows into styles (repairs e39a378): shadow token table; miniTitle/miniMuted styles reference textOnAccent/textOnAccentSoft shadow tokens in the theme file; delete the raw TextShadow blocks from mini-player.raescene; keep the TextShadow component only as the last-resort override and add the scene-loader lint line for raw usage. Gate: 106 screenshots identical. §2, §7, M2.
- [x] #233 Theme M3 — radius + padding tokens: category-aware token resolution in the scene deserialiser (extend the optFloatOrToken pattern to CornerRadius.radius, Shape.radius, Padding insets); add radiusChip/radiusCard/radiusPill + cardPadding-class presets to the theme file; sweep 106 scenes' repeated raw radii/paddings onto tokens with identical values. §5, M3.
- [x] #234 Theme M4 — 106 spacing sweep: replace raw gap/padding floats in 106 scenes and builders with the existing spaceXS-spaceXL tokens (values identical; keep the literal+comment convention only where module-level lets force it). Gate: byte-identical screenshots. §8, M4.
- [x] #235 Theme M5 — live theme switching: store palette slot NAMES instead of parse-time-baked RGBA in scene-authored colors; resolve appearance through the single accessor keyed by a theme generation counter, so dark/light toggle and theme-scene hot-reload become a repaint (no scene re-parse / world rebuild). Largest slice; land last of the value-neutral ones. §6, M5.
- [x] #236 Theme — explicit partial overrides: `styleOverride` sparse per-entity block over the resolved style (base styleId always kept); document + lint the override friction ladder (derive a style >> styleOverride >> raw component). §7.
- [x] #237 Theme M6 — composite button/container styles: named bundles referencing palette slots + radius/padding/text/icon tokens (`primaryButton`-class); StateStyle variants reference theme styles per widget state instead of carrying raw values; depends on widget-model tasks #214/#215. §5 tier 6, M6.
- [ ] #238 Compiled backend: `List(Task(T))` element access lowers to `RaeAny` instead of `RaeTask*` — can't hold task handles in a `List` (generic-container element monomorphization). Workaround: use individual `Task` locals. Found building examples/40_raytracer.
- [ ] #239 Compiled backend: `List(Struct).get()` returns boxed `RaeAny` and isn't auto-unboxed to the struct (must use low-level `rae_ext_rae_buf_get`). Generalize typed-struct-list element get(). Workaround in raytracer: flat `List(Float)` struct-of-arrays.
- [ ] #240 Language: add real `const` declarations (compile-time, immutable). Today "constants" are zero-arg functions (e.g. `func PI()`/`func TAU()` in lib/math.rae) because Rae has no immutable value binding (`let` is reassignable). Proposed: `const NAME: Type = <const-expr>` at module (and local) scope; UPPER_SNAKE naming; const-expr folding (literals + ops on consts); reassignment is a compile error; folded/inlined in both backends (VM constant pool, C `static const`/literal). Then convert PI()/TAU() to `const PI`/`const TAU` and drop the call `()`.
- [ ] #241 Lint: optional compiler/linter WARNINGS for naming-convention violations (Types/enums PascalCase; functions/vars/consts/enum-cases camelCase). Must be warnings, not errors; exempt/suppressible for extern (C-binding) names. Needs a general warning framework first.
- [ ] #242 Examples: raytracer step 6 — GPU raytracer (deferred; needs a compute/GPU path).
- [ ] #243 Strategy: execution targets & deployment decided — see docs/execution-targets-and-deployment.md. No TypeScript target; WASM (via the C backend) is the primary app/game target; C backend stays for native/desktop/real-time/hardware; Live VM kept on probation.
- [ ] #244 Prototype: WASM generation via the C backend (clang/wasi-libc or Emscripten). Compile the raytracer to .wasm, run from a tiny JS harness writing into a SharedArrayBuffer drawn to a <canvas>. Measure WASM-vs-native speed. (Highest value-per-effort; gates the rest.)
- [ ] #245 Prototype: WASM threads (SharedArrayBuffer + workers) + SIMD128 for the tiled raytracer — confirm real parallelism in the sandbox.
- [ ] #246 Prototype: iPhone PoC app with OTA updates — Capacitor-style WKWebView shell running the Rae→WASM app, WASM/JS bundle hosted remotely and updated over-the-air without App Store resubmission (native shell fixed, WASM layer updatable). Confirm the App-Store-legal split.
- [ ] #247 Prototype (if needed): one Rae→C native plugin driven at control-rate (e.g. audio) to validate the partition-by-data-rate bridge; Rae generates both ends.
- [ ] #248 Later: WebGPU path from the WASM app (GPU compute / GPU raytracer step 6).
- [ ] #249 Strategy: cross-platform tech stack & dependency policy decided — see docs/tech-stack-and-dependencies.md. Preference ordering Rae > C > any-lang-behind-clean-C-API > Rust > C++ > Obj-C; own small/controllable, buy hard domains, all foreign code behind Rae-owned APIs. Relax strict no-C++/no-Rust. Intended stack: SDL3 (platform) + WebGPU-everywhere/WGSL (render) + miniaudio (audio) + Rae-owned layers.
- [ ] #250 Deprecate raylib: PARKED (2026-07-01) — decided NOT to remove raylib for now (wasted effort). raylib (98_mobile_ui + raylib examples) and the gpu2d/WebGPU stack (106_mobile_ui + lib/ui/render_gpu2d) coexist side by side; lib/ui keeps its raylib path and gains a gpu2d backend. Revisit only if raylib becomes a real maintenance/deploy burden. Build the Rae Platform/Render/2D/Audio layers as additive, not as replacements.
- [ ] #251 GPU denoiser / temporal reprojection for the interactive path tracer (example 52/53). Motion can't accumulate across frames (camera moved → reset every frame), so it's limited to spp samples = noisy while moving. Reuse the previous frame's result warped to the new camera (reproject by re-projecting world hit points / or screen-space motion vectors) so accumulation survives camera motion (TAA-style), optionally + an à-trous / edge-aware spatial denoise pass. Cleanest motion at high fps; needs a history buffer + per-pixel reprojection in the WGSL. Alternative cheaper lever already scoped: auto-drop render resolution while moving (reuses the P-toggle machinery). Effort: large.
- [ ] #252 Strategic (parallel track): iPhone OTA proof-of-concept. Wrap the wasm app in a WKWebView/Capacitor shell, host the bundle remotely, demonstrate over-the-air updates (native shell fixed, wasm/JS layer updatable). Proves the deployment thesis (WASM-first, OTA-legal on iOS).
- [ ] #253 Incremental raylib removal — PARKED (see the Deprecate-raylib note): raylib and gpu2d coexist; we're not retiring raylib examples now.
- [ ] #254 (gated dogfooding) Own DEFLATE + the PNG container in Rae. Inflate first (~400-600 LOC; decode is what texture loading needs), validated against lodepng; then deflate (fixed-Huffman first, then LZ77 hash-chain match-finder). Shared later with .raepack/gzip. lodepng stays as the correctness+speed reference oracle; drop only once the Rae path passes round-trip + zlib-interop + acceptable-speed gates. ~1500-2500 LOC across bitio/huffman/inflate/deflate/checksums/png modules. See docs/png-and-deflate-strategy.md. Effort: large.
- [ ] #255 Module namespacing for the stdlib (Option A) — see docs/module-namespacing.md. Stdlib accessed via module qualifier (math.sin, filesystem.exists, gpu.reset); project files/subfolders keep flat auto-visibility; core stays global. Dot resolution: LHS that is an in-scope stdlib module name => namespace qualifier, else value => normal UFCS; unqualified UFCS must NOT search stdlib namespaces (collision risk). Phase 1 (additive, front-end only ~100-150 LOC in main.c merge_module_graph + sema.c method_call): stamp each decl with its module_path, resolve `mod.func(args)` when LHS is a module name (rewrite to plain call, no receiver) alongside existing flat calls. Phase 2: migrate stdlib+examples module-by-module (suite green each step). Phase 3: drop flat auto-load of non-core stdlib. Optional: `import ... as` alias; namespace-qualified UFCS (path.filesystem.exists()).
- [ ] #256 lib/filesystem.rae — Rae Filesystem & Paths API (namespaced), wrapping SDL3's SDL_filesystem.h (known folders incl. Desktop/Pictures, list/glob/getPathInfo, makeDir/remove/rename) + pure-Rae path helpers (join/baseName/dirName/ext). See docs/filesystem-and-paths.md. Then fix the raytracer PNG save: target = Desktop (else prefDir/render), filename rae_render_<ISO-date>_<NNNN>.png via glob-scan next-index (no overwrite). Needs a runtime strftime date helper. Depends on module namespacing.
- [ ] #257 Namespacing test-case debt — ~38 tests/cases fail with "X is in package Y, which is not open here" (json/hot_reload/file_watch/time/raylib) after the module-namespacing migration. Add the needed `open`/qualified calls (or per-case prelude) so `make test` is green again. Pre-existing, non-blocking; unrelated to recent compiler changes (verified: 525_bitwise_ops passes both targets, no codegen regressions).
- [ ] #258 Theme M6 follow-up — container styles: apply a container style's `text` style to the node's child Text entity (needs a child-propagation pass at scene-mount; today the `text` field is parsed+carried but not applied), and add `icon` styles (§5 tier 5, never built) so a container style can reference an icon size+tint bundle. lib/ui/container_style.rae currently expands only background/radius/padding.
- [ ] #259 Theme M5 follow-up — migrate remaining 106 code-driven colors to slots so the theme toggle drops its world rebuild (pure repaint). The page background now theme-tracks via `pageBgColor`+the `background` palette slot, but the nav-bar (surface@238), pill, and gradient blends still bake at world-build, so the toggle keeps `switching=true`. Blocker: those carry per-instance alpha/blend a bare slot can't model — needs slot+alpha composition. (Re-adds the follow-up lost when the queue was renumbered.)
- [x] #260 ECS E0a — establish `ComponentTable(T)` (lib/ui/ecs.rae) as THE ECS table primitive (the design's conceptual "Table(T)"): it already carries the table-level revision as its `generation` counter (kept under that name — more general than "revision", agnostic to insert/remove/reorder; no rename just to mirror the doc). Added unit test 534_component_table_revision asserting `generation` bumps once on set/replace/mod/remove and NOT on a no-op remove; documented in ecs.rae + doc §2 that ComponentTable IS the primitive and `generation` IS the table revision. No separate `Table(T)` type, no duplication.
- [x] #261 ECS E0b — instance + system revision conventions: component instances carry `revision: Int` bumped EXPLICITLY via `bumpRevision(...)`/bump-after-change helpers (no implicit bumping on `mod`); a system is a plain `type` of Table fields + `revision: Int` + free `markChanged(sys)`; eager bump chain component→(table if structural)→system. Document the convention + unit test with a toy system. §5/§7 of the doc.
- [x] #262 ECS E1a — `SystemRegistry` + `SystemId`: registry owns registered systems, handles are IDs (never pointers); access surface exposes only `view` reads and command sends — `mod` never crosses a system boundary. Decide the Rae shape for heterogeneous registration (per-type fields first; compiler feature only if genuinely needed). §6/§8 of the doc.
- [ ] #263 ECS E1b — generic `Observe` + `refreshObservers`: `Observe { system: SystemId, kind: component|table|system, target ids, observedRevision }` component + ONE generic refresh pass (`if current != observed { rebuild; observed = current }`) replacing the per-domain refreshXViewsIfNeeded pattern; runs in the observation phase; old DataDependency path stays in parallel until #268. §6/§8.
- [ ] #264 ECS E2a — frame phases + command inbox + SYSTEM LIFECYCLE contract: define the 106 frame pipeline Input → Apply(A) → Observation refresh → Apply(B) → Layout → Render (two apply passes per frame, §4/§4.1); add a simple `Queue(T)`-style inbox primitive; and define the shared lifecycle every system implements in a fixed execution order — initialize / processCommands (drains own inbox holding `mod self`) / update(dt) (timers, animation, polling, async harvest — own tables only) / shutdown, with observation driven externally by the refresh pass (§6). One documented contract so no system invents its own lifecycle later. Restructure 106's main loop onto these named phases without behavior change.
- [ ] #265 ECS E2b — carve out `PlaybackSystem` (first actor): plain `type` with inbox + the #264 lifecycle contract (first real implementer — validates the contract); `nowPlaying` table (track/art/position/isPlaying); Spotify becomes integration functions called from `update()` that write ONLY PlaybackSystem's own tables + bump revisions; play/pause/like become commands. 106's playback state migrates off PlaybackState/AppState. §3/§5.1/§6 of the doc.
- [ ] #266 ECS E2c — carve out `MediaLibrarySystem`: albums/tracks/artists/thumbnail tables out of AppState; loaders write via its own functions; library/history views read via `view`. Split eagerly — history may become its own PlayHistorySystem if it grows. §5.1.
- [ ] #267 ECS E2d — per-feature UI systems: split 106 UI into UiPlaybackSystem (mini-player + now-playing widgets) first, then others (UiMenuBarSystem for the dock/nav) as follow-ups; persistent widget state (focus/selection/scroll) lives here; they observe data systems and send commands (never write foreign tables). §5.2.
- [ ] #268 ECS E3 — port consumers to Observe (FIXES the stale mini-player cover bug): cover + play/pause icon observe PlaybackSystem.nowPlaying instance revision and re-point by identity; delete rebindAlbumCoversToSpotify + pollSpotify's direct UI writes; title/artist/time labels go the same way; verify: play track A, then B from same screen → cover follows. §1/§9 of the doc.
- [ ] #269 ECS E4 — delete the resource enums: remove DataResourceId/DataFieldId/AppDataResources/bumpXResource once every consumer is on Observe; suite green. §8/§10.
- [ ] #270 ECS E5 — retained render cache (dependency-driven): ComputedRect pool with per-entry input-revision dependencies (window size, parent layout, text, theme tokens) → dirty on dependency change, reuse otherwise; glyph layout + GPU buffer reuse under the same rule; list virtualization (render only visible rows, O(visible) not O(total)). Largest slice; land after E3 proves observation. §5.3/§11.4. Effort: large.
- [ ] #271 ECS E6 — threading proof (one system off-thread): move one suitable system (asset loading or a compiler-worker stand-in) onto a spawn'd thread; its inbox becomes a cross-thread channel with the SAME send API (transport hidden, §3.2); observation stays read-only snapshots/revisions. Builds on the raytracer spawn machinery; drives whatever compiler work real cross-thread channels need — no hacks. Effort: large.
- [ ] #272 Compiler papercut — passing an rvalue (e.g. an enum literal `SystemId.playback`) to a `view` param fails to compile: "cannot take the address of an rvalue". The C backend lowers `view` args to `&expr` without materialising a temp for rvalues. Either materialise an lvalue temp for rvalue `view` args, or diagnose clearly. Workaround today: pass small enums/handles BY VALUE (the correct idiom anyway), or bind to a local first. Low priority; surfaced in #262.
- [ ] Compiled backend: `List(Task(T))` element access lowers to `RaeAny` instead of `RaeTask*` — can't hold task handles in a `List` (generic-container element monomorphization). Workaround: use individual `Task` locals. Found building examples/40_raytracer.
- [ ] Compiled backend: `List(Struct).get()` returns boxed `RaeAny` and isn't auto-unboxed to the struct (must use low-level `rae_ext_rae_buf_get`). Generalize typed-struct-list element get(). Workaround in raytracer: flat `List(Float)` struct-of-arrays.
- [ ] Language: add real `const` declarations (compile-time, immutable). Today "constants" are zero-arg functions (e.g. `func PI()`/`func TAU()` in lib/math.rae) because Rae has no immutable value binding (`let` is reassignable). Proposed: `const NAME: Type = <const-expr>` at module (and local) scope; UPPER_SNAKE naming; const-expr folding (literals + ops on consts); reassignment is a compile error; folded/inlined in both backends (VM constant pool, C `static const`/literal). Then convert PI()/TAU() to `const PI`/`const TAU` and drop the call `()`.
- [ ] Lint: optional compiler/linter WARNINGS for naming-convention violations (Types/enums PascalCase; functions/vars/consts/enum-cases camelCase). Must be warnings, not errors; exempt/suppressible for extern (C-binding) names. Needs a general warning framework first.
- [ ] Examples: raytracer step 6 — GPU raytracer (deferred; needs a compute/GPU path).
- [ ] Strategy: execution targets & deployment decided — see docs/execution-targets-and-deployment.md. No TypeScript target; WASM (via the C backend) is the primary app/game target; C backend stays for native/desktop/real-time/hardware; Live VM kept on probation.
- [ ] Prototype: WASM generation via the C backend (clang/wasi-libc or Emscripten). Compile the raytracer to .wasm, run from a tiny JS harness writing into a SharedArrayBuffer drawn to a <canvas>. Measure WASM-vs-native speed. (Highest value-per-effort; gates the rest.)
- [ ] Prototype: WASM threads (SharedArrayBuffer + workers) + SIMD128 for the tiled raytracer — confirm real parallelism in the sandbox.
- [ ] Prototype: iPhone PoC app with OTA updates — Capacitor-style WKWebView shell running the Rae→WASM app, WASM/JS bundle hosted remotely and updated over-the-air without App Store resubmission (native shell fixed, WASM layer updatable). Confirm the App-Store-legal split.
- [ ] Prototype (if needed): one Rae→C native plugin driven at control-rate (e.g. audio) to validate the partition-by-data-rate bridge; Rae generates both ends.
- [ ] Later: WebGPU path from the WASM app (GPU compute / GPU raytracer step 6).
- [ ] Strategy: cross-platform tech stack & dependency policy decided — see docs/tech-stack-and-dependencies.md. Preference ordering Rae > C > any-lang-behind-clean-C-API > Rust > C++ > Obj-C; own small/controllable, buy hard domains, all foreign code behind Rae-owned APIs. Relax strict no-C++/no-Rust. Intended stack: SDL3 (platform) + WebGPU-everywhere/WGSL (render) + miniaudio (audio) + Rae-owned layers.
- [~] Deprecate raylib: PARKED (2026-07-01) — decided NOT to remove raylib for now (wasted effort). raylib (98_mobile_ui + raylib examples) and the gpu2d/WebGPU stack (106_mobile_ui + lib/ui/render_gpu2d) coexist side by side; lib/ui keeps its raylib path and gains a gpu2d backend. Revisit only if raylib becomes a real maintenance/deploy burden. Build the Rae Platform/Render/2D/Audio layers as additive, not as replacements.
- [ ] GPU denoiser / temporal reprojection for the interactive path tracer (example 52/53). Motion can't accumulate across frames (camera moved → reset every frame), so it's limited to spp samples = noisy while moving. Reuse the previous frame's result warped to the new camera (reproject by re-projecting world hit points / or screen-space motion vectors) so accumulation survives camera motion (TAA-style), optionally + an à-trous / edge-aware spatial denoise pass. Cleanest motion at high fps; needs a history buffer + per-pixel reprojection in the WGSL. Alternative cheaper lever already scoped: auto-drop render resolution while moving (reuses the P-toggle machinery). Effort: large.
- [ ] Strategic (parallel track): iPhone OTA proof-of-concept. Wrap the wasm app in a WKWebView/Capacitor shell, host the bundle remotely, demonstrate over-the-air updates (native shell fixed, wasm/JS layer updatable). Proves the deployment thesis (WASM-first, OTA-legal on iOS).
- [~] Incremental raylib removal — PARKED (see the Deprecate-raylib note): raylib and gpu2d coexist; we're not retiring raylib examples now.
- [ ] (gated dogfooding) Own DEFLATE + the PNG container in Rae. Inflate first (~400-600 LOC; decode is what texture loading needs), validated against lodepng; then deflate (fixed-Huffman first, then LZ77 hash-chain match-finder). Shared later with .raepack/gzip. lodepng stays as the correctness+speed reference oracle; drop only once the Rae path passes round-trip + zlib-interop + acceptable-speed gates. ~1500-2500 LOC across bitio/huffman/inflate/deflate/checksums/png modules. See docs/png-and-deflate-strategy.md. Effort: large.
- [ ] Module namespacing for the stdlib (Option A) — see docs/module-namespacing.md. Stdlib accessed via module qualifier (math.sin, filesystem.exists, gpu.reset); project files/subfolders keep flat auto-visibility; core stays global. Dot resolution: LHS that is an in-scope stdlib module name => namespace qualifier, else value => normal UFCS; unqualified UFCS must NOT search stdlib namespaces (collision risk). Phase 1 (additive, front-end only ~100-150 LOC in main.c merge_module_graph + sema.c method_call): stamp each decl with its module_path, resolve `mod.func(args)` when LHS is a module name (rewrite to plain call, no receiver) alongside existing flat calls. Phase 2: migrate stdlib+examples module-by-module (suite green each step). Phase 3: drop flat auto-load of non-core stdlib. Optional: `import ... as` alias; namespace-qualified UFCS (path.filesystem.exists()).
- [ ] lib/filesystem.rae — Rae Filesystem & Paths API (namespaced), wrapping SDL3's SDL_filesystem.h (known folders incl. Desktop/Pictures, list/glob/getPathInfo, makeDir/remove/rename) + pure-Rae path helpers (join/baseName/dirName/ext). See docs/filesystem-and-paths.md. Then fix the raytracer PNG save: target = Desktop (else prefDir/render), filename rae_render_<ISO-date>_<NNNN>.png via glob-scan next-index (no overwrite). Needs a runtime strftime date helper. Depends on module namespacing.
- [ ] Namespacing test-case debt — ~38 tests/cases fail with "X is in package Y, which is not open here" (json/hot_reload/file_watch/time/raylib) after the module-namespacing migration. Add the needed `open`/qualified calls (or per-case prelude) so `make test` is green again. Pre-existing, non-blocking; unrelated to recent compiler changes (verified: 525_bitwise_ops passes both targets, no codegen regressions).

<!-- SUMU_QUEUE_END -->
