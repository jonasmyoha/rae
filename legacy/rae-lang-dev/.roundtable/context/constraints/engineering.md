# Engineering constraints

- React + TypeScript shared UI codebase.
- Desktop wrapper is Tauri and mobile wrapper is Capacitor.
- Cloud is only for signaling and matchmaking.
- Persist Roundtable artifacts inside .roundtable workspace folder.
- Keep file writes deterministic and merge-friendly.
