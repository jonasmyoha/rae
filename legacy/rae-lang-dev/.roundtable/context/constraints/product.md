# Product constraints

- Keep changes scoped and deterministic.
- Keep user-facing behavior voice-first.
- Avoid introducing features outside the active queue scope.
- Keep participant count at least 2 (current 3).
- Respect configured max rounds (current 3).
- Respect per-provider retry budget (current 1).
- Respect per-provider timeout window (current 1200 ms).
- Require explicit human approval before any code apply action (current not approved).
