# IO library for Rae
extern func rae_log_cstr(text: String)
extern func rae_log_stream_cstr(text: String)

func log(value: Any) {
  # log is currently handled as an intrinsic in the VM compiler,
  # but for the C backend or future VM versions it can be a regular call.
  # For now, we'll just provide placeholders if needed.
}

func logS(value: Any) {
}
