# Raw String Literals

This example demonstrates Rust-style raw string literals in Rae.

## Syntax
- `r"content"`
- `r#"content"#`
- `r##"content"##`

Raw strings do not process escape sequences (like `\n`) and can contain newlines and quotes safely by increasing the number of `#` characters.

