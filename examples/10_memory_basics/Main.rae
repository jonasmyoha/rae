type Point {
  x: Int
  y: Int
}

func main() {
  # 1. Value Type
  let p1: Point = { x: 10, y: 20 }
  # Copy
  let p2: Point = p1
  p2.x = 30
  # Prints 10
  log(p1.x)
  # 2. Borrow Type (mutable view)
  let pointRef: mod Point => p1
  pointRef.y = 50
  # Prints 50
  log(p1.y)
  # 3. Borrow Type (read-only view)
  let pointView: view Point => p1
  log(pointView.x)
}
