func main() {
  log("Opening database...")
  defer {
    log("Closing database connection.")
  }
  
  log("Begin transaction...")
  defer {
    log("End transaction (cleanup).")
  }
  
  let success: Bool = true
  if success {
    log("Processing data...")
    ret
  }
  
  log("This part is skipped because of 'ret' above.")
}
