# The editor's visual references (#1000): `make references` re-renders every
# sample headlessly and rewrites references/*.png; the example gate
# (compiler/tools/run_examples.sh) compares fresh screenshots against them.
.PHONY: references
references:
	bash tools/references.sh
