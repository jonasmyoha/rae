func main() {
  def numbers: List(Int) = {10, 20, 30, 40, 50}
  
  log(value: "Looping through list:")
  def i: Int = 0
  loop i < numbers.length() {
    def val: Int = numbers[i]
    log(value: val)
    i = i + 1
  }
}
