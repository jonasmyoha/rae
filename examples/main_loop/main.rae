# Main loop demo: log ticks until the process is stopped.
extern func nextTick(): ret Int

func main() {
  log("Main loop running (use Stop to exit)")
  while true {
    logS("tick ")
    log(nextTick())
  }
}
