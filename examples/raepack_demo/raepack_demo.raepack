pack RaePackDemo {
  format: "raepack"
  version: 1
  defaultTarget: live
  targets: {
    target live: {
      label: "Live"
      entry: "main.rae"
      sources: {
        source: {
          path: "."
          emit: live
        }
      }
    }
    target compiled: {
      label: "Compiled"
      entry: "main.rae"
      sources: {
        source: {
          path: "."
          emit: compiled
        }
      }
    }
  }
}
