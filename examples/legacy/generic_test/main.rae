type Box(T) {
  value: T
}

func setValue(T)(b: mod Box(T), val: T) {
  b.value = val
}

func main() {
  # Int Box
  let b: Box(Int) = { value: 42 }
  # Float Box
  log(b.value)
  setValue(b, val: 100)
  log(b.value)
  let bf: Box(Float) = { value: 3.14 }
  # String Box
  log(bf.value)
  setValue(bf, val: 2.718)
  log(bf.value)
  let bs: Box(String) = { value: "hello" }
  log(bs.value)
  setValue(bs, val: "world")
  log(bs.value)
}
