pack AdvancedPong {
  format: "raepack"
  version: 1
  defaultTarget: compiled
  targets: {
,
    target compiled: {
      label: "Compiled"
      entry: "main.rae"
      sources: {
        source: { path: "main.rae", emit: compiled }
        source: { path: "ai.rae", emit: compiled }
      }
    }
  }
}
