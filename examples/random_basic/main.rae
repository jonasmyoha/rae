func main() {
  log("--- Random Basic Example ---")
  
  # Set a seed for deterministic results
  seed(n: 123)
  log("Seed set to 123")

  log("Three random floats (0.0 to 1.0):")
  log(random())
  log(random())
  log(random())

  log("Three random integers (1 to 100):")
  log(randomInt(min: 1, max: 100))
  log(randomInt(min: 1, max: 100))
  log(randomInt(min: 1, max: 100))
}
