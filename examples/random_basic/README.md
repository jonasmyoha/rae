# Random Basic

Demonstrates basic random number generation using the `math` library.

## Functions used:
- `seed(n: Int)`: Seeds the PRNG.
- `random(): Float`: Returns a float between 0.0 and 1.0.
- `randomInt(min: Int, max: Int): Int`: Returns an integer between `min` and `max` (inclusive).
