pack AdvancedPong {
  format: "raepack"
  version: 1
  defaultTarget: live
  targets: {
    target live: {
      label: "Live"
      entry: "main.rae"
      sources: {
        source: { path: "main.rae", emit: live }
        source: { path: "ai.rae", emit: live }
      }
    }
  }
}
