pack RaePackDemo {
  format: "raepack"
  version: 1
  defaultTarget: compiled
  targets: {
    target compiled: {
      label: "Compiled"
      entry: "Main.rae"
      sources: {
        source: {
          path: "."
          emit: compiled
        }
      }
    }
  }
}
