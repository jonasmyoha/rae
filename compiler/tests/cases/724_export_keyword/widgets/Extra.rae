# `export` merges this file's symbols into the folder's same-named main module
# `Widgets`, so widgets.Widgets.extraValue() resolves even though it lives here (#787).
export
func extraValue() ret Int { ret 41 }
