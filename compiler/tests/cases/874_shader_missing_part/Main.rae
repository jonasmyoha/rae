# A part that does not exist is a compile error at the shader(...) line,
# naming what was tried — not "shader file not found" printed frames later.
func main() {
  let lit: Shader = shader(files: ["assets/helper.wgsl", "lib/no_such_shader.wgsl"])
  log("unreachable {lit.generation}")
}
