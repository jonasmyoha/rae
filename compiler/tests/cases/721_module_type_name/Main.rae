func main() {
  # `Widget` before `:` is the type; before `.` it is the namespace. Both resolve
  # in the same expression even though the folder is named after the type.
  let w: Widget = Widget.makeWidget(n: 7)
  log("widget value = {Widget.widgetValue(w: w)}")
}
