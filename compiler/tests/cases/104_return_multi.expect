MODULE
  func divide
    param a: Int
    param b: Int
    return result: Int
    return error: opt Error
    body
      if (b is 0)
        then
          ret error: "divide by zero"
      ret result: (a / b)
