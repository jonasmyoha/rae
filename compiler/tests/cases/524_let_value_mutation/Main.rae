import core
type P {
    x: Int
    y: Int
}
func main() {
    let p: P = { x: 1, y: 2 }
    p.x = 9
    let items: List(Int) = createList(cap: 2)
    items.add(value: 7)
    log("px={p.x} items={items.length}")
}
