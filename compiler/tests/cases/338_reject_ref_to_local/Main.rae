type Point {
  x: Int
}

func badMod() ret mod Int {
  let p: Point = { x: 1 }
  # ERROR: reference escapes local storage
  ret mod  p.x
}

func badView() ret view Int {
  let p: Point = { x: 1 }
  # ERROR: reference escapes local storage
  ret view  p.x
}

func main() {
  log("should not run")
}
