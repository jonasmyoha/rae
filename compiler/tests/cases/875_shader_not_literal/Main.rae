# The parts must be a literal list of string literals: a shader is a
# build-time fact, composed and validated before the program exists.
func main() {
  let name: String = "assets/helper.wgsl"
  let lit: Shader = shader(files: [name])
  log("unreachable {lit.generation}")
}
