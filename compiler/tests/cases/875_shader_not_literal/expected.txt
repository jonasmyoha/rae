tests/cases/875_shader_not_literal/Main.rae:5:27: shader(files: [...]) takes one argument, 'files', a literal list of string literals naming the WGSL parts in order (e.g. shader(files: ["lib/noise.wgsl", "assets/main.wgsl"])) — a shader is composed and validated at build time, so its parts cannot be computed
     5 |   let lit: Shader = shader(files: [name])
       |                           ^~~~
