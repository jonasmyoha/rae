import time

func main() {
  log(formatTimestamp(epochMs: 0))
  log(formatTimestamp(epochMs: 1000))
  log(formatTimestamp(epochMs: 86400000))
  log(formatTimestamp(epochMs: 1700000000000))
  log(formatDate(epochMs: 0))
  log(formatDate(epochMs: 1700000000000))
}
