type Point {
  x: Int
}

func getX(point: mod Point) ret mod Int {
  # OK: derived from param
  ret mod  point.x
}

func main() {
  let point: Point = { x: 1 }
  let rx: mod Int => getX(point: point)
  rx = 10
  # expect 10
  log(point.x)
}
