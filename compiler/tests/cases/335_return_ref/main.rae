type Point {
  x: Int
}

func getX(p: mod Point) ret mod Int {
  # OK: derived from param
  ret mod  p.x
}

func main() {
  let p: Point = { x: 1 }
  let rx: mod Int => getX(p: p)
  rx = 10
  # expect 10
  log(p.x)
}
