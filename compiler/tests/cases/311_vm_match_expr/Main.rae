func main() {
  let x: Int = 1
  let value: Int = match x { case 1 => 10, default => 20 }
  log(value)
  let s: String = "a"
  let strResult: String = match s { case "a" => "got_a", default => "other" }
  log(strResult)
}
