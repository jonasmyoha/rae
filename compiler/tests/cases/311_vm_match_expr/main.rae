func main() {
  def x: Int = 1
  def value: Int = match x { case 1 => 10, default => 20 }
  log(value)
  def s: String = "a"
  def strResult: String = match s { case "a" => "got_a", default => "other" }
  log(strResult)
}
