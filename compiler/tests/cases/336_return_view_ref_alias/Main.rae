type Point {
  x: Int
}

func xView(point: view Point) ret view Int {
  ret view  point.x
}

func main() {
  let point: Point = { x: 7 }
  let r: view Int => point.xView()
  # expect 7
  # expect 7
  log(r)
  log(point.x)
}
