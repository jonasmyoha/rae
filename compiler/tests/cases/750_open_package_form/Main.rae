# The package form: `open ecs` covers every module under ecs/, so both the
# ecs/World constructor and an ecs/Entity helper are bare-callable.
open ecs

func main() {
  var allocator: EntityAllocator = createEntityAllocator()
  log("alive={allocatorAliveCount(allocator: allocator)}")
}
