func main() {
  def value: Int = match 2 {
    case 1 => 10
    case _ => 20
  }
  log(value: value)

  def strResult: Int = match "hi" {
    case "hi" => 42
    case _ => 0
  }
  log(value: strResult)
}
