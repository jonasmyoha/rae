import core

# The keyword is `cap:`, matching createList(T, cap: N) so migrating a
# List to an Array is a one-token edit. Any other keyword is an error.

func main() {
  let a: Array(Float, count: 16) = Array(Float, count: 16)
  log("unreachable")
}
