/* Generated by Rae C backend (experimental) */
#include <stdio.h>
#include <stdint.h>
#include <string.h>

static void report(int64_t value);

static void report(int64_t value) {
  if ((value > 3)) {
  puts("value greater than three");
  fflush(stdout);
  } else {
  puts("value small");
  fflush(stdout);
  }
}

int main(void) {
  report(5);
  report(2);
  return 0;
}

