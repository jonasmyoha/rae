import core

# #772: `any` is the compile-time wildcard, legal ONLY as a fields() loop
# binding pattern. Using it as a real type must be rejected.
func takesAny(value: any) {
  log("nope")
}

func main() {
  takesAny(value: 3)
}
