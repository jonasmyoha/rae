import core

# Rae's rule: type arguments are positional, value arguments are named.
# A bare number in a generic argument list is rejected with a message that
# names the fix, rather than the generic "expected type".

func main() {
  let a: Array(Float, 16) = Array(Float, 16)
  log("unreachable")
}
