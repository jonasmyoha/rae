import core

# `cap:` must be a COMPILE-TIME constant. A runtime `var` cannot size a
# type, and the diagnostic must say so rather than failing later in codegen.

func main() {
  var n: Int = 4
  let a: Array(Float, cap: n) = Array(Float, cap: n)
  log("unreachable")
}
