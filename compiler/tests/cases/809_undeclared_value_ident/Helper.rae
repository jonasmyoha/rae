# A sibling module Main opens: recordScore is a genuine, cross-file function,
# so the call below resolves fine — only the ARGUMENT value is the bug.
func recordScore(total: mod Int, delta: view Int) {
  total = total + delta
}
