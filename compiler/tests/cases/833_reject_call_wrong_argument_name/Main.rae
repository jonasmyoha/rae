# #926: the name at a call site is CHECKED information, not decoration. This
# used to compile, bind `gamma` positionally to `beta` and print 3.
func takesTwo(alpha: view Int, beta: view Int) ret Int {
  ret alpha + beta
}

func main() {
  log("{takesTwo(alpha: 1, gamma: 2)}")
}
