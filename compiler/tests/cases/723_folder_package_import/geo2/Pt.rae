# Folder-package `geo2` (auto-scanned as part of the project), reached via the
# 3-level qualifier geo2.Pt.mk(...) — folder namespace + module + func (#786/#787).
type Pt { x: Int }
func mk(x: copy Int) ret Pt { ret Pt { x: x } }
func getx(p: view Pt) ret Int { ret p.x }
