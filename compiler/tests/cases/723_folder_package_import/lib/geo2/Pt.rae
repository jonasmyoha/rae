# A lib/ folder-package (auto-scan skips lib/, so `import geo2` is required to
# load its modules). Reached via `geo2.Pt.mk(...)` — folder import + 3-level. (#786)
type Pt { x: Int }
func mk(x: copy Int) ret Pt { ret Pt { x: x } }
func getx(p: view Pt) ret Int { ret p.x }
