enum Color {
  Red,
  Green,
  Blue
}

func main() {
  def c: Int = Color.Green
  log(c)
  
  if c is Color.Red {
    log("Red")
  } else {
    if c is Color.Green {
      log("Green")
    } else {
      log("Blue")
    }
  }
}
