/* Generated by Rae C backend (experimental) */
#include <stdint.h>
#include <string.h>
#include "rae_runtime.h"

static void doubleValue(int64_t value);
static void helper(int64_t input);
static void sumChain(int64_t a, int64_t b, int64_t c);

static void doubleValue(int64_t value) {
  int64_t doubled = (value * 2);
  helper(doubled);
}

static void helper(int64_t input) {
  rae_log_cstr("Helper ran");
}

static void sumChain(int64_t a, int64_t b, int64_t c) {
  rae_log_cstr("sumChain start");
  int64_t total = ((a + b) + c);
  doubleValue(total);
  rae_log_cstr("sumChain done");
}

int main(void) {
  rae_log_cstr("C backend demo start");
  sumChain(1, 2, 3);
  rae_log_cstr("C backend demo end");
  return 0;
}
