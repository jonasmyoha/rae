/* Generated by Rae C backend (experimental) */
#include <stdio.h>
#include <stdint.h>
#include <string.h>

static void doubleValue(int64_t value);
static void helper(int64_t input);
static void sumChain(int64_t a, int64_t b, int64_t c);

static void doubleValue(int64_t value) {
  int64_t doubled = (value * 2);
  helper(doubled);
}

static void helper(int64_t input) {
  puts("Helper ran");
  fflush(stdout);
}

static void sumChain(int64_t a, int64_t b, int64_t c) {
  puts("sumChain start");
  fflush(stdout);
  int64_t total = ((a + b) + c);
  doubleValue(total);
  puts("sumChain done");
  fflush(stdout);
}

int main(void) {
  puts("C backend demo start");
  fflush(stdout);
  sumChain(1, 2, 3);
  puts("C backend demo end");
  fflush(stdout);
  return 0;
}
