# This file has no `unsafe` token at all (the old staging never opted it in);
# calling the unsafe extern declared in the sibling file outside a block is
# still rejected.
func main() {
  let result: Int = nativeAbs(value: 0 - 4)
  log("{result}")
}
