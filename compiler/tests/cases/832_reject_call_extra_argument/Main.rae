# #926: a call with one argument too many is a sema error at the call site.
func takesTwo(alpha: view Int, beta: view Int) ret Int {
  ret alpha + beta
}

func main() {
  log("{takesTwo(alpha: 1, beta: 2, gamma: 3)}")
}
