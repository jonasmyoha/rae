
# An innocent sibling. Because the project entry is loaded FIRST and siblings
# after it, this file is the merged module's LAST-loaded file — the one the
# old diagnostics named for every error in the project (#815).
type Pos { x: Int }

func describe(p: view Pos) ret String {
  ret "x={p.x}"
}
