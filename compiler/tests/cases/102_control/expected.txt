MODULE
  func classify
    param n: Int
    return result: String
    body
      if (n < 0)
        then
          ret result: "negative"
        else
          match n
            case 0
              ret result: "zero"
            default
              expr (spawn heavyWork(idx: n))
              ret result: "positive"