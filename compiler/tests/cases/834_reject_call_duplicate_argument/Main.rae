# #926: naming the same parameter twice is a sema error (this exact shape
# hid in lib/compress/Deflate.rae: `putBits(this: w, value: 1, value: 2)`).
func takesTwo(alpha: view Int, beta: view Int) ret Int {
  ret alpha + beta
}

func main() {
  log("{takesTwo(alpha: 1, alpha: 2)}")
}
