func main() {
  let m: StringMap(Int) = createStringMap(initialCap: 4)
  set(m, k: "a", value: 1)
  set(m, k: "b", value: 2)
  set(m, k: "c", value: 3)
  
  log("Initial keys count: {m.keys().length()}")
  
  remove(m, k: "b")
  log("Keys after remove 'b': {m.keys().length()}")
  
  let ks: List(String) = m.keys()
  let i: Int = 0
  loop i < ks.length() {
    let k: String = ks.get(index: i)
    log("Key {i}: {k} -> {m.get(k: k)}")
    i = i + 1
  }
}
