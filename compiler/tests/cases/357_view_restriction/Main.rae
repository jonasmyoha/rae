type Point {
  x: Int
}

func testAssign(point: view Point) {
  point = { x: 10 } # Error: cannot assign to view
}

func testMutate(point: view Point) {
  point.x = 10 # Error: cannot mutate field of view
}

func main() {
  let pt: Point = { x: 1 }
  testAssign(pt)
  testMutate(pt)
}
