import core

# Value generic arguments are a builtin capability for now — Array is the
# only consumer. A user generic given one must say so clearly rather than
# resolving the value as if it were a type.

func main() {
  let a: List(Float, cap: 4) = createList(Float, cap: 4)
  log("unreachable")
}
