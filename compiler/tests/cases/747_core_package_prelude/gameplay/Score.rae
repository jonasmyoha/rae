# A nested project module, also with NO import of core — log + List are bare.
func scored() {
  let ys: List(Int) = createList(Int, cap: 2)
  ys.add(value: 7)
  ys.add(value: 35)
  log("nested sum={ys.copyAtDefault(index: 0) + ys.copyAtDefault(index: 1)}")
}
