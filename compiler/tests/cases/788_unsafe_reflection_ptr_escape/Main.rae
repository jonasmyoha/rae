type NativeState {
  pointer: Ptr
}

func inspect(this: view NativeState) {
  loop let field: view any in fields(this) {
    log("{field}")
  }
}

func main() {
  unsafe {
    log("activate boundary")
  }
}
