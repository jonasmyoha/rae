import core

# #665: a bare name defined in two project folders is a compile error — the
# programmer must qualify it (enemies.tick / particles.tick). No more silent
# "one wins".
func main() {
  log("{tick()}")
}
