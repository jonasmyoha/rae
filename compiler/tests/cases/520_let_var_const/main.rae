import core
const maxPlayers = 40
const doubled = maxPlayers * 2
func main() {
    let name = 7
    var count = 0
    count = count + 1
    count = count + 1
    var items: List(Int) = createList(cap: 2)
    items.add(value: 5)
    log("name={name} count={count} max={maxPlayers} doubled={doubled} items={items.length}")
}
