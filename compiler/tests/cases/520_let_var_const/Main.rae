const maxPlayers: Int = 40
const doubled: Int = maxPlayers * 2
func main() {
    let name: Int = 7
    var count: Int = 0
    count = count + 1
    count = count + 1
    var items: List(Int) = createList(cap: 2)
    items.add(value: 5)
    log("name={name} count={count} max={maxPlayers} doubled={doubled} items={items.length}")
}
