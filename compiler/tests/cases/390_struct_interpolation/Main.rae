type Point {
  x: Int
  y: Int
}

type Box {
  topLeft: Point
  size: Int
  label: String
}

func main() {
  let p: Point = { x: 10, y: 20 }
  log("point: {p}")
  log("toString: {p.toString()}")

  let b: Box = { topLeft: { x: 1, y: 2 }, size: 50, label: "lid" }
  log("box: {b}")
}
