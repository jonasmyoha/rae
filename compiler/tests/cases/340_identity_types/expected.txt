MODULE
  type Point
    field x: Int
  func main
    body
      let pid: id Point = 1
      let pkey: key Point = "p1"
      let opid: opt id Point = none
      let opkey: opt key Point = none
      expr log(pid)
      expr log(pkey)
