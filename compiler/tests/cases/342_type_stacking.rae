type Point { x: Int, y: Int }

func test(p: opt mod id Point) {
  log(value: "Stacked properties parsed correctly")
}

func main() {
  # This is just a parsing test for now
  log(value: "Parsing test")
}
