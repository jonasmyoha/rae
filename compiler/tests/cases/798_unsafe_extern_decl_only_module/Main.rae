# Helper.rae (a sibling file merged into this module) is declaration-only and
# has NO unsafe token; its plain `extern` is still rejected under #877.
func main() {
  var result: Int = 0
  unsafe {
    result = nativeAbs(value: 0 - 4)
  }
  log("{result}")
}
