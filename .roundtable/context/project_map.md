Rae is a single monorepo. Compiler/runtime live in `compiler/`, Rae libraries in
`lib/`, examples in `examples/`, benchmarks in `benchmarks/`, and Devtools Web
in `tools/devtools-web/`. The only active queue is root `QUEUE.md`.
